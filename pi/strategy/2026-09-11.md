# GHP daily strategy brief — 2026-09-11

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-09-11T14:11:05+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Fix the insights join — all 5 recent posts read `likes=? comments=? reach=?`**
- WHAT: The strategy brief is flying blind. Every post in the window shows unknown likes/comments/reach, so no data-driven prioritization is possible.
- WHO/WHERE: `automation/logs/ig_insights_*.json` (check freshness/most-recent timestamp) joined against `social/posted_archive.json` (media IDs 18016589321866793, 18089090465654289, 18141637246589801, 17937615456367750, 18390434311164341).
- WHY: Revenue-first requires knowing which hook converts. Right now the top of the funnel is measurable but the response layer isn't. Yesterday's brief flagged this system as "read-only" — it's silently reading nulls.
- HOW: Run the insights fetcher manually, inspect the newest `ig_insights_*.json` for those 5 media IDs, and if any are missing, debug the fetch (likely token/scope or media_id mismatch) before the next daily brief fires.

**2. Verify `untapped_warm=0` is real, not a classifier miss**
- WHAT: Count of 0 across 3 inbox sweeps in ~36h is either a genuinely quiet inbox or a broken classifier. Both need human eyes.
- WHO/WHERE: `social/engagement_log.json` inbox_sweep entries at 2026-09-10T13:44Z, 23:32Z, and 2026-09-11T13:37Z. Cross-check with IG app DM / follow-request tray on `raspberrypi5-ts`.
- WHY: If inbound engagement is truly zero while we're posting daily, that's the top revenue risk this week. If the classifier is silently dropping warms, we're leaving reciprocation on the table.
- HOW: Open IG on the account, count real DMs/follow requests from the last 36h, compare to `untapped_warm=0`. If they don't match, patch the classifier (likely a filter over-rejecting).

**3. Use the unused outbound engagement budget on the cabinet/pantry niche**
- WHAT: Log shows 0 follow / 0 like / 0 comment against caps of 10 / 30 / 5. Only inbox_sweeps happened. Outbound reach is idle while the queue burns supply.
- WHO/WHERE: Target ~5 accounts in the home-organization niche whose content mirrors the queue head (cabinet declutter, under-sink chaos, pantry reset). Log actions to `social/engagement_log.json`.
- WHY: 307-post queue depth means we're publishing faster than we're distributing. Free-tier growth requires seeding replies where our audience already is.
- HOW: Manually pick 5 accounts, drop 5 non-generic comments (respecting the 5/day cap), skip any `._community` / promo handles per the standing rule.

## Content angle to push
**"Specific dollar amount + solution that didn't work"** — the $1,400 mattress hook in media=18141637246589801 is the only recent post with a hard, concrete number in the first line. The other 4 open with time frames ("two whole years", "ten years", "longer than I want to admit") which are softer. Queue head (307 posts) leans on time-based openers ("Three weeks ago", "four years") — none lead with dollars. Add 3-5 dollar-anchored hooks in the next batch: "$X on Y, still had the problem, here's what actually fixed it." I can't cite likes/reach because insights are broken (action #1), so this is a structural bet on hook specificity, not measured performance — revisit after insights come back online.

## Watch-outs
- All 5 recent post insights are `?` — insights pipeline is stale or broken.
- `untapped_warm=0` across 3 sweeps — either genuinely quiet or classifier dropping.
- Engagement log shows ONLY inbox_sweeps in 36h — no outbound like/follow/comment activity logged; caps entirely unused.
- Queue at 307 with no measurable feedback loop — production outpacing signal.
