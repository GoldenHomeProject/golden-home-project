# GHP daily strategy brief — 2026-06-17

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-06-17T14:03:10+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

1. **Fix the IG comment selector — the comment cap is effectively 0/5.**
   - WHO/WHERE: `pi/dm-funnel/` comment_post() (Playwright). Failing targets on 2026-06-16T14:10-14:20Z: `simplyspaced`, `organizedbyellis`, `iheartorganizing`, `cindyology`, `breathing.room.home`, `shiragill`. Pattern: textarea resolves, but a `wbloks_*` Collection / `x1qjc9v5...` overlay / `role="dialog" aria-modal="true"` intercepts pointer events → 30s timeout, 6/6 comments lost.
   - WHY: 100% comment failure rate in the 14:10-14:20Z sweep window = the highest-trust engagement lever (5 comments/day cap) produced zero touches yesterday. Likes/follows are commodity; comments on tier-1 organizer accounts (iheartorganizing, simplyspaced, shiragill) are the actual relationship signal.
   - HOW: Before clicking the textarea, dismiss the modal/overlay first (`page.keyboard.press("Escape")` on the dialog, or wait for `[role="dialog"]` to detach, then re-locate textarea); fallback to `textarea.focus()` + `page.keyboard.type(...)` instead of `.click()`. Re-run against one target manually before re-enabling.

2. **Manually comment on the 6 burned tier-1 targets today (human-in-the-loop, not via Pi).**
   - WHO/WHERE: `@simplyspaced`, `@organizedbyellis`, `@iheartorganizing`, `@cindyology`, `@breathing.room.home`, `@shiragill` — the exact posts the Pi queued. iheartorganizing also threw `no_message_btn` at 18:15Z, so the DM path to that account is also blocked.
   - WHY: These were already algorithmically picked as warm targets and the day's 5-comment budget is unspent. Recovering the touch from a human session preserves the relationship signal while the selector bug is being fixed.
   - HOW: Pull the post URLs from `social/engagement_log.json` for the 14:10-14:20Z window and comment manually (≤5, stay within cap). Skip iheartorganizing DM until message-button selector is also re-verified.

3. **Reconcile insights — last 5 posts are all `likes=? comments=? reach=?`.**
   - WHO/WHERE: `automation/logs/ig_insights_*.json` join against `social/posted_archive.json` for media ids 18080292026210433, 18086771459434877, 18454135165115198, 18169107988438159, 18073147433680217.
   - WHY: Five consecutive posts with null metrics means the strategy brief is flying blind — "content angle to push" is currently a guess, not a data call. This is the single thing that unblocks every future brief.
   - HOW: Run the insights fetcher manually (or check the GHActions insights workflow's last run); if the IG insights token/scope is stale, that's the root cause and needs eyes before tomorrow's brief.

## Content angle to push

**"The sponge is destroying your nonstick pan" / single-object diagnosis hooks.** Three of the last five captions follow the same shape — *"The reason your X stays Y isn't the Z you think — it's [unexpected single object]"* (cabinets/nothing-has-a-home, nonstick/sponge, under-sink/bottles). It's the strongest pattern in the queue (mirrored in the queue head too) and reads as the house voice. Next angle: pick one more "wrong-culprit" reveal — candidate: **drawer slides vs. drawer contents** ("The reason your kitchen drawers won't close isn't what's in them — it's the slides nobody adjusts in 10 years"). Caveat: we can't yet confirm this pattern is *working* because insights are null for all 5 posts (see action 3) — push it one more day, then re-evaluate once metrics are back.

## Watch-outs

- 6/6 comment_post failures + 1 DM failure on 2026-06-16 = IG DOM changed; the entire comment funnel is silently broken, and the watchdog didn't flag it.
- All 5 recent posts report `likes=? comments=? reach=?` — insights pipeline is degraded.
- `untapped_warm count=0` — either genuinely caught up, or the inbox sweep isn't tagging warm prospects correctly; worth a spot-check given the DM/comment errors on the same accounts.
- `social/dm_keyword_registry.json.bak-20260611` sitting in the tree for 6 days — leftover from a manual edit; either commit or delete.
