# GHP daily strategy brief — 2026-08-24

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-08-24T14:10:53+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

1. **Backfill post insights so this brief has real numbers to reason on.**
   - WHO/WHERE: `automation/logs/ig_insights_*.json` + `social/posted_archive.json`; media IDs 17904995907480402, 17970042372006033, 18402009514095260, 17878511532527316, 18610931881053410.
   - WHY: All 5 most recent posts show `likes=? comments=? reach=?`. Without insights, strategy is flying blind — cannot tell which of the two duplicate "low-grade mess" hooks (kitchen vs closet) is actually pulling.
   - HOW: Run the IG insights fetch job manually against those 5 media IDs and rejoin into the archive; if the joiner is silently dropping, add a one-line log of the join key mismatch.

2. **De-dupe the queue's "low-grade mess / things went to die" hook before it burns the pattern.**
   - WHO/WHERE: `social/post_queue.json` head — "Three weeks ago this cabinet was where things went to die…" duplicates the archived kitchen (17904995907480402) and closet (18610931881053410) posts almost verbatim; queue_size=262 so plenty of alternatives exist.
   - WHY: Same opener twice in <5 posts trains the algorithm to treat it as boilerplate; revenue-relevant impressions get suppressed.
   - HOW: Pop that caption to position ~40 and promote a distinct-hook post (sleep or neck-pain lane) into the next slot.

3. **Prime tomorrow's follow budget from DM/inbox sweep signal, not a cold list.**
   - WHO/WHERE: `social/engagement_log.json` — only 1 follow (`ani_sachi3`) and 1 DM (`simplyspaced`) in 36h; untapped_warm count=0.
   - WHY: Warm queue is empty and follow budget (10/day) is 90% unused — the sweep is running but not converting DMs/replies into a warm list, so the cap is meaningless.
   - HOW: Manually seed 5–8 handles from the last `simplyspaced` DM thread + IG notifications tab into the untapped_warm source the sweep reads, then let the 14:00 sweep pick them up (stay well under 10 follow / 30 like / 5 comment).

## Content angle to push
**Sleep/neck-pain lane, not another "low-grade mess" org post.** The archive shows 3 of 5 recent captions are org-clutter hooks (kitchen, under-sink, closet — all near-identical openers), while the 2 sleep-adjacent posts (hot-sleeper sheet upgrade 17970042372006033, neck-pain/mattress 18402009514095260) are the *only* differentiated angles in the last 5. Revenue intent is higher on sleep (mattress toppers, cooling sheets are top affiliate categories). Push one concrete, product-anchored sleep post next — e.g. "the pillow height that fixed my morning neck pain" — before shipping the 4th org-clutter variant sitting at queue head.

## Watch-outs
- All 5 recent posts have null likes/comments/reach → insights join is broken or the fetch job hasn't run; fix before tomorrow's brief or it's the same output.
- untapped_warm=0 despite an active DM yesterday → inbox sweep is logging but not populating the warm list; check the extractor.
- Queue head has 2 near-duplicate "things went to die" cabinet hooks in the first 3 slots — content generator may be stuck in a template.
- 3 `.bak` files created in `social/` today (post_queue, posted_archive, copy_library) — normal rotation, but worth confirming nothing rolled back state.
