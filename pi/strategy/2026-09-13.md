# GHP daily strategy brief — 2026-09-13

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-09-13T14:03:22+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

1. **Fix / investigate IG insights ingestion — blocking all data-driven decisions**
   - WHO/WHERE: `automation/logs/ig_insights_*.json`, joined against `social/posted_archive.json` for media IDs `18089090465654289`, `18141637246589801`, `17937615456367750`, `18390434311164341`, `17985194421097908`.
   - WHY: All 5 most recent posts show `likes=?  comments=?  reach=?`. Without insights the daily brief can't rank content angles or spot dying reach — the whole "judgment" purpose of this briefing collapses.
   - HOW: Manually run the insights fetcher for those 5 media IDs and check whether Graph API is 400ing (token scope) or the join key is stale.

2. **Diagnose `untapped_warm=0` — either genuine dead air or a broken query**
   - WHO/WHERE: latest inbox sweep at `2026-09-13T13:38:48Z` (self), producer of the `untapped_warm` count.
   - WHY: Two consecutive sweeps and zero warm prospects, combined with zero follow/like/comment actions in 36h, is either a real engagement drought (revenue risk) or a silent detection bug. Both need eyes today. Warm reciprocation is the cheapest, cap-safe revenue lever we have.
   - HOW: Re-run the inbox sweep with debug logging and manually spot-check the last 20 DMs/mentions/story replies against what the sweep classified.

3. **Diversify one caption in the 309-deep queue to break the formula**
   - WHO/WHERE: `social/post_queue.json`, top 3 items (cabinet-of-death, 4am neck pain, bottles-under-sink) — all copies of the same confession→relief arc already dominant in the last 5 posted items.
   - WHY: Queue is 309 deep and the last 5 posts + next 3 all use the same "low-grade mess I ignored for years" opener. Even without insights we can see mode collapse; one different angle lets us A/B whatever we can eventually measure once (1) lands.
   - HOW: Rewrite one top-of-queue caption to lead with a specific product + specific dollar/time save (revenue-first framing) instead of the confession opener.

## Content angle to push
**Specific-product, specific-save posts** — e.g. "This $18 tension rod under the sink saved me from replacing the cabinet." I can't cite real reach numbers because insights are broken across all 5 posted media, but qualitatively the queue and archive are 100% confession-arc captions with no product-first hook. Revenue-first constraint says the next slot should test a caption that names the product in line 1 and the payoff in line 2, so the affiliate click has a reason to happen before the reader scrolls past.

## Watch-outs
- Insights are `?` on all 5 recent posts (fetcher likely broken); `untapped_warm=0` two sweeps running; engagement log shows only sweeps, zero follow/like/comment actions in 36h — caps are nowhere near hit, meaning we're leaving the daily 10/30/5 budget entirely on the table.
