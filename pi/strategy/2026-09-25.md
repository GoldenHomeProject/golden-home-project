# GHP daily strategy brief — 2026-09-25

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-09-25T14:10:10+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Fix the missing IG insights ingestion — the whole strategy loop is blind without it**
- WHO/WHERE: `automation/logs/ig_insights_*.json`, media IDs `18118326971473956`, `18092239964433786`, `18104582033217107`, `18193673143348615`, `17990436333023891`
- WHY: All 5 recent posts show `likes=? comments=? reach=?`. Yesterday's "brief" was actually the install README, meaning this is effectively the first run and no historical performance signal exists. Every downstream decision (which angle to push, which queue items to promote) is a guess until this is restored.
- HOW: SSH the Pi, tail the latest `ig_insights_*.json`, confirm the fetch job ran and Graph API token isn't expired — `ssh raspberrypi5-ts 'ls -lt ~/golden-home-project/automation/logs/ig_insights_*.json | head -5 && tail -50 <newest>'`.

**2. Manually audit whether the inbox sweep is actually surfacing anyone**
- WHO/WHERE: `social/engagement_log.json` — only two `inbox_sweep self` entries in 36h, zero follows/likes/comments logged; untapped_warm count=0
- WHY: Caps allow 10/30/5 per day and we're running 0/0/0. Either (a) the sweep truly finds nobody (account visibility problem — matches the missing insights), or (b) the sweep found people but the reciprocation step is silently no-op'ing. Both are revenue-blocking; both need a human eye, not another script.
- HOW: Open IG on phone, check notifications tab for the last 36h of real engagements, and cross-reference against `social/engagement_log.json` to see if the sweep is dropping them.

**3. Pull one high-performing evergreen from the 326-post queue for a manual push instead of sequential drip**
- WHO/WHERE: `social/post_queue.json` head — cabinet-under-the-sink caption ("Three weeks ago this cabinet was where things went to die") and the 4am-neck-pain pillow hook
- WHY: 326-deep queue means good hooks are sitting 40+ days out. The pillow/sleep angle already appeared in recent posts (`18104582033217107` comforter) and the pain-point-first hook style is our strongest format historically. Front-loading a proven angle beats waiting.
- HOW: In `social/post_queue.json`, promote the 4am-neck-pain pillow post to next slot after checking it has an affiliate link attached.

## Content angle to push
**Sleep/bedroom pain-points (pillow, comforter, cooling)** — one of the last 5 posts (`18104582033217107`, hot-sleeper comforter) is in this lane and the queue head has a strong 4am-neck-pain pillow hook. Sleep is high-intent, year-round, and Amazon has deep affiliate coverage. Without insights numbers I can't confirm CTR yet, but the recurring appearance in both recent posts and queue suggests the content engine has already converged on it — lean in with 2-3 sleep posts this week instead of scattering across closets/kitchens.

## Watch-outs
- All 5 recent posts return `likes=? comments=? reach=?` — insights fetch is silently broken.
- Zero non-sweep engagement actions in 36h despite 10/30/5 caps — sweep may be no-op'ing or account has zero inbound.
- Yesterday's "brief" is the install README, not a real prior brief — no drift check possible against actual prior guidance.
- 326-item queue with no performance signal means we're posting blind at scale.
