# GHP daily strategy brief — 2026-06-15

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-06-15T14:07:20+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

1. **WHAT:** Pause comment_post in the sweep until the modal/overlay selector is fixed — 6 consecutive comment failures yesterday (breathing.room.home, simplyspaced, cindyology, shiragill, organizedbyellis, iheartorganizing) all due to a `wbloks` Collection/dialog div intercepting pointer events on `textarea[aria-label*='comment']`. **WHO/WHERE:** `pi/dm-funnel/` comment_post handler; gate the 5-comment quota off until the selector handles the dialog-overlay case (close any open `role="dialog"` or `bk.components.Collection` before clicking). **WHY:** 100% comment failure rate on 2026-06-14 14:04-14:15Z burns the daily budget on nothing AND each retry storm (50+ click attempts per post) is a strong bot signal to IG. **HOW:** Add a pre-click step that dismisses any `div[role="dialog"][aria-modal="true"]` and waits for `wbloks_*` to detach, or fall back to focusing the textarea via JS instead of click.

2. **WHAT:** Investigate why `untapped_warm = 0`. **WHO/WHERE:** inbox sweeps at 2026-06-14T13:32Z, 23:38Z, 2026-06-15T13:38Z all returned zero reciprocity candidates; check `social/engagement_log.json` + the inbox_sweep query. **WHY:** Either (a) the funnel is genuinely starved — which means we're posting but not getting warm signals, a revenue red flag — or (b) the sweep is broken and silently returning empty. Three sweeps in a row at zero is suspicious given queue_size=49 and active posting. **HOW:** Manually open IG DMs + recent post comments and cross-check against what the sweep reports for the same window.

3. **WHAT:** Fix the IG insights pipeline — last 5 posts all show `likes=?  comments=?  reach=?`. **WHO/WHERE:** `automation/logs/ig_insights_*.json` join in `pi/strategy/` brief script; media IDs 18123193945593946, 18117625282835495, 18080292026210433, 18086771459434877, 18454135165115198 all missing metrics. **WHY:** Without engagement numbers we cannot tell which content angle is working — every "content angle to push" decision after this is guesswork, and the queue (49 deep) keeps draining blind. **HOW:** Check whether the IG Graph insights pull is running and writing fresh files; if the script is alive but media_id join is empty, log the lookup misses to find the format mismatch.

## Content angle to push

**"Nothing has a home" / system-not-storage angle.** Three of the last five captions hammer this thesis — "Buying more containers will never fix your under-sink cabinet," "The reason your cabinets stay messy is that nothing has a home," "Three weeks ago this cabinet was where things went to die" — and the queue head repeats the under-sink + cabinet framing. We have no insights to confirm what's working, but the content engine is converging on this thesis on its own. Push the next variant toward **pantry or bathroom drawers** (new room, same thesis) so we get a clean signal on whether the angle generalizes or is just cabinet-specific. Avoid another under-sink for at least 3 posts.

## Watch-outs

- 6/6 comment_post calls failed on 2026-06-14 with the same dialog/overlay interception — selector is broken, not flaky.
- All 5 recent posts have null likes/comments/reach in the insights join — strategy brief is flying blind.
- `untapped_warm = 0` across 3 consecutive sweeps — either funnel is dry or sweep is broken.
- DM error on simplyspaced (no_message_btn) — account may have message-restricted us or layout drifted.
