# GHP daily strategy brief — 2026-09-01

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-09-01T14:05:42+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

1. **Fix insights collection — every post shows `likes=? comments=? reach=?`**
   - WHO/WHERE: `automation/logs/ig_insights_*.json` + whatever writer feeds `social/posted_archive.json` join. Media IDs affected: 18028642553675299, 18077127896341641, 17903975967329994, 18109078079117864, 18001186088795007.
   - WHY: Every one of the last 5 posts is missing metrics. Without likes/reach the "Content angle to push" section is guesswork and the whole strategy loop is flying blind — this is the highest-leverage fix because it unblocks all future briefs.
   - HOW: SSH the Pi, run the insights fetcher manually against one media ID and inspect the response — either the IG token scope changed, the graph endpoint 404'd, or the join key drifted.

2. **Prime the untapped-warm pipeline — `count=0` after 3 inbox sweeps is suspicious**
   - WHO/WHERE: `social/engagement_log.json` inbox_sweep entries at 2026-08-31T13:44Z, 2026-08-31T23:46Z, 2026-09-01T13:38Z. Only 2 outbound comments in 36h (neatmethod, breathing.room.home), way under the 5/day cap.
   - WHY: Warm-prospect reciprocation is the cheapest conversion vector. Zero warm across three sweeps means either the sweep is broken or we've already replied to everyone — both need eyeballs. Meanwhile we're burning only ~40% of the comment budget.
   - HOW: Manually open IG DMs + notifications, pick 3 real warm handles (non-`._community`), and hand-comment on their latest post — treat as ground truth to validate the sweep.

3. **Queue is 285 deep but posting only ~1/day — decide whether to raise post cadence or prune**
   - WHO/WHERE: `social/post_queue.json` head (cabinet-death, 4am-neck, bottles-on-feet — all clutter/pain-point angles, no product/monetization tie).
   - WHY: 285 captions is ~9 months of runway at current pace. Revenue-first constraint means content should be feeding a funnel, not a backlog. Either post 2x/day for a week to test whether reach scales, or prune stale captions.
   - HOW: Look at last 2 weeks of posted_archive timestamps — if it's truly 1/day, flip the GHActions cron to 2x/day for a 7-day A/B and compare reach.

## Content angle to push

**"Cabinet under the sink" — concrete before/after storage fix.** The queue head has *two* cabinet-under-sink captions ("things went to die", "four years of bottles falling on my feet") and one recent post used the same frame ("nothing has a home"). This is the tightest signal in the data that the content engine has already converged on it as a working angle. Ship a save-worthy carousel version with a specific product list (drawer risers, tension rod, over-door caddy — all subscription-agnostic) so it doubles as an affiliate-ready template. Cannot cite reach numbers because insights are broken (see action 1) — validating this angle depends on fixing that first.

## Watch-outs

- All 5 recent posts have `?` for likes/comments/reach — insights pipeline is silently degraded.
- `untapped_warm=0` after 3 sweeps in 36h — likely sweep bug, not real zero.
- Only 2 comments posted in 36h vs 5/day cap — under-utilizing the highest-conversion action.
- Queue depth 285 vs ~1 post/day cadence = 9-month backlog with no monetization angle in the head 3.
