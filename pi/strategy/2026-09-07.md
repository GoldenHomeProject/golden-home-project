# GHP daily strategy brief — 2026-09-07

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-09-07T14:03:48+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Fix the insights pipeline — likes/comments/reach all show `?`**
- WHAT: The last 5 posts have no engagement data attached (`likes=? comments=? reach=?`). Without this, every daily brief flies blind and content-angle recommendations are guesses.
- WHO/WHERE: `automation/logs/ig_insights_*.json` join in `pi/strategy/` briefing script; `social/posted_archive.json` for the 5 media IDs above (18627445102005153, 18188477200398066, 17910006384465924, 18016589321866793, 18089090465654289).
- WHY: Zero signal in today's brief for the most important input — post performance. This is the single biggest blocker to revenue-first decisions.
- HOW: SSH the Pi, tail the most recent `ig_insights_*.json`, confirm whether the fetch is failing or the join key mismatches, then patch the join.

**2. Manually seed 3-5 warm prospects into the untapped queue**
- WHAT: `untapped_warm count=0` after two inbox sweeps in 24h means either (a) nobody is engaging us, or (b) the sweep filter is too aggressive.
- WHO/WHERE: Inbox sweep logic; check `social/engagement_log.json` inbox_sweep entries at 2026-09-06T13:42:22Z and 23:42:56Z.
- WHY: If the warm-prospect pipeline is dry, the daily like/follow/comment caps go unused and outbound conversion stalls — that's the revenue path.
- HOW: Open IG notifications manually, list 5 recent non-`._community` accounts that liked/commented in last 48h, and drop them into whatever file the sweep reads as its output.

**3. Trim post queue backlog decision (queue_size=295)**
- WHAT: 295 queued captions at current cadence = many months of runway. If insights are broken (see #1), we're queuing blind.
- WHO/WHERE: `social/post_queue.json`.
- WHY: A too-large blind queue means winning angles won't get reinforced fast enough; losing angles keep shipping.
- HOW: Once #1 is fixed, sort last 30 days of posts by reach and reorder the queue head to lead with the top-performing angle family. No new content generation needed.

## Content angle to push

**"Two-years-of-avoidance → 20-minute fix" (personal-embarrassment hook).**
Three of the last five captions lean on this exact frame — "avoided opening this cabinet for two whole years," "four years of bottles falling on my feet," "low-grade mess for longer than I want to admit." That's the pattern the human is drafting into. The content engine should push more captions with a **specific time-of-avoidance number in the first line** and a concrete failed-attempt list ("tried clear bins, tried stackable trays, tried labeling"). Caveat: I cannot confirm this angle is *working* until insights (action #1) are restored — right now it's the most-frequent angle, not the proven-best.

## Watch-outs

- All 5 recent posts have `likes=? comments=? reach=?` — insights join is broken.
- `untapped_warm count=0` after 2 sweeps — pipeline dry or filter mis-tuned.
- Post queue at 295 is very deep relative to daily cadence — risk of shipping stale angles.
- 5 recent posts, at least 3 are cabinet/under-sink — thematic saturation risk; the 4th-of-July centerpiece post is already seasonally stale on 2026-09-07.
