# GHP daily strategy brief — 2026-06-04

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-06-04T14:09:31+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

1. **Fix the broken Message-button selector in the DM flow**
   - WHO/WHERE: `lifeinjeneral` failed at 2026-06-03T18:02:05Z with `no_message_btn` — Locator timeout on `get_by_role("button", name=re.compile(r"^Message$"))`. Likely IG DOM change affecting all DM sends, not just this one handle.
   - WHY: A broken DM path silently kills the affiliate-link delivery funnel — the whole CTA in the last 5 captions ("save this" / comment-for-link) depends on DM follow-through. Zero untapped_warm today could partly reflect DMs failing rather than no warm leads.
   - HOW: Add a fallback selector chain (`a[href*="/direct/t/"]`, `div[role="button"]:has-text("Message")`, profile-action-bar XPath) in `pi/dm-funnel/` and re-run against `lifeinjeneral` to verify.

2. **Manually backfill the DM that failed to lifeinjeneral**
   - WHO/WHERE: `@lifeinjeneral` — one outbound DM, well within the 5-comment / no-cap-affecting limit.
   - WHY: This person engaged us recently enough to enter the funnel; losing them to an 8s timeout wastes a warm signal we already paid for. Untapped_warm=0 means there is no other prospect competing for the slot.
   - HOW: Send the standard affiliate-link DM by hand from the phone today, log it in `social/engagement_log.json` as `dm_manual_backfill`.

3. **Resolve missing post insights (likes/comments/reach all `?`)**
   - WHO/WHERE: All 5 most recent posts (`18315795139302670`, `18400197637155262`, `18098496266140274`, `18465834370100122`, `18596156872042762`) report `likes=? comments=? reach=?` — the joiner against `automation/logs/ig_insights_*.json` is returning nothing.
   - WHY: Without per-post insights you (and this brief) are flying blind on which angle is actually working. Every downstream decision below is degraded until this is fixed.
   - HOW: Check whether the most recent `automation/logs/ig_insights_*.json` is being written, or whether the media-id join key drifted; one read of the latest log file will tell you which.

## Content angle to push

**Under-sink cabinet** — 3 of the last 5 captions are already on this theme (`18315795139302670`, `18098496266140274`, `18596156872042762`) and the next 2 queued posts continue it. Without insight numbers I cannot confirm it is working, but the queue is heavily concentrated here. **Push the content engine toward a side-sleeper / pillow-and-neck-pain angle next** (extending `18400197637155262` and `18465834370100122`, which broke the cabinet pattern) — higher AOV affiliate category (pillows, toppers) than organizers, and the "74% of us sleep on our sides" hook is a stronger save-driver than another bin tip. Revert this recommendation once insight data comes back and the cabinet posts prove out.

## Watch-outs

- DM selector broke on `lifeinjeneral` — assume all DMs are failing until proven otherwise.
- Every post in the last 5 has `likes=? comments=? reach=?` — insights pipeline is not joining.
- `untapped_warm=0` is suspicious given active posting + a DM failure today; verify it's real and not a sweep-side parse bug.
- Queue is 30 deep but heavily skewed to under-sink cabinet; diversify before fatigue.
