# GHP daily strategy brief — 2026-08-10

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-08-10T14:09:49+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

1. **Fix the ig_insights join so likes/comments/reach populate in this brief.**
   - WHO/WHERE: `automation/logs/ig_insights_*.json` → merged into `social/posted_archive.json` view used by `pi/strategy/`. Media IDs `18070239620714601`, `17925775788385747`, `18088070462180529`, `18095600717358600`, `18032886038830495` all returned `likes=? comments=? reach=?`.
   - WHY: 5-of-5 posts have null metrics. Every downstream judgment call (content angle, kill/keep, what to reshare) is blind until this works. This is the single highest-leverage fix — restores the whole feedback loop.
   - HOW: Run the insights fetcher manually against the latest 5 media IDs and confirm join key (media_id vs id) matches `posted_archive.json`; if fetcher hasn't run in 24h, kick it and re-run this brief.

2. **Verify `untapped_warm=0` is real, not a broken detector.**
   - WHO/WHERE: inbox sweep at `2026-08-10T13:31:11Z` in `social/engagement_log.json`; detector logic in `pi/dm-funnel/` (the `__pycache__` in git status hints recent code motion).
   - WHY: Two days of untapped_warm=0 with only 2 comments + 1 DM sent = either we're perfectly caught up (unlikely for an active IG account) or the "engaged us but we haven't reciprocated" query is silently returning empty. Yesterday's engagement (comments to `organizedbyellis`, `horderly`, DM to `simplyspaced`) shows outbound is fine, but the *inbound signal* is the revenue funnel.
   - HOW: Spot-check IG notifications manually for 3-5 recent likers/commenters on our posts, confirm whether they appear in the sweep's raw JSON before the "untapped" filter.

3. **Use the unspent comment + follow budget on the accounts we already know convert.**
   - WHO/WHERE: `organizedbyellis` and `horderly` — both got comments 2026-08-09; these are established organizing niche adjacents. Follow up on their newest post today with a second comment (different post, still in cap). Add `simplyspaced` follow-back if not already.
   - WHY: Yesterday burned 2/5 comments and 0/10 follows. Cap headroom is opportunity cost when the untapped_warm queue is empty — reinvest in accounts already showing reciprocation.
   - HOW: Manually queue 3 more comments (stay under 5/day) on organizing-niche accounts and 2-3 targeted follows from their recent commenters.

## Content angle to push
**Under-sink / under-cabinet "graveyard" transformations.** The queue head has two variants of this ("cabinet where things went to die", "four years of bottles falling on my feet") — this suggests the content engine already picked up the pattern is working, but we can't confirm because `media=18088070462180529` (the cabinet post) has `likes=? comments=? reach=?`. Push the next post as *before-photo-forward* under-sink or bathroom-cabinet reset with a save-worthy 3-product stack. If insights come back today (see action 1) and the cabinet post underperforms, kill the third variant in queue before it posts.

## Watch-outs
- All 5 recent posts have null insights — likely a broken insights fetch/join, not a real zero.
- `untapped_warm=0` two sweeps running — verify detector, not funnel.
- Queue is 228 deep — risk of stale/repetitive captions posting before we confirm which angles work.
- `social/pinterest_queue.json.bak`, `.bak2`, `.bak3` and multiple `.bak-2026...` files in git status — someone or something is churning backups; check whether a script is failing mid-write.
