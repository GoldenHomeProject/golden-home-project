# GHP daily strategy brief — 2026-06-06

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-06-06T14:02:53+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Fix the insights join — we are flying blind on post performance**
- WHAT: Repair the join between `social/posted_archive.json` and `automation/logs/ig_insights_*.json`. All 5 recent posts show `likes=? comments=? reach=?`.
- WHO/WHERE: Posts 18098496266140274, 18465834370100122, 18596156872042762, 17970202323079046, 17891284695540093 — none have insights attached. Check the strategy-brief script that builds this section + the insights fetcher that writes `automation/logs/ig_insights_*.json`.
- WHY: Every downstream decision (which angle to push, which post to boost organically, which to retire) depends on this. Today's brief literally cannot compare cabinet vs pillow performance.
- HOW: Tail the latest `ig_insights_*.json` and confirm media IDs match the archive; if the file is empty/stale, re-run the insights fetcher manually and rebuild today's brief.

**2. Verify untapped_warm=0 is real, not a sweep regression**
- WHAT: Confirm the inbox sweeps at 2026-06-05T23:43Z and 2026-06-06T13:37Z actually parsed comments/DMs, vs. silently failing and reporting zero.
- WHO/WHERE: `social/engagement_log.json` shows only the two `inbox_sweep self` entries with no follow/like/comment outputs in this window. Check the sweep logs the Pi writes alongside those entries.
- WHY: Zero warm prospects two days in a row while we're posting daily is unusual — could be a true quiet period, or the sweep stopped seeing engagers. The whole strategy depends on warm-prospect recycling.
- HOW: `ssh raspberrypi5-ts 'tail -200 ~/golden-home-project/pi/dm-funnel/*.log'` and grep for the two sweep timestamps; confirm non-zero candidates were seen, even if none qualified.

**3. Break the cabinet monoculture at the queue head**
- WHAT: Reorder `social/post_queue.json` so the next 3 posts are not all the same angle.
- WHO/WHERE: Queue head currently: cabinet, pillow/neck, cabinet (kitchen sink). Recent posts: 3 of last 5 were cabinet. Total queue depth = 32 so there is room to reshuffle without thinning.
- WHY: Posting 5+ near-identical cabinet variants in a row trains the algo on one niche and burns out followers who already saved the first one. Pillow content is the only diversifier we've shown recently.
- HOW: Promote one non-cabinet, non-pillow caption from deeper in the queue into position 2 or 3; leave the cabinet+pillow alternation but inject a third vertical (drawer / closet / bath organization — whatever is already written).

## Content angle to push

**Side-sleeper pillow angle, anchored on the "74% of us sleep on our sides" stat.** Two of the last five posts already lean on this hook (media 18465834370100122 and 17891284695540093), which means the content team has implicitly bet on it twice without insight data to confirm. Have the content engine generate the *next* pillow variant as a "what 30-degree neck angle does over a week" payoff post — same hook, stronger consequence frame — so when insights come back online (action #1) we can A/B this versus the cabinet angle on equal footing. Hard numbers to push next: nothing yet, because likes/reach are `?` across the board — flag this as the first thing to revisit tomorrow.

## Watch-outs

- Insights pipeline silently broken: 5/5 recent posts have `likes=? comments=? reach=?`.
- `untapped_warm=0` after two sweeps — possible parser regression, possible quiet inbox; needs verification, not assumption.
- Queue head is 2 cabinet posts in the top 3, on top of 3 cabinet posts in last 5 published → angle concentration risk.
- Engagement log shows only sweeps, no follow/like/comment actions for the window — confirm the daily caps aren't being missed entirely (under-utilization, not over).
