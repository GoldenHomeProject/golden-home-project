# GHP daily strategy brief — 2026-07-26

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-07-26T14:08:19+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Investigate the duplicate-caption cluster on the last 5 posts**
- WHO/WHERE: media IDs `18110992469058210`, `18099472732957681`, `18130778470554791`, `18122603191697557` — all lead with *"Buying more containers will never fix your under-sink cabinet."* Only `18133634410613772` (patio) differs. Check `social/posted_archive.json` and the poster in `social/pinterest/` / IG queue logic.
- WHY: 4 of the last 5 posts share the identical opening line. Either the content-engine is republishing the same caption across carousels (which will tank IG's dedup ranking) or A/B variants aren't being logged distinctly. Either way, we can't attribute revenue to an angle we can't tell apart.
- HOW: `jq '.[-5:] | .[] | {id, caption:.caption[0:80], asin}' social/posted_archive.json` — confirm whether these are different ASINs sharing one caption template, then either force caption variation in the generator or void the duplicates before more ship.

**2. Repair the insights pipeline — every one of the last 5 posts shows `likes=? comments=? reach=?`**
- WHO/WHERE: `automation/logs/ig_insights_*.json` (newest file) and whatever join the strategy script does against `posted_archive`.
- WHY: We are flying blind on what's working. Yesterday's brief was supposed to *use* this data to pick angles; without it every "content angle" recommendation is a guess. This is upstream of every revenue decision.
- HOW: `ls -lt automation/logs/ig_insights_*.json | head -3` — if the file is stale or empty, re-run the insights fetcher; if fresh, fix the media-id join in the strategy script.

**3. Reconcile the 2 follow_errors and the `untapped_warm=0` anomaly**
- WHO/WHERE: handles `pakhli_manua` (likely truncated — should be `pakhli_manual` or similar) and `mentalhealth.fams`; sweep code that emits `untapped_warm` from the inbox_sweep at `2026-07-26T13:33:57Z`.
- WHY: Truncated/invalid handles waste sweep budget silently. And `untapped_warm=0` after 3 sweeps in 24h is implausible given we're actively DM-ing and commenting — more likely the detector regressed than that we're perfectly caught up. If the warm-list is broken, we're leaving the highest-conversion prospects on the floor.
- HOW: grep the handle source (`social/dm_keyword_registry.json` or seed list) for `pakhli_manua`, fix truncation; then re-run one sweep with debug logging to confirm untapped_warm actually populates.

## Content angle to push
**Push the "4am pain / can't turn my neck" pillow angle from the queue head** — it's the only queued caption that opens with a visceral moment-in-time hook rather than a category noun ("cabinet", "patio"). We currently have zero shipped pillow/sleep content in the last 5 posts, and the under-sink angle is already saturating the feed (4/5 posts). Body-pain hooks convert on Amazon affiliate because the reader is in-market *right now*. Pull this one to the front of `social/post_queue.json`.

## Watch-outs
Insights blank on all 5 recent posts; 4/5 posts share identical caption; `untapped_warm=0` after 3 sweeps looks like a detector regression, not a real zero; `pakhli_manua` handle appears truncated; only 1 DM + 2 comments used against 5/30/10 caps — we're underutilizing headroom on the days the pipeline *is* working.
