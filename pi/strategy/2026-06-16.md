# GHP daily strategy brief — 2026-06-16

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-06-16T14:08:05+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

1. **Fix the IG comment selector — the entire comment leg is 0% functional.**
   - **WHO/WHERE:** `pi/dm-funnel/` comment_post handler. Failed targets in the last sweep: `simplyspaced`, `breathing.room.home`, `iheartorganizing`, `cindyology`, `shiragill`, `organizedbyellis` (all 14:06-14:16 UTC 2026-06-15).
   - **WHY:** 6/6 comment attempts errored with `Locator.click: Timeout 30000ms exceeded` — overlay (`wbloks_*` Collection / `role="dialog"`) intercepts pointer events. Comments are the highest-quality signal in the 10/30/5 cap; losing all 5 daily comments = ~50% of inbound reach loss from the sweep. This is pure execution rot, not strategy.
   - **HOW:** Before clicking the textarea, dismiss the modal/login-nag (`page.keyboard.press("Escape")` or close the `[role="dialog"]`), then `locator(...).first.click(force=True)` as fallback. One commit to `pi/dm-funnel/`.

2. **Hand-seed the warm-prospect funnel — `untapped_warm = 0` means the engagement loop is open at one end.**
   - **WHO/WHERE:** `social/engagement_log.json` shows 3 inbox sweeps in 36h all returning self with **zero** warm prospects surfaced. Check IG notifications/DMs manually for the last 36h on the GHP account.
   - **WHY:** If real engagement is happening but the sweeper finds nothing reciprocal, either (a) we're getting no inbound (content problem) or (b) the inbox parser is broken. Either way, the comment-leg failure above made it worse — no comments out means no comments in. Revenue funnel starts with warm replies.
   - **HOW:** 10-minute manual pass on IG notifications today; if real warmth exists, file a bug against the inbox sweeper. If none exists, that confirms the content angle below is the lever.

3. **Backfill insights on the last 5 posts — every row shows `likes=?  comments=?  reach=?`.**
   - **WHO/WHERE:** `automation/logs/ig_insights_*.json` joined to `social/posted_archive.json`. Media IDs: `18117625282835495`, `18080292026210433`, `18086771459434877`, `18454135165115198`, `18169107988438159`.
   - **WHY:** With no insights, the strategy brief is flying blind — we can't pick winning angles, can't decide queue order on 52 pending posts, can't justify revenue-leaning content. This is the data input that gates every other decision.
   - **HOW:** Re-run the insights pull for the 5 media IDs above and confirm the join key in the brief script reads them; if the IG insights endpoint is empty, log a bug against the insights cron.

## Content angle to push

**The "$1,400 mattress didn't fix it" / cheap-swap-beats-expensive-purchase angle.** The mattress caption is the only one of the 5 that names a real dollar number and a personal failure — that's the format that earns saves. The other four ("plug-in upgrade," "cabinet was where things went to die," "containers don't fix this," "it's the sponge not the pan") are all in the same *small-fix-beats-big-purchase* family — that's clearly our working pattern. Push the content engine to write the next 3 in this template: **[specific expensive thing I bought] didn't fix [problem]. [Cheap swap or behavior] did. Here's why.** Bonus: it sets up affiliate-product mentions cleanly (revenue first).

## Watch-outs

- 6/6 comment failures + 0 untapped_warm + every post showing `?/?/?` = three independent signals of telemetry/execution rot in the same 36h window; treat as one outage, not three.
