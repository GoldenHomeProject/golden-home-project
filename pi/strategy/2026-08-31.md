# GHP daily strategy brief — 2026-08-31

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-08-31T14:04:22+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

1. **Instrument post insights before making any content decisions**
   - WHAT: Fix the insights pipeline — last 5 posts all show `likes=?  comments=?  reach=?`
   - WHO/WHERE: `automation/logs/ig_insights_*.json` join in the strategy script; posts `18133069864723053`, `18028642553675299`, `18077127896341641`, `17903975967329994`, `18109078079117864`
   - WHY: Zero performance signal = every content/angle decision downstream (including this brief) is flying blind. Highest leverage because it unblocks everything else.
   - HOW: SSH to Pi, run the insights fetcher manually against those 5 media IDs and confirm the join key matches `posted_archive.json`; if the Graph call is failing, log the error.

2. **Push the shower-caddy dorm post into a monetizable funnel while back-to-school intent is live**
   - WHAT: Attach an affiliate-linked shower caddy pick to media `18133069864723053` via a pinned first comment + LinkTree update
   - WHO/WHERE: post `18133069864723053` ("dorm packing list… shower caddy"); update `social/linktree` (or equivalent bio link file) with the specific SKU
   - WHY: Only post in the recent set with a clear buy-intent, time-bound audience (dorm move-in is right now — late Aug). The other 4 are lifestyle/save posts with no obvious conversion path.
   - HOW: Add a pinned comment "Exact caddy in bio →" and swap the bio link to the product page today.

3. **Manually source 10 new warm prospects to seed tomorrow's sweep**
   - WHAT: untapped_warm=0 means the follow/like/comment budget will burn on cold targets tomorrow. Hand-pick 10 accounts from commenters on the 5 recent posts.
   - WHO/WHERE: write to the prospects file the Pi sweep reads (check `pi/dm-funnel/` for the input path); exclude any `._community` / promo handles
   - WHY: With 10 follow / 30 like / 5 comment caps, warm targets convert 5-10x cold ones. Empty warm queue = wasted daily budget.
   - HOW: Open each of the 5 recent posts in the IG app, screenshot the top 10 non-promo commenters, append handles to the prospect input file.

## Content angle to push
**"Give it a home" organization angle, applied to a single high-friction cabinet (under-sink or bathroom).** Two of the last 5 posts (`18077127896341641` "nothing has a home" + `17903975967329994` "low-grade mess") lean on this exact frame, and the queue head has *three more* cabinet/under-sink drafts stacked. That's the theme with the most inventory — the next post should pick the strongest queue draft ("four years of bottles falling on my feet") and pair it to a specific affiliate-eligible under-sink organizer SKU so we stop shipping save-bait with no revenue path.

## Watch-outs
- All 5 recent posts have null likes/comments/reach — insights fetch is broken or the join is misaligned
- `untapped_warm=0` after a DM was sent 2026-08-30 — either the sweep is over-consuming warm leads or the classifier stopped flagging them
- `pi/dm-funnel/__pycache__/` is untracked in git status — add to `.gitignore` before it lands in a commit
- Queue depth 279 is healthy, but 3 of top drafts are near-duplicate cabinet/under-sink stories — risk of thematic fatigue if posted back-to-back
