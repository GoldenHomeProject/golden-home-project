# GHP daily strategy brief — 2026-09-18

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-09-18T14:06:02+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Instrument post insights — the feedback loop is blind.**
- WHO/WHERE: `automation/logs/ig_insights_*.json` join → last 5 posts in `social/posted_archive.json` (media 17988600426112673, 18037538666831058, 18445369654126368, 18126169924846751, 18080262443339457) all show `likes=?  comments=?  reach=?`.
- WHY: Every strategy call — including this one — has to guess "what's working" because insights aren't landing on the posts we ship. Content angle selection is a coin flip until this is fixed.
- HOW: SSH the Pi, run the insights fetcher manually against the 5 media IDs above and confirm output writes to `automation/logs/ig_insights_*.json`; if it 404s, the media_id → ig_container_id mapping is stale.

**2. Refill the warm-prospect pipeline — comment budget is stranded.**
- WHO/WHERE: `social/engagement_log.json` — only 2 comments posted in 24h (horderly, lifeinjeneral) against a cap of 5; untapped_warm = 0.
- WHY: The inbox sweep is finding nothing to reciprocate, which means we're not being seen. Two outbound comments and 1 follow_error is not a sweep working at pace — it's a sweep starving.
- HOW: Seed 3-5 target accounts adjacent to horderly/lifeinjeneral (home-org creators in the 5-50k range) into the sweep's discovery list so the next 13:45Z run has candidates; keep under the 10 follow / 5 comment ceiling.

**3. Diagnose the follow_error on carol8381921.**
- WHO/WHERE: `social/engagement_log.json` 2026-09-17T13:42:15Z `follow_error carol8381921 no_state_change`.
- WHY: `no_state_change` on a follow is the shape of a soft block or a rate-limit trip early in the sweep — the kind of thing that silently degrades reach for days if it recurs.
- HOW: Grep the sweep log around that timestamp for the underlying IG response code; if it's a challenge/checkpoint, back the account off follows for 48h and stay on likes/comments only.

## Content angle to push
**"Under-the-sink cabinet rescue"** — the queue head has 2 of 3 captions on exactly this ("Three weeks ago this cabinet was where things went to die", "four years of bottles falling on my feet"), and the last shipped post (17988600426112673) was cabinet-hardware framed. The signal in the queue says the writers already know this angle converts; ship the under-sink pair next and A/B the hook ("things went to die" vs "bottles falling on my feet") so we get a real read once insights are wired up (see Action 1).

## Watch-outs
- All 5 recent posts missing likes/comments/reach — insights pipeline is broken or unmapped.
- Comment budget massively underused (2/5) and untapped_warm=0 — sweep is starving for targets, not throttled.
- `follow_error … no_state_change` on carol8381921 unexplained — check for checkpoint before next follow attempt.
- Queue depth 317 is healthy but heavily under-sink/cabinet weighted at the head — watch for topic fatigue if insights come back flat on those.
