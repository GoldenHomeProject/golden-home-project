# GHP daily strategy brief — 2026-06-09

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-06-09T14:15:15+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Fix the IG insights data pipeline — it's returning `?` for all 5 most recent posts.**
- WHO/WHERE: `automation/logs/ig_insights_*.json` joined to `social/posted_archive.json`; media IDs `17970202323079046`, `17891284695540093`, `17863244193594261`, `18128236441614387`, `18179385856400397`.
- WHY: Every post in the last sample shows `likes=? comments=? reach=?`. Without insights, every content decision below (queue ordering, angle selection, untapped_warm scoring) is blind.
- HOW: Tail the newest `ig_insights_*.json`, confirm it has rows for those 5 media IDs; if empty, re-run the insights fetcher and check the IG Graph token isn't soft-expired.

**2. Burn down today's engagement-cap allowance — we used 0/10 follow, 0/30 like, 0/5 comment in the last 36h.**
- WHO/WHERE: `social/engagement_log.json` shows three `inbox_sweep self` events at 13:42Z, 23:33Z, 13:34Z and nothing else. No outbound actions logged.
- WHY: Untapped_warm=0 + zero outbound = the sweep is finding nothing to act on, OR action execution is failing silently. Either way, paying for an engagement bot that does only inbox sweeps is dead capacity. The cap exists to be used.
- HOW: Manually trigger one outbound sweep with verbose logging; if it still acts on zero accounts, the candidate-selection query has drifted (likely the warm-prospect detector after recent inbox cleanup).

**3. Diversify the queue head — 3 of next 3 are cabinet/under-sink stories, same as 2 of last 5 posts.**
- WHO/WHERE: `social/post_queue.json` head items 1 and 3 are both under-sink-cabinet anecdotes; recent posts `17970202323079046` and `17863244193594261` were already cabinet stories.
- WHY: We're posting four cabinet stories in five slots. Even if cabinets convert, audience fatigue + algorithm topic-clustering will compress reach. The pillow post (74% side-sleepers) and kettle post (plastic safety) are the format outliers worth keeping mixed in.
- HOW: Re-order queue so one of the two cabinet stories drops to position 6+; promote a stat-hook or health/safety post into its slot.

## Content angle to push

**Stat-hook health/safety posts in the pillow/kettle template — not personal-anecdote cabinet stories.** The pillow post opened with "74% of us sleep on our sides … standard pillows put your spine at a 30 degree" — a hard number + a body-mechanics claim. The kettle post opened with "Most kettles let plastic touch your boiling water." Both lead with a quantified problem the reader didn't know they had. Cabinet posts open with "Three weeks ago…" / "I had four years of…" — personal time markers, no claim. Next queued original should be: one stat + one body/health risk + one fix, on a product category we already sell into (sleep, hydration, cookware).

## Watch-outs

- Insights `?` on every recent post (likely token or fetcher rot); engagement bot ran 3 inbox sweeps and zero outbound actions in 36h; untapped_warm=0 is suspicious in isolation but plausible if outbound is silently failing — fix #1 and #2 together before trusting #3.
