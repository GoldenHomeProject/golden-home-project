# GHP daily strategy brief — 2026-09-17

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-09-17T14:06:59+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

1. **Reciprocate the 1 untapped warm prospect before it goes cold**
   - WHO/WHERE: The single account flagged in the latest `inbox_sweep` (2026-09-17T13:42Z) in `social/engagement_log.json`; identify via the sweep's untapped_warm list.
   - WHY: Only 2 comments went out in the last 36h (neatmethod, organizedbyellis) and warm inbound is our highest-conversion signal — leaving 1 unreciprocated wastes the strongest lead we have this cycle. Also, `follow_error carol8381921 no_state_change` at 13:42:15Z suggests the follow queue didn't clear cleanly, so warm reciprocation is under-served today.
   - HOW: Manually leave a 1-line specific comment on their most recent post (counts toward the 5/day comment cap; we've used 2/5).

2. **Diagnose the `follow_error … no_state_change` on carol8381921**
   - WHO/WHERE: `social/engagement_log.json` entry `2026-09-17T13:42:15Z follow_error carol8381921 no_state_change`; check the Pi sweep log at `~/.ghp-strategy/runs/last.log` and the follow-executor in `automation/` for the failure path.
   - WHY: A silent `no_state_change` on a follow attempt means we're burning follow-cap slots without state advancing (10/day cap). If this is a session/cookie issue, tomorrow's 2x sweep degrades further.
   - HOW: `ssh raspberrypi5-ts 'tail -200 ~/.ghp-strategy/runs/last.log'` and grep for carol8381921 + surrounding stack; fix or blacklist the handle.

3. **Backfill insights on the last 5 posts — every metric is `?`**
   - WHO/WHERE: `automation/logs/ig_insights_*.json` join in the strategy script; media IDs 17985194421097908, 17988600426112673, 18037538666831058, 18445369654126368, 18126169924846751.
   - WHY: With likes/comments/reach all null, tomorrow's brief (and content angle selection) is flying blind. This is the single biggest lift for judgment quality with zero new tokens/SaaS.
   - HOW: Manually run the insights fetcher for those 5 media IDs (or check whether the IG Graph token expired — most likely cause of a full null column).

## Content angle to push
**"Under-the-sink / cabinet-of-doom rescue" — kitchen edition.** Two of the next three queued captions ("cabinet was where things went to die", "four years of bottles falling on my feet … under the kitchen sink") point at the same jobs-to-be-done, and the closet-mess post (media=17985194421097908) already leans into the same "I ignored it for years" confession voice. Produce ONE flagship kitchen-under-sink reel/carousel with a save-worthy 3-step fix (tension rod + 2-tier organizer + labeled caddy — all subscription/affiliate-linkable) rather than letting the two queued captions compete against each other on the same theme within a week. **Caveat:** post-metric backfill (action 3) should confirm the closet post actually outperformed before we double down.

## Watch-outs
- All 5 recent posts show `likes=? comments=? reach=?` — insights pipeline is likely broken (expired IG Graph token?).
- `follow_error … no_state_change` on carol8381921 — follow executor may be silently failing.
- Queue depth 317 is high; if posting cadence stalls, freshness will decay — confirm GHActions poster ran on schedule.
- Only 2 comments + 0 follows + 0 likes logged in 36h; well under caps — the engagement sweep may be under-executing, not just conservative.
