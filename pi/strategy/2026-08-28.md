# GHP daily strategy brief — 2026-08-28

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-08-28T14:12:13+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Fix the insights → posted_archive join (revenue-blind right now).**
- WHAT: All 5 most recent posts show `likes=? comments=? reach=?`. The daily brief cannot tell what's driving revenue — every future "content angle" recommendation is a guess.
- WHO/WHERE: `automation/logs/ig_insights_*.json` → `social/posted_archive.json`; the join lives in the daily-strategy pull script (`pi/strategy/`).
- WHY: Zero visible metrics on media IDs `18610931881053410`, `18102216248071670`, `18619390060003367`, `18133069864723053`, `18028642553675299`. Either the latest insights file isn't landing, or the media-id join key drifted.
- HOW: `ls -lt automation/logs/ig_insights_*.json | head -3` on the Pi — check freshness, then diff a media_id from posted_archive against the insights payload to find the mismatched key.

**2. Purge stale-seasonal captions from the 270-deep queue.**
- WHAT: Kill or reschedule any July 4th / patriotic / summer-only captions still in `social/post_queue.json`.
- WHO/WHERE: `social/post_queue.json` (270 items); the July 4th tiered-tray caption already went out on 8/27 (`media=18102216248071670`) — that's the smoke.
- WHY: A patriotic tray in late August burns a slot that should be back-to-school / dorm / labor-day / early-fall entertaining. Dorm content is proven — the shower caddy caption (`media=18133069864723053`) was the freshest hit in the last five.
- HOW: `jq '[.[] | select(.caption | test("July 4|patriotic|red white blue|memorial day"; "i"))] | length' social/post_queue.json` — if >0, filter them out and re-serialize.

**3. Sanity-check the untapped_warm=0 result + spend some engagement headroom.**
- WHAT: Verify the inbox sweep is actually finding warm leads (it should almost never be 0), and manually convert 3-5 of them into comments/follows today.
- WHO/WHERE: `pi/dm-funnel/` inbox sweep output; engagement log shows only 1 outbound DM (shiragill) in 36h vs caps of 5 comment / 10 follow / 30 like.
- WHY: `count=0` with three sweeps in 24h is suspicious — either the funnel dedupe is over-eager (marking everyone as "already reciprocated") or the sweep is silently failing to parse. Meanwhile we're leaving ~4 comments / ~9 follows on the table daily.
- HOW: `grep -c "warm" ~/pi/dm-funnel/*.log` (or wherever the sweep logs land) for the last 3 runs — if the log shows candidates but the count reports 0, it's a dedupe bug, not an empty inbox.

## Content angle to push
**Back-to-school small-space storage — specifically dorm bathroom and dorm closet.** The dorm shower caddy caption (`media=18133069864723053`) is the freshest post that reads on-season for late August. Move-in weekend is right now through Labor Day. Prioritize 2-3 dorm/small-space storage captions from the queue (there's a kitchen-sink cabinet one already at the head — reframe or slot behind a dorm-closet version). Skip anything patriotic or July 4th — the queue clearly still has stale seasonal drift.

## Watch-outs
- Zero engagement metrics on all 5 recent posts — insights join is broken or the pull isn't landing.
- July 4th patriotic caption went live on 2026-08-27 — queue has stale seasonal content.
- The "4am, neck won't turn left" caption appears in both the last-5-posted list and the queue head — possible dupe about to re-post.
- `untapped_warm=0` across three sweeps with only 1 outbound DM in 36h — likely a sweep/dedupe bug, not a real empty inbox.
