# GHP daily strategy brief — 2026-06-01

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-06-01T14:08:50+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Fix the insights pipeline — every recent post shows `likes=? comments=? reach=?`**
- WHAT: Restore post-level metrics ingestion. All 5 most recent posts in the brief have `?` for likes/comments/reach, meaning content decisions are being made blind.
- WHO/WHERE: `automation/logs/ig_insights_*.json` (latest file is the join source for the strategy brief); media IDs 18114279580875787, 18130380709600264, 18315795139302670, 18400197637155262, 18098496266140274.
- WHY: Without per-post metrics we can't tell whether "side sleeper pillow" or "under-sink cabinet" is the angle to double down on — and the queue is biased ~50/50 between those two themes. Revenue depends on picking the winner.
- HOW: SSH the Pi, check the most recent `automation/logs/ig_insights_*.json` for emptiness or stale timestamp, then trigger a manual insights pull for those 5 media IDs.

**2. Stop leaving 80%+ of the engagement budget on the table**
- WHAT: Last 24h used 2/5 comments, **0/10 follows, 0/30 likes**, and untapped_warm = 0. Either the inbox sweep isn't surfacing engagers, or we're caught up and need to seed outbound.
- WHO/WHERE: `social/engagement_log.json` (only 2 comments: thehomeedit, organizedbyellis); inbox_sweep on `self` ran 2x with no warm output.
- WHY: Under-utilized caps = forgone reach, and `untapped_warm count=0` two sweeps in a row is suspicious — likely the sweep is broken, not that nobody engaged us.
- HOW: Manually open IG → Notifications → confirm whether any non-`._community` accounts liked/commented/followed in the last 36h; if yes, the sweep is silently failing — check the parser. If no, queue 3-5 manual likes on lookalike creators (thehomeedit, organizedbyellis adjacency) within the 30-like cap.

**3. Repair the `simplyspaced` DM locator before it cascades**
- WHAT: 2026-05-31T18:09:14Z `dm_error simplyspaced no_message_btn` — the `Message` button selector timed out at 8s.
- WHO/WHERE: Playwright locator `get_by_role("button", name=re.compile(r"^Message$", re.IGNORECASE))` in whatever DM module triggered this (engagement_log entry from 18:09 UTC).
- WHY: IG ships UI changes frequently; one failed locator today is usually three failed locators next week. Letting this rot kills the only outbound DM channel.
- HOW: Open simplyspaced's profile manually in a headed browser, inspect the new button (likely renamed "Send message" or moved into a kebab menu), and patch the locator with a fallback regex.

## Content angle to push
The queue head is **3-deep on cabinet/under-sink + 4am neck pain** — same two themes the last 5 posts already hit. Without insights data we can't prove either is the winner, but doubling down on two themes for 8 posts straight is saturation risk. **Push a third pillar next: a bathroom-counter or linen-closet "I avoided this for X years" post**, mirroring the proven "I avoided opening this cabinet for two whole years" hook (media 18315795139302670) that already shipped. Same emotional template (avoidance → breaking point → fix), different room — gives us a third datapoint and breaks the kitchen/bedroom monotony.

## Watch-outs
- **Insights blank on all 5 recent posts** (`likes=? comments=? reach=?`) — likely broken ingestion, not zero engagement.
- **untapped_warm = 0 across 2 sweeps** — almost certainly a parsing failure, not reality.
- **Engagement caps massively under-used** (0/10 follow, 0/30 like, 2/5 comment) — wasted reach budget.
- **simplyspaced DM locator broken** — IG UI drift; will spread to other targets if unpatched.
- **Queue thematically narrow** — 21 items but the head is all cabinet + sleep.
