# GHP daily strategy brief — 2026-08-01

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-08-01T14:12:19+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Repair the IG insights pipeline — you are flying blind on content decisions.**
- WHO/WHERE: `automation/logs/ig_insights_*.json` — the join into the brief returned `likes=? comments=? reach=?` for ALL 5 most recent posts (18109377388825612, 18066150305499254, 18141356803537440, 18095933063571256, 18080660579679095).
- WHY: Zero insights = zero ability to score which angles convert. Every content-angle decision below this line is guesswork until this is fixed.
- HOW: `ls -la automation/logs/ig_insights_*.json | tail -5` and check the latest file's timestamp + payload; if empty or 401/permissions-error, refresh the IG Graph token and rerun the insights fetcher manually.

**2. Diagnose why the engagement pipeline logged 0 outbound actions in 36h.**
- WHO/WHERE: `social/engagement_log.json` — three `inbox_sweep self` rows and nothing else since 2026-07-31T13:36Z. Untapped-warm queue = 0.
- WHY: Caps are 10 follow / 30 like / 5 comment per day; we used 0/0/0 across two sweep cycles. Either the sweep isn't finding candidates, engagement calls are silently erroring, or actions aren't being written to the log. Either way, revenue-driving reciprocation stopped.
- HOW: Grep the last sweep run's stderr (`~/.ghp-engagement/runs/last.log` on the Pi) for exceptions or "0 candidates" and confirm `dm_funnel_cron.log` (untracked) shows successful writes.

**3. Deduplicate the queue head — you are about to post the same theme you just posted.**
- WHO/WHERE: `social/post_queue.json` head vs. `social/posted_archive.json`. Just posted: "cabinet where things went to die" (media 18109377388825612). Queue #1: same cabinet hook. Just posted: mattress/neck-pain hook. Queue #2: another 4am-neck-won't-turn hook. Just posted: kitchen mess (18141356803537440). Queue #3: under-sink cabinet again.
- WHY: 195-deep queue but the top 3 all rerun themes from the last 5 posts. Back-to-back near-duplicates train the algorithm and audience to scroll past.
- HOW: Rotate the next 3 slots — pull a rental/wall-hack or hot-sleeper item from deeper in the queue into position 1-3, defer the duplicates by ~10 slots.

## Content angle to push
**Rental-friendly wall + surface hacks** (peel-and-stick wood wallpaper, post 18080660579679095, was the only recent post whose theme is NOT repeated in the queue head). Every other recent theme — cabinets, kitchen mess, sleep/neck — is doubled up in queue positions 1-3, so a rental/wall hack next fills a genuine gap. Caveat: without insights numbers I cannot confirm this post outperformed; recommendation is based on theme diversity in the posted-vs-queued mix, not measured engagement. Re-evaluate the moment action #1 is fixed.

## Watch-outs
- All 5 latest post insights returned `?` (metrics fetch likely broken); 0 engagement actions logged across 2 sweep cycles despite 10/30/5 headroom; queue head duplicates just-posted themes 3-for-3; untapped_warm=0 which is suspicious given 2 fresh posts should have generated at least a few new interactors.
