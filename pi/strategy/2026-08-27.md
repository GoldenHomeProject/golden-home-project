# GHP daily strategy brief — 2026-08-27

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-08-27T14:07:15+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Fix the insights → posts join (measurement is dark).**
- WHAT: All 5 most-recent posts show `likes=? comments=? reach=?` in the brief, even though `9408eb7 IG insights: 2026-08-27` landed yesterday. The brief-builder is not merging insight rows into `posted_archive.json` entries.
- WHO/WHERE: `pi/strategy/` brief script + newest `automation/logs/ig_insights_*.json` join key (likely media_id vs `id`/`ig_id` mismatch). Posts affected: 17878511532527316, 18610931881053410, 18102216248071670, 18619390060003367, 18133069864723053.
- WHY: Revenue-first prioritization is impossible without post-level performance. Every content decision below is guesswork until this is fixed.
- HOW: Diff the ID field in `ig_insights_2026-08-27.json` against `posted_archive.json` and update the join in the strategy script.

**2. Diagnose the dead engagement funnel.**
- WHAT: `untapped_warm = 0` and zero follow/like/comment actions in 24-36h despite caps of 10/30/5. The Pi is only logging `inbox_sweep self` — the classifier or the reciprocation step is producing nothing.
- WHO/WHERE: `pi/dm-funnel/` sweep output + `social/engagement_log.json`. Confirm last non-self action timestamp.
- WHY: Caps are the throttle; zero utilization means the warm-lead → reciprocate pipeline is broken or starved. This is the highest-leverage lever for DM → email → revenue.
- HOW: Run the sweep manually with verbose output and inspect what it classifies as warm vs. filtered (rule out over-aggressive `._community`/promo filter).

**3. Prune out-of-season captions from the 267-deep queue.**
- WHAT: A July 4th tiered-tray post is in the last 5 published — it's Aug 27. Queue depth 267 means seasonal drift is compounding.
- WHO/WHERE: `social/post_queue.json` (267 entries). Grep for `july`, `4th`, `patriotic`, `memorial`, `easter`, `valentine` etc. and demote/remove.
- WHY: Off-season posts burn a slot that could carry back-to-school / early-fall intent, which is the current buying window.
- HOW: Filter queue by seasonal keywords, archive matches to `social/post_queue.json.bak-seasonal-YYYYMMDD`, keep evergreen + fall-relevant.

## Content angle to push
**Back-to-school / dorm & first-apartment small-space organization**, extending the dorm shower-caddy post (18133069864723053) that just ran. Late August is the peak intent window and the queue head is dominated by under-sink/kitchen (3 of the top items are the same cabinet angle). Concrete ask: 2-3 captions on dorm desk, under-bed bin logic, and shared-bath caddy routines. Note the numeric caveat — I can't confirm the caddy post's actual reach because insights are broken (see action #1); pushing this angle is based on timing, not measured performance.

## Watch-outs
- All 5 recent posts show `?` metrics — insights ingest exists but isn't joining.
- 0 outbound engagement + 0 untapped_warm = funnel is fully idle, not "clean."
- July 4th caption in the recent-post list on Aug 27 signals seasonal captions aren't being gated.
- Under-sink cabinet appears twice in last 5 posts and twice in queue head — thematic repetition risk.
