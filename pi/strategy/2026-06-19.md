# GHP daily strategy brief — 2026-06-19

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-06-19T14:02:52+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Fix the IG comment_post locator — 7/7 comment attempts failed yesterday in a single batch.**
- WHAT: The comment textarea is being intercepted by an overlay (`wbloks_1 wbloks_93 wbloks_91` Bloks Collection and `x1qjc9v5 … xl56j7k`). Every comment in the 2026-06-18 14:10–14:22Z sweep timed out at 30s.
- WHO/WHERE: `pi/dm-funnel/` (or wherever `comment_post` lives — grep `textarea[aria-label*='comment']`). Affected targets: `breathing.room.home`, `iheartorganizing`, `lifeinjeneral`, `organizedbyellis`, `simplyspaced`, `horderly`, `cindyology`.
- WHY: Comments are the highest-signal action in the 10/30/5 cap (5 cap = scarce). Zero successful comments yesterday = wasted human-judgment outreach budget. Likes/follows in the log are absent too — sweep may be running but the comment branch is fully broken.
- HOW: Add a dismiss step for the overlay before clicking textarea — try `page.keyboard.press("Escape")` then re-locate, OR click textarea via JS (`evaluate(el => el.click())`) to bypass pointer interception. Test against one of the 7 failed handles.

**2. Wire likes/comments/reach into the strategy feed — every post in the last 5 shows `?`.**
- WHAT: Posts `18454135165115198`, `18169107988438159`, `18073147433680217`, `18455303995112614`, `18150970045493792` all return `likes=? comments=? reach=?`. The strategy brief is flying blind on what's working.
- WHO/WHERE: Joiner between `social/posted_archive.json` and `automation/logs/ig_insights_*.json` — check the script in `pi/strategy/` that builds the input.
- WHY: Without post performance, "content angle to push" is guessing. The whole point of this daily brief is judgment on signal, and signal is missing.
- HOW: Run the most recent `automation/logs/ig_insights_*.json` manually — verify the file actually contains those media IDs. If insights pull is broken, fix the IG insights fetch; if join key is mismatched, fix the lookup.

**3. Manually scout 3-5 warm replacements since untapped_warm = 0.**
- WHAT: `count=0` on untapped warm prospects means either inbox sweep returned nothing OR the comment failures upstream prevented any reciprocation candidates from being logged. Don't burn the 10-follow cap on cold.
- WHO/WHERE: Look at IG notifications + story-reply DMs in the GHP account directly; also check recent commenters on posts `18073147433680217` (under-sink) and `18455303995112614` (pillow) — those are concrete pain-point posts likely to attract organic engagers.
- WHY: Zero warm means the funnel's top is empty for the day. One warm conversion > 10 cold follows on revenue terms.
- HOW: Open IG, eyeball comments on the last 2 posts, jot 3-5 handles into `social/engagement_log.json` as manual `warm_seen` entries so tomorrow's sweep picks them up.

## Content angle to push

**"The reason X is broken isn't X — it's Y" diagnosis hooks.** Three of the last five posts use this exact rhetorical move (cabinets/homes, pans/sponges, pillows/wrong-pillow) and the post queue head has another two ("cabinet was where things went to die," "neck won't turn left"). It's clearly the voice that's making the cut into the queue. **But** with insights returning `?` I can't prove these are the winners — pushing this angle is a bet on prior trajectory, not measured lift. Recommendation: keep the diagnosis-hook angle, AND add ONE comparison post next ("under-sink: containers vs. tension-rod") so we can A/B once insights are wired (action #2).

## Watch-outs
- 7/7 comment timeouts on 2026-06-18 14:10-14:22Z all show identical overlay-intercept pattern → IG DOM change, not flaky network.
- No like/follow rows in the 24-36h log either — likes (30/day cap) should be the most frequent action; absence suggests sweep is degraded beyond just comments OR logging changed.
- Insights join returning `?` across all 5 recent posts = strategy brief is operating without performance signal. Fix this before trusting any "what's working" claim.
- 61 posts in queue is healthy; not a watch-out, just noting cushion exists while #1 and #2 get fixed.
