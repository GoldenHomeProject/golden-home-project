# GHP daily strategy brief — 2026-09-16

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-09-16T14:05:00+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Clear the 2 untapped_warm prospects before they cool off**
- WHAT: Reciprocate the 2 warm prospects still in the queue after last night's sweep (only 1 follow used of 10 cap).
- WHO/WHERE: `social/engagement_log.json` untapped_warm list; skip anything with `._community` / giveaway in handle.
- WHY: Sweep at 2026-09-15T23:33Z only converted 1 (`daniibear3`) and skipped 1 (`youngnagisa`, no follow button). 2 warm prospects remain — these are the highest-intent leads we have and they decay fast.
- HOW: Next inbox sweep — manually clear the 2 warm handles (follow + 1 comment each), stay well under caps.

**2. Backfill insights on the last 5 posts — we are flying blind**
- WHAT: Repair the insights join so likes/comments/reach populate for recent media.
- WHO/WHERE: `automation/logs/ig_insights_*.json` → `social/posted_archive.json` join in the daily strategy script.
- WHY: All 5 most-recent posts show `likes=? comments=? reach=?`. Without this, no strategy brief (including this one) can actually judge what's working. This is the single biggest leverage fix for future briefs.
- HOW: Check the most recent `ig_insights_*.json` file exists and has entries for media IDs 18390434311164341 / 17985194421097908 / etc.; if empty, the insights fetcher is silently failing.

**3. Trim the 317-deep post queue — it's a signal problem, not a supply problem**
- WHAT: Prune queue to ~30-50 captions of the strongest angles; archive the rest.
- WHO/WHERE: `social/post_queue.json` (currently 317 items).
- WHY: A 317-post backlog at ~1-2 posts/day = 5-10 months of stale captions. New high-performing angles get buried. Queue head shows repetition (2 of top 3 are "cabinet where things went to die" variants).
- HOW: Keep queue ≤50; move overflow to `social/post_queue_archive.json` and re-surface only if an angle proves out.

## Content angle to push

**Under-sink / under-cabinet rescue** — the queue head has 2 of 3 captions on this exact angle ("cabinet where things went to die", "four years of bottles falling on my feet… cabinet under the kitchen sink"), and the last-5 posts already have a hardware/cabinet angle (media=17988600426112673, "cabinet hardware"). The engine is clearly gravitating here organically. Push ONE strong under-sink post this week with an affiliate-ready product list (tension rods, stackable bins, lazy Susan) — revenue-first, subscription-only. Cannot confirm performance because insights are missing (see action #2), but the queue's own weighting is the signal.

## Watch-outs

- All 5 recent posts have null insights — brief cannot judge performance until fixed.
- `youngnagisa` follow_skip (no follow button) — private/restricted account; verify it's not a pattern before it eats warm-prospect slots.
- Queue at 317 vs. ~1-2 posts/day cadence = ~6+ month backlog; new angles are getting buried.
