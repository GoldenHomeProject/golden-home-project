# GHP daily strategy brief — 2026-08-19

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-08-19T14:13:47+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

1. **Backfill post insights so tomorrow's brief is not flying blind.**
   WHO/WHERE: `automation/logs/ig_insights_*.json` + `social/posted_archive.json`; media IDs 17953384878080121, 18162323293466949, 18105322931129139, 18113695585752130, 18608922436003615.
   WHY: All 5 most recent posts show `likes=? comments=? reach=?` — the strategy loop has no signal to rank angles, and untapped_warm=0 means the inbox sweep isn't surfacing anyone either. Without numbers this brief is guessing.
   HOW: Run the IG insights fetcher for those 5 media IDs and re-join to `posted_archive.json` before the next 14:00 UTC brief.

2. **Turn the two comments we left yesterday into actual reciprocation checks.**
   WHO/WHERE: `@horderly` (2026-08-18T14:10:38Z) and `@lifeinjeneral` (2026-08-18T14:12:42Z) from `social/engagement_log.json`.
   WHY: These are our only outbound touches in 36h and both are high-fit organizing accounts. If either replied or liked back, they belong in untapped_warm — but the count is 0, which suggests the sweep isn't scanning comment threads we authored.
   HOW: Manually open both comment threads in IG, and if either responded, add 1 like on their next post (well under the 30/day cap) — no follow yet.

3. **Cut the queue from 242 → publish-ready top 20 before adding more.**
   WHO/WHERE: `social/post_queue.json` (queue_size=242); generator is still appending (see `cf52fc1 pinterest: queue +1 pin(s)`).
   WHY: 242 captions deep means the newest, most-timely angles (Fourth of July tray, dorm move-in) sit behind stale inventory. Revenue-first means the freshest hooks should ship first, not FIFO.
   HOW: Re-sort `post_queue.json` by seasonality/recency, keep top 20 hot, archive the rest to `post_queue.json.bak-20260819` (matches existing bak convention).

## Content angle to push
**Dorm-room / back-to-school stick-on wall storage** — media 18105322931129139 ("dorm room desk isn't the problem — your wall is empty") is the only seasonally urgent post in the last 5 and the window closes in ~2 weeks. We have zero reach data to confirm, but the queue head has no follow-up dorm angles — that's a gap. Push one more dorm-adjacent piece (under-bed / over-door / command-strip) into the top of the queue this week.

## Watch-outs
- **untapped_warm=0 with only 2 outbound comments in 36h** — either the inbox sweep is broken or genuinely nobody engaged; verify the sweep is reading comment replies, not just DMs.
- **All 5 recent posts missing insights** — insights pipeline likely stalled; check `automation/logs/ig_insights_*.json` freshness.
- **3 untracked `.bak` files from 2026-08-19 in `social/`** (`pinterest_queue.json.bak`, `.bak2`, `.bak3`) — something is looping backups without committing; investigate before disk fills.
