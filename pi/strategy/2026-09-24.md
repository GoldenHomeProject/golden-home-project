# GHP daily strategy brief — 2026-09-24

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-09-24T14:13:38+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

1. **Fix the insights collector — every one of the last 5 posts shows `likes=? comments=? reach=?`**
   - WHO/WHERE: `automation/logs/ig_insights_*.json` producer + the join step feeding this brief; posts affected include media `18112330244155579`, `18118326971473956`, `18092239964433786`, `18104582033217107`, `18193673143348615`
   - WHY: With zero performance signal on the last 5 posts, no content angle decision below this line is defensible. Everything downstream (queue prioritization, angle picking, affiliate ranking) is flying blind.
   - HOW: SSH the Pi, run the insights fetcher manually against one known media ID, capture the failure mode (auth expiry vs. schema drift vs. empty file), and patch — this is the single highest-leverage fix.

2. **Trim the 325-caption queue — it is now a backlog, not a queue**
   - WHO/WHERE: `social/post_queue.json` (queue_size=325)
   - WHY: At ~1 post/day the head captions won't ship for ~a year; the top-of-queue "cabinet under sink" caption is already stylistically overlapping with the "4am neck" and "low-grade mess" posts recently shipped — freshness and diversity both suffer.
   - HOW: Do one manual pass: cut to ~60 (2 months at current cadence), keeping only captions with a distinct product angle or hook; discard duplicates and dated seasonal ones.

3. **Bring the untapped_warm signal back online**
   - WHO/WHERE: inbox_sweep output (3 sweeps in window, all reporting `count=0`)
   - WHY: Three consecutive sweeps returning zero warm prospects is either (a) genuinely quiet, or (b) the sweep parser stopped detecting engagement — indistinguishable from this data. Warm-prospect reciprocation is the main revenue-adjacent lever the caps allow.
   - HOW: Spot-check one recent post's actual IG notifications against what the sweep logged; if divergent, fix the detection; if aligned, note that reach is genuinely low and route effort to action #1.

## Content angle to push

**Under-sink / cabinet-organizer rescue with a visible before-state.** The queue head, the recent "low-grade mess" post, and the earlier closet-visibility post all cluster on *hidden-storage anxiety* — a proven angle for this account. Push one more in that lane with a specific product (clear stackable bins or a two-tier under-sink shelf) rather than another bedding/kitchen-surface angle. **Caveat:** I cannot confirm this angle is *working* — likes/comments/reach are all `?` for the last 5 posts. Treat this as "keep serving the audience we appear to have" until action #1 restores real numbers.

## Watch-outs

- All 5 recent posts have null insights (`likes=? comments=? reach=?`) — insights pipeline likely broken.
- `untapped_warm count=0` for 3 consecutive sweeps — possibly a parser regression, not genuine silence.
- Queue at 325 = ~1 year of backlog at current cadence; drift and staleness risk is real.
- Only 3 engagement log entries in 24-36h and all are `inbox_sweep self` — no follows/likes/comments recorded, so the daily caps aren't the bottleneck; execution or logging is.
