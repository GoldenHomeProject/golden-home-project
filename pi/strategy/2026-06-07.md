# GHP daily strategy brief — 2026-06-07

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-06-07T14:06:42+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

1. **Restore post insights pipeline.** WHAT: All 5 recent posts show `likes=? comments=? reach=?` — the insights join is broken or `ig_insights_*.json` is stale. WHO/WHERE: `automation/logs/ig_insights_*.json` + the join in the strategy script that reads `social/posted_archive.json`. WHY: Without per-post numbers, no angle/CTA decision in this brief or the next is data-driven — we're flying blind on the queue of 33. HOW: Check newest `ls -lt automation/logs/ig_insights_*.json`; if stale >24h, re-run the insights fetcher; if fresh, fix the media_id join key.

2. **Refill the warm-prospect funnel.** WHAT: `untapped_warm count=0` and only 2 comments posted in 36h (well under the 5/day cap). WHO/WHERE: inbox sweep at `2026-06-07T13:42:54Z` returned nothing; comments went to `breathing.room.home` and `simplyspaced` on 06-06. WHY: Zero warm reciprocation queue means the next sweep has nothing to act on — engagement engine will idle until new inbound arrives. HOW: Run one manual outbound discovery pass against side-sleeper / under-sink hashtags to seed 3-5 new warm targets (stay under 10 follow / 30 like caps), then let the next inbox sweep pick up replies.

3. **Trim duplicate captions from the queue.** WHAT: Queue head shows the "cabinet where things went to die" caption and "bottles falling on my feet" caption are both already in the last-5-posted set (media 17970202323079046 and 17863244193594261). WHO/WHERE: `social/post_queue.json` head vs. `social/posted_archive.json`. WHY: Re-posting identical captions within a week burns reach and looks like a bot to IG's ranker. HOW: Dedupe queue against last 14 days of `posted_archive.json` before next scheduled post.

## Content angle to push

**Side-sleeper pillow angle — but with a product-specific hook this time.** The "74% of us sleep on our sides" caption was posted twice in the last 5 (media 18465834370100122 and 17891284695540093), meaning the content engine is leaning on it but we have no insight numbers to confirm it's actually working. Next variant: pair the same stat with a vetted product (the recent auto-promote of `B0B3WSZ3QP` shows the promote pipeline is live) and a concrete "spine angle" before/after frame, so the post earns a click instead of just a save.

## Watch-outs

- Insights fields all `?` across 5 posts (pipeline broken or stale log).
- `untapped_warm=0` + only 2 comments/36h = engagement engine starving for inputs.
- Post queue contains captions already posted in the last 5 — dedupe is failing.
