# GHP daily strategy brief — 2026-09-22

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-09-22T14:10:00+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Fix the IG insights ingestion — we're flying blind**
- WHAT: All 5 most-recent posts show `likes=? comments=? reach=?`. The `automation/logs/ig_insights_*.json` join isn't returning values, so no post can be judged as "working."
- WHO/WHERE: `automation/logs/ig_insights_*.json`, joined against `social/posted_archive.json` in the daily strategy script.
- WHY: Without per-post metrics, we can't pick a content angle from evidence, can't prune the 324-post queue, and can't defend affiliate pushes. This is the highest-leverage unblock — everything downstream depends on it.
- HOW: SSH the Pi, run the most recent insights fetcher by hand and inspect the JSON — either the fetch is empty (token/permissions) or the join key drifted (media_id vs. shortcode).

**2. Investigate `untapped_warm=0` before spending a comment slot**
- WHAT: Three inbox sweeps in 36h all returned zero warm prospects, and the engagement log shows no follow/like/comment actions — only sweeps. We're at 0/10 follow, 0/30 like, 0/5 comment. Either genuine engagement is dead, or the sweep's "warm" detector is broken.
- WHO/WHERE: `social/engagement_log.json` (last entries are all `inbox_sweep self`), inbox-sweep script that populates warm-prospect count.
- WHY: Zero-in / zero-out is the flywheel stalling. If detection is broken, we're leaving reciprocation on the table daily; if engagement is genuinely dead, that's the real signal and content needs to change.
- HOW: Open Instagram directly, count actual notifications from the last 24h, and diff against what the sweep reported. If sweep undercounts, the detector needs a fix; if IG itself is quiet, escalate to #3.

**3. Manually seed 5 comments on relevant hashtag posts (within caps)**
- WHY: With no warm inbound and no metrics, the only way to restart the flywheel this cycle is a small, human-curated outbound push. Comments (not likes) are the highest-signal action for growing reach in the home-organization niche.
- WHO/WHERE: Search `#smallspaceorganization` / `#renterfriendly` / `#peelandstick` on IG; skip any `._community` / giveaway accounts per rules.
- HOW: 5 substantive comments (uses full daily comment cap, leaves follow/like room untouched). Log each in `social/engagement_log.json` with the standard action schema so tomorrow's brief can attribute any warm response back to them.

## Content angle to push
**One product, one transformation — peel-and-stick wallpaper on kitchen cabinets.** Of the 5 recent posts, four are pure story ("cabinet was where things went to die", "4am neck pain", "low-grade mess") with no product anchor. Only `media=18092239964433786` (embossed black silk wallpaper on cabinets) has a clean affiliate hook. We can't confirm it outperformed the others because insights are broken (see action #1), but it's the only one where a viewer's next click has a revenue path. Queue up 3 more captions in this shape: specific renter-friendly product + before/after moment + no-tools claim. Cabinet fronts, backsplash, stair risers are all in the same product family.

## Watch-outs
- All 5 post insight rows are `likes=? comments=? reach=?` — data pipeline is silently failing, not just quiet.
- `untapped_warm=0` for 3 consecutive sweeps is either a detector bug or a real engagement collapse — both need eyes today.
- Engagement log shows only sweeps, no follow/like/comment actions in 36h — the outbound side is idle even though caps are untouched.
- Queue depth 324 is fine, but two of the three head captions are near-duplicates of already-posted items ("cabinet was where things went to die", "4am neck") — check for accidental re-queueing.
