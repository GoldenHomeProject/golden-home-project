# GHP daily strategy brief — 2026-09-21

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-09-21T14:07:02+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

1. **Backfill insights on the last 5 posts before drafting new angles.** WHO/WHERE: `automation/logs/ig_insights_*.json` joined to media IDs `18126169924846751`, `18080262443339457`, `18108984778893597`, `18112330244155579`, `18118326971473956` in `social/posted_archive.json`. WHY: every one of the last 5 posts shows `likes=? comments=? reach=?` — the strategy loop is flying blind and cannot pick a winning angle without this. HOW: run the insights fetcher for those 5 media IDs and re-commit the joined file so tomorrow's brief has real numbers.

2. **Seed the untapped_warm pool by commenting on 3 accounts you've already engaged.** WHO/WHERE: extend from today's `comment_posted` targets `simplyspaced` and `cindyology` — pick 3 adjacent small-org accounts from their recent commenters (skip any `._community`/promo). WHY: `untapped_warm count=0` means no reciprocal pipeline exists right now; the daily 5-comment cap is under-used (2/5 today). HOW: on next inbox sweep, leave one substantive comment each on 3 non-promo accounts that already liked/commented on `simplyspaced` or `cindyology` posts — stay within 5 comments/day.

3. **Prune or reorder the 318-deep post queue around the "before/after transformation" angle.** WHO/WHERE: `social/post_queue.json` (queue_size=318); the "Three weeks ago this cabinet was where things went to die" caption is already queued next. WHY: 318 items is >10 months of runway at ~1/day and the queue head is dominated by opening-line "problem" hooks; without insight data (see action 1) we're stacking untested variants. HOW: promote the 2 transformation-arc captions to the top of the queue and hold the rest until action 1 lands — do not add new drafts this cycle.

## Content angle to push
**Before/after transformation with a concrete dollar or time claim in line 1.** The "Three weeks ago this cabinet…" and "My home had been a low-grade mess…" captions are the only two in the last 5 that follow an arc rather than pitch a problem — everything else opens with "your X is wasted / failing / broken." With `likes/comments/reach = ?` across all 5 we cannot prove it, but arc-style captions historically out-earn scold-style in home-org niches. Content engine should draft the next 3 posts as "here's what it cost / how long it took / what changed" — revenue-adjacent (affiliate-ready) and differentiated from queue head.

## Watch-outs
- All 5 recent posts have null `likes/comments/reach` — insights ingestion is broken or lagging.
- `untapped_warm count=0` two sweeps in a row — either inbox is truly quiet or the detector isn't classifying warm signals correctly.
- Queue depth 318 is ~10+ months at current cadence; risk of stale/duplicated captions accumulating.
- Only 2/5 comments used today with no follows/likes logged in 24h — engagement cap is under-utilized, not over.
