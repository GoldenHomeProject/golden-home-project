# GHP daily strategy brief — 2026-06-18

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-06-18T14:06:56+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

1. **Fix the IG comment selector before another sweep burns the daily 5-comment cap on zero successful comments.**
   - WHO/WHERE: `pi/dm-funnel/` (the engagement bot that emits `comment_post`). Failures on `organizedbyellis`, `shiragill`, `cindyology`, `simplyspaced`, `iheartorganizing`, `breathing.room.home` — 6/6 comment attempts on 2026-06-17 14:09–14:20Z timed out with "subtree intercepts pointer events" (modal/overlay covering the textarea).
   - WHY: Six consecutive `comment_error`s in an 11-minute window, zero successful comments logged in 24h. The textarea resolves but a dialog/`bk.components.Collection` overlay is eating clicks. This is a regression in IG's DOM, not bad targets — same pattern across every handle.
   - HOW: In the comment helper, dismiss/close the overlay before clicking (look for `div[role="dialog"]` close button, ESC keypress, or click outside) — then `locator.fill()` instead of `click()` on the textarea, since `fill` focuses without needing pointer-event clearance.

2. **Manually reciprocate to `shiragill` today — the bot can't reach her via DM or comment.**
   - WHO/WHERE: `shiragill` on IG. She has both a `comment_error` (14:11Z) and a `dm_error` (18:13Z `no_message_btn` — Message button never appeared, suggests she's privacy-restricted or already in a thread we missed).
   - WHY: She's the only handle in 24h that the bot touched twice and failed both times — that's a warm prospect the system literally cannot serve. `untapped_warm=0` says we have no other backlog, so she's the highest-value human-judgment win available right now.
   - HOW: Open her profile manually on phone IG, leave a substantive comment on her latest post (counts toward the 5/day cap, but bot landed 0 comments today so budget is open), no DM attempt.

3. **Pull the "wrong pillow for 10 years" caption forward in the queue and tag it as the next post.**
   - WHO/WHERE: `social/post_queue.json` — the pillow post (`media=18455303995112614`) just shipped, and queue head is the same sink-cabinet angle (`bottles falling on my feet`) that already ran as `media=18073147433680217`. Queue size = 58, so we have room to re-order.
   - WHY: The last 5 captions are 3× sleep/neck-pain + 2× kitchen-organization. Sleep/neck is the cluster that opened with personal cost ("$1,400 mattress," "4am, neck won't turn"). The next queued caption ("Three weeks ago this cabinet…") doubles down on a duplicate kitchen angle instead of compounding the sleep-pain thread that's already 60% of recent output.
   - HOW: In `social/post_queue.json`, move the next sleep/pain-narrative caption to position 0; bump the cabinet repeat down 5 slots.

## Content angle to push

**Sleep-pain → product fix narrative**, first person, named dollar cost. 3 of last 5 posts (60%) used this template (`$1,400 mattress`, `4am neck won't turn`, `wrong pillow for 10 years`) — and it's the only angle where we volunteer a price tag, which is what makes an affiliate ROUNDUP-style follow-up natural. Next caption should be a *side-sleeper specific* pillow/topper angle, since the existing pillow post was generic. This compounds the buyer-intent blog roundup that just shipped (`cebc187`).

## Watch-outs

- **All five `likes=? comments=? reach=?`** — insights join is broken; we're flying blind on which captions actually worked. Check `automation/logs/ig_insights_*.json` exists and the media_id join.
- **Zero `untapped_warm`** is suspiciously clean given 6 failed comments — either inbox_sweep ran before the failures registered, or warm-detection is also degraded.
- **6 comment_errors in 11 min with no backoff** — bot kept hammering through the broken selector; sweep should bail after 2 consecutive errors.
