# GHP daily strategy brief — 2026-07-31

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-07-31T14:10:17+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Fix the comment engine — it's silently failing**
- WHAT: Debug the `claude_empty` comment errors that killed comment posting yesterday.
- WHO/WHERE: `social/engagement_log.json` shows two failures 2026-07-30T14:02:56Z (`thehomeedit`) and 14:04:02Z (`organizedbyellis`), both `claude_empty`. Check the comment-generation call site in the engagement sweep code (likely `pi/dm-funnel/` or `social/` engagement runner).
- WHY: Zero non-inbox actions in 36h means the 5-comment/day cap is being wasted. Comments are the highest-signal engagement we do — losing them is losing warm-prospect creation at the top of the funnel.
- HOW: Grep for `claude_empty` in the engagement code, log the prompt+raw response for the next run, and fix whatever guardrail is returning empty (likely a refusal or an over-strict content filter on lifestyle captions).

**2. Fix the insights pipeline — we're flying blind**
- WHAT: All 5 recent posts show `likes=? comments=? reach=?`. Insights fetch is not landing.
- WHO/WHERE: `automation/logs/ig_insights_*.json` — the strategy script says it joins posted_archive to the newest insights file. Verify the insights fetcher ran, has valid tokens, and is writing keyed by `media_id`.
- WHY: Without per-post metrics we cannot tell which of the 192 queued posts should be prioritized or which angle to double down on. Every decision below this is a guess.
- HOW: Run the insights fetcher manually against media `17900294982474637` (mosquito post) and check the write path; if the token is stale, refresh via the standing IG long-lived token flow.

**3. Investigate the untapped_warm=0 signal before trusting it**
- WHAT: Zero untapped warm prospects across 3 inbox sweeps is either (a) we're perfectly caught up, or (b) the sweep is not correctly detecting new engagers.
- WHO/WHERE: `social/engagement_log.json` shows 3 inbox_sweeps (2026-07-30T13:35, 23:31, 2026-07-31T13:36) all with no downstream actions. Cross-check against IG notifications for the same window.
- WHY: If sweeps are broken we're leaving the 10-follow cap entirely unused — that's the cheapest, safest daily growth lever we have.
- HOW: Manually open IG notifications for the last 36h and count new likes/comments/follows on our posts; if any exist and untapped=0, the classifier is dropping them.

## Content angle to push
**Seasonal outdoor / mosquito-patio angle.** Of the last 5 posts, four are indoor organization or sleep — angles the queue is already heavy on (queue head shows two more cabinet posts and one neck-pain post out of 192). The mosquito/patio caption (`media=17900294982474637`) is the only seasonal outdoor hook and it's late July — peak intent for anti-mosquito, patio comfort, outdoor lighting, and citronella/screen products. Content engine should generate 3–5 more outdoor-summer captions (bug zappers, patio screens, misting fans, outdoor rugs that don't hold water) and jump them ahead of the cabinet backlog. Note: I can't measure lift on the mosquito post because insights are broken (see action 2) — this recommendation is based on absence in queue + calendar fit, not measured engagement.

## Watch-outs
- Comment engine returning `claude_empty` (2/2 attempts failed) — comment cap wasted.
- Insights data absent for all 5 recent posts — cannot rank content.
- Zero non-sweep engagement actions in 36h despite 10/30/5 daily caps — outbound funnel is idle.
- Queue backlog at 192 with repeated cabinet-under-sink angle appearing twice in top 5 — risk of thematic staleness on-feed.
