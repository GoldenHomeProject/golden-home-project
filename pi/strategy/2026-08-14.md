# GHP daily strategy brief — 2026-08-14

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-08-14T14:03:21+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Fix the `claude_empty` comment failures**
- WHO/WHERE: `social/engagement_log.json` — 3 comment attempts on 2026-08-13 14:08-14:12 UTC returned `claude_empty` (thehomeedit, breathing.room.home, shiragill); 1 more returned `no_posts_on_profile` (neatmethod). Driver code path is whatever wraps the comment-generation `claude -p` call in the engagement sweep.
- WHY: 4/4 comment attempts in the last window failed. The outreach top-of-funnel is silently broken — no comments means no DM handoff means no conversions. We're within the 5-comment cap but landing 0.
- HOW: Grep the sweep script for the comment prompt path, add a retry-once-then-log-prompt on empty response, and manually reproduce one of the four failed profiles to see whether it's a prompt-side refusal or an API/stdout parsing bug.

**2. Repair the post-insights ingestion**
- WHO/WHERE: `automation/logs/ig_insights_*.json` join into strategy brief; all 5 posts in the archive (media 18032886038830495, 17981514854877564, 17938619823090252, 17896677240374841, 18227026603322659) show `likes=? comments=? reach=?`.
- WHY: Content strategy is flying blind. We can't push more of what works if every insight field is `?`. Fixing this unlocks every future decision on angles, hooks, and product-fit posts.
- HOW: Check the newest `ig_insights_*.json` — either the file isn't being produced (cron/token issue) or the strategy brief's join key is stale. Verify a fresh insights pull for the most recent media ID.

**3. De-dup the "low-grade mess" opener pattern**
- WHO/WHERE: `social/post_queue.json` head + `social/posted_archive.json`. Posted 18227026603322659 opens *"My kitchen had been a low-grade mess…"*; posted 18032886038830495 opens *"My patio had been a low-grade mess…"*; queue head #1 opens *"Three weeks ago this cabinet was where things went to die…"* — same confessional-clutter beat three times.
- WHY: IG suppresses repetitive template patterns and audiences fatigue on identical hooks. Queue is 232 deep — plenty of headroom to reorder, not a supply problem.
- HOW: Add a queue-linter rule (or one-shot rewrite) that flags any two consecutive posts sharing the "[room] had been a low-grade mess" / "[thing] was where things went to die" opener and swaps in a non-confessional hook (stat, question, or contrarian claim).

## Content angle to push
**Data-driven product-fit posts, modeled on the side-sleeper pillow post** (media 17938619823090252: *"Most pillows are designed for back sleepers. 74% of us sleep on our sides…"*). It's the only recent post with (a) a specific stat, (b) a clear product-market fit angle, and (c) revenue-adjacent framing — the other 4 recent posts are lifestyle/organization storytelling with no product hook. Even without insights data confirming performance, this is the only shape in the recent set that maps cleanly to affiliate/product conversion. Next angle: pick one more high-conversion product category (mattress toppers, under-sink organizers matching the queued cabinet post, or entryway hooks matching post 17981514854877564) and write it in the same stat-first → problem → product-fit structure.

## Watch-outs
- Insights pipeline returning `?` for every field across all 5 recent posts — likely broken, not just missing.
- 4 consecutive comment failures on 2026-08-13 (3 `claude_empty` + 1 `no_posts_on_profile`) — outreach engine is degraded but not alerting.
- `untapped_warm=0` looks clean, but combined with the comment failures could mean the inbox sweep isn't finding warm signals rather than that we've drained the backlog — worth spot-checking one sweep manually.
