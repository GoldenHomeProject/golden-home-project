# GHP daily strategy brief — 2026-09-03

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-09-03T14:14:49+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Fix the insights join — every post metric is "?"**
- WHAT: The last-5-posts block shows `likes=? comments=? reach=?` for all 5 media IDs (18077127896341641, 17903975967329994, 18109078079117864, 18001186088795007, 18627445102005153). Yesterday's brief spec says these join to `automation/logs/ig_insights_*.json`. Nothing is landing.
- WHO/WHERE: `automation/logs/ig_insights_*.json` + whatever joins it into `pi/strategy/` — likely `pi/strategy/build_brief.py` or similar.
- WHY: Without per-post metrics, no content decision below is data-driven — we're flying blind on a 289-deep queue.
- HOW: SSH to `raspberrypi5-ts`, check the newest `ig_insights_*.json` exists and is non-empty, and confirm the strategy script's media_id key matches (leading `1` truncation / int-vs-str is the usual culprit).

**2. Engagement caps went 100% unused in 24-36h**
- WHAT: Log shows 3 `inbox_sweep self` entries and zero follow/like/comment actions. Daily caps are 10/30/5 — utilization 0/0/0. `untapped_warm=0` at the same time is suspicious.
- WHO/WHERE: `social/engagement_log.json` + whatever the sweep writes untapped_warm to.
- WHY: Either (a) sweep detection is broken (more likely given both signals are zero) or (b) genuinely nobody engaged us, in which case we need proactive discovery, not reciprocation. Either way, the sweep is doing nothing revenue-adjacent right now.
- HOW: On next sweep, log the raw candidate list *before* the untapped filter; if the raw list is non-empty, the filter is over-aggressive. If empty, seed one hashtag-based discovery pass against a known-warm hashtag (still within 10/30/5 caps).

**3. Wire the auto-promoted product B01NBNDC1T (UTOPIA) into queued content**
- WHAT: Git log shows `Auto-promote vetted product B01NBNDC1T -> live keyword UTOPIA` — this is the current revenue vehicle. None of the last 5 posts or the queue head (cabinet / neck pillow / under-sink) reference it. Queue is 289 deep with pain-point setups and no visible product landing.
- WHO/WHERE: `social/post_queue.json` — the 3 head captions I can see are all cabinet-pain-point openers with no CTA.
- WHY: Content engine is producing pain-point hooks but not converting the hook into a specific vetted product. That's the entire funnel gap.
- HOW: Add a post-processing step that, for any caption matching organizer/under-sink pain-points, appends a bio-link / product CTA tied to the currently live UTOPIA promotion.

## Content angle to push
**"Under-sink cabinet: the one $X organizer that actually held"** — tied to product B01NBNDC1T (auto-promoted for keyword UTOPIA). Three of the last 5 captions and 2 of the 3 queue-head captions are under-sink / cabinet pain-point openers with *no product resolution*. The angle already resonates enough to be written this many times — the missing move is closing the loop with the vetted product. Can't cite real like/comment numbers because the insights join is broken (see action 1).

## Watch-outs
- All 5 recent post insights = `?` (metrics pipeline dead); untapped_warm=0 AND zero engagement actions in 24-36h (sweep may be detecting nothing); 289-post queue with no visible product CTA — hook volume outpacing monetization.
