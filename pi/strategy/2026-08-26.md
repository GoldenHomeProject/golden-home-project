# GHP daily strategy brief — 2026-08-26

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-08-26T14:15:09+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

1. **Pull real insight numbers on the last 5 posts before recommending an angle**
   - WHAT: The insights join is returning `likes=?  comments=?  reach=?` for all 5 recent posts, so today's "content angle" is being chosen blind.
   - WHO/WHERE: `automation/logs/ig_insights_*.json` join in `pi/strategy/` (the daily-brief script); posts `18402009514095260`, `17878511532527316`, `18610931881053410`, `18102216248071670`, `18619390060003367`.
   - WHY: Every one of the 5 rows in today's brief has `?` for likes/comments/reach — the join key or the insight-fetch job is broken. Without this the daily brief cannot rank angles by performance, which is the whole point.
   - HOW: Read the most recent `automation/logs/ig_insights_*.json`, confirm whether it has these 5 media IDs, then fix the join (likely media_id string vs int) in the strategy script.

2. **Drain the "mattress / neck pain" angle into a revenue post while it's live in the queue**
   - WHAT: The just-posted "Stop blaming your mattress for your neck pain" caption (media `18402009514095260`) and a queued follow-up ("it is 4am, your neck won't turn left") are both pain-first, high-intent sleep content — that's the highest-AOV Amazon category we touch (pillows, toppers, adjustable bases).
   - WHO/WHERE: `social/post_queue.json` head item 2 (the "4am neck" caption); pair with a cervical pillow / wedge affiliate.
   - WHY: Two of the 5 recent posts and 1 of the next 3 queued posts are already leaning into this pain point — signal that the content engine is clustering here. Ride it with an actual product link instead of pure story.
   - HOW: Edit the "4am neck" queue entry to append a single Amazon cervical-pillow link (subscription/free-tool constraint: use existing affiliate, no new tokens).

3. **Reciprocate with `iheartorganizing` on a second touchpoint**
   - WHAT: We commented on `iheartorganizing` at 14:07Z and DM'd them at 18:02Z on 2026-08-25 — that's a warm double-touch on a high-authority organizing account with no follow-through today.
   - WHO/WHERE: `iheartorganizing` (IG); log at `social/engagement_log.json`.
   - WHY: `untapped_warm=0` means the inbox sweep sees nobody new — but this account got two of our 5 daily comment/DM slots yesterday, which is a deliberate investment we should convert. One more like on their newest post (well under the 30 like / day cap) keeps us top-of-feed without burning the 5 comment cap.
   - HOW: On next sweep, add 1 like on `iheartorganizing`'s newest post; do NOT re-comment or re-DM.

## Content angle to push

**Pain-first sleep / neck / back copy with an affiliate attached.** Two of the last 5 posts (`18402009514095260` mattress/neck, and the queued "4am neck") and zero of the last 5 posts (`17878511532527316` under-sink, `18610931881053410` closet, `18102216248071670` July 4th tray, `18619390060003367` patio) monetize a high-AOV category. Sleep pain converts on Amazon better than tray styling. Caveat: we cannot rank by actual reach today because insights are `?` across all 5 — action #1 fixes that; treat this angle as a hypothesis until numbers land.

## Watch-outs

- Insights join is broken for all 5 recent posts (all `likes=?  comments=?  reach=?`) — brief is running blind.
- `untapped_warm=0` — either the inbox sweep is genuinely dry (unusual) or the sweep isn't writing/reading the warm-prospect field; worth a spot-check.
- `queue_size=268` is very deep; check the queue isn't accumulating stale seasonal content (the July 4th tray post already went out on 2026-08-25, which is out of season).
- 3 `social/*.json.bak-*` files from as far back as 2026-06-11 are sitting in the repo working tree — housekeeping, not urgent.
