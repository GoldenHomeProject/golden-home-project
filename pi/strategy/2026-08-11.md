# GHP daily strategy brief — 2026-08-11

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-08-11T14:09:38+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

1. **WHAT:** Investigate why the untapped_warm queue is empty (count=0) despite 3 inbox sweeps in 36h. Either engagement genuinely dried up, or the sweep classifier is broken.
   **WHO/WHERE:** `social/engagement_log.json` inbox_sweep entries at 2026-08-10T13:31Z, 2026-08-10T23:41Z, 2026-08-11T13:34Z; classifier in `pi/dm-funnel/` (untracked `__pycache__` suggests recent code edits).
   **WHY:** Only 1 follow + 1 DM in 36h. Yesterday's brief presumably had prospects; today has zero. That's either a real cliff (worth a content-angle pivot) or a silent regression that starves the whole funnel.
   **HOW:** Manually run the inbox sweep with verbose logging and compare against IG DM inbox counts: `ssh raspberrypi5-ts 'cd ~/golden-home-project && python -m pi.dm-funnel.inbox_sweep --verbose --dry-run'`.

2. **WHAT:** Fix the insights join — every one of the last 5 posts shows `likes=?  comments=?  reach=?`. Without post-level metrics, no content decision (this brief included) is actually data-driven.
   **WHO/WHERE:** `automation/logs/ig_insights_*.json` join in the strategy script; last-5 posts include media IDs 17925775788385747, 18088070462180529, 18095600717358600, 18032886038830495, 17981514854877564.
   **WHY:** The strategy brief's own "based on what's working" section is uninformed until this is fixed. Also blocks any prioritization of the 229-deep post queue.
   **HOW:** Check `ls -lt automation/logs/ig_insights_*.json | head -3` and diff the media IDs in the newest file against `social/posted_archive.json` — the join key or timestamp window is likely off.

3. **WHAT:** Vet + reply to the `iheartorganizing` DM thread (sent 2026-08-10T18:13Z) before it goes cold.
   **WHO/WHERE:** `@iheartorganizing` — high-authority home/organization account, exactly on GHP's ICP. DM thread in IG inbox.
   **WHY:** This is the single highest-authority outbound in the last 36h and the only DM sent. Warm replies from accounts like this drive follower quality more than any queued post. If they replied, we're already late; if they didn't, one soft follow-up within 48h is the norm.
   **HOW:** Open the thread manually, check for a reply, and either respond within 5 min or send one contextual follow-up referencing a specific recent post of theirs.

## Content angle to push
**Under-sink / cabinet-rescue transformation stories.** Two of the top 5 recent posts (`18088070462180529` "cabinet was where things went to die" and the queued "four years of bottles falling on my feet") plus the queue head all lean into "hidden cabinet chaos → fixed in a weekend" — clearly a working vein even without insight numbers, since the content engine is doubling down on it in the queue. Push one more variation targeting **bathroom vanity under-sink** specifically — same emotional beat (dread → relief), different room, opens a second product category (organizers + tension rods vs. kitchen utensil sets).

## Watch-outs
- untapped_warm=0 with only 1 DM + 1 follow in 36h — funnel may be starved or classifier broken.
- All post insights showing `?` — metrics pipeline is silently failing.
- `social/pinterest_queue.json.bak`, `.bak2`, `.bak3` accumulating — someone/something is retrying without cleanup.
- Post queue at 229 deep — content generation is outpacing posting cadence by a wide margin; consider whether the queue is being pruned or just growing forever.
