# GHP daily strategy brief — 2026-09-09

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-09-09T14:16:03+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

1. **Backfill insights on the last 5 posts before deciding anything else.**
   WHO/WHERE: `automation/logs/ig_insights_*.json` and the `?` fields on media IDs `17910006384465924`, `18016589321866793`, `18089090465654289`, `18141637246589801`, `17937615456367750`.
   WHY: Every post in the sample shows `likes=? comments=? reach=?`. Without those numbers, "what's working" is a guess and today's content angle is unfalsifiable. This is the single biggest blind spot in the brief.
   HOW: Run the IG insights fetcher against those 5 media IDs and re-emit the join so tomorrow's brief has real numbers.

2. **Do a manual DM/comment pass on the 4th-of-July centerpiece post (`18016589321866793`).**
   WHO/WHERE: replies + saves on that post; stay under the caps (≤5 comments, ≤10 follows). Skip any `._community` / giveaway handles.
   WHY: It's the only post in the sample with an explicit "one DM away" CTA — that's the only revenue-shaped surface currently live, and the untapped_warm queue is empty (count=0), so the warm-lead funnel is dry and needs to be re-primed at the source.
   HOW: Open the post, reply to every non-promo commenter with the promised follow-up, DM savers who fit ICP.

3. **Deduplicate the queue head; the patio-cabinet story is repeating.**
   WHO/WHERE: `social/post_queue.json` (queue_size=302) and `social/posted_archive.json` — media `17910006384465924` and `17937615456367750` both open with "My patio had been a low-grade mess…", and the queue's next cabinet post ("Three weeks ago this cabinet was where things went to die") echoes the already-posted `18089090465654289` cabinet hook.
   WHY: 302 items deep and we're already double-posting near-identical openers. That kills reach and burns the queue.
   HOW: Grep the queue for the first 6 words of each of the last 5 posted captions; delete or rewrite the collisions before the next scheduled post.

## Content angle to push
"**The $1,400 mistake**" — expand the mattress/neck-pain post (`18141637246589801`) into a mini-series on *expensive fixes that didn't work → cheap fix that did*. It's the only post in the last 5 with a specific dollar figure and a personal-failure hook, and the queue's next mattress caption ("4am, your neck won't turn left") is already leaning that way — commit to it as a 3-post arc (mattress, then two more high-price-tag misfires) instead of one-off. Caveat: I'm recommending this on hook shape alone because likes/comments/reach are missing — re-evaluate once action #1 lands real numbers.

## Watch-outs
- All 5 recent posts have null likes/comments/reach — insights pipeline is broken or not being joined.
- `untapped_warm=0` while inbox sweeps ran 3x in 24h — either genuinely dry (bad) or the sweep isn't tagging warm signals correctly (worse); spot-check one sweep's raw output.
- Duplicate patio caption posted as two separate media IDs (`17910006384465924` and `17937615456367750`) — the posting pipeline may not be marking items as posted before the next run picks them up again.
