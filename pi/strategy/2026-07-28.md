# GHP daily strategy brief — 2026-07-28

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-07-28T14:02:47+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

1. **Pull real insights on the last 5 posts before writing any more captions.**
   WHO/WHERE: media IDs `18130778470554791`, `18122603191697557`, `18133634410613772`, `17900294982474637`, `18109377388825612`; source `automation/logs/ig_insights_*.json`.
   WHY: Every row above shows `likes=?  comments=?  reach=?` — the strategy loop is flying blind on what's working. Two of the five posts are duplicate under-sink captions and one is near-duplicate ("Three weeks ago this cabinet…"), so we can't tell if the duplication is helping or cannibalizing without numbers.
   HOW: Re-run the insights fetcher for those 5 IDs and back-fill the join in the strategy input before the next brief fires.

2. **Deduplicate the queue head — the "under-sink cabinet" angle is stacking.**
   WHO/WHERE: `social/post_queue.json` positions 1 and 3 (both cabinet-under-sink openers), plus the two identical live posts (`18130778470554791` / `18122603191697557`).
   WHY: Queue size = 186 with visible near-duplicates at the head; posting the same hook twice in a week trains the algorithm to suppress the second. Untapped-warm count is 0, so we cannot lean on inbound to rescue a flat post.
   HOW: Reorder `post_queue.json` so the second cabinet caption drops at least 10 slots behind the first, and flag any exact-caption dupes in the archive for the poster to skip.

3. **Send one warm-follow-up DM off yesterday's `shiragill` thread.**
   WHO/WHERE: `shiragill` (DM sent 2026-07-27T18:02:18Z), logged in `social/engagement_log.json`.
   WHY: Only one outbound DM in the 36h window and zero untapped warm — the pipeline has nothing else moving. A same-week second touch on an already-warm handle is the highest-EV revenue action available under the caps (well under 5-comment / 10-follow ceilings).
   HOW: Reply once in-thread with a specific reference to her last message (not a template); log as `dm_followup` in `engagement_log.json`.

## Content angle to push
**"What I stopped buying" — subtractive organization**, seeded by the top-performing hook pattern in the current live set: *"Buying more containers will never fix your under-sink cabinet"* (media `18130778470554791` / `18122603191697557`, posted as a duplicate pair — signal that the content engine already believes in this hook). The patio and mosquito posts are additive/tool-recommendation angles; the anti-consumption framing is the differentiator and maps directly to affiliate pivots (one *right* product vs. five wrong ones). Ask the content engine for 3 captions in this frame targeting kitchen, bathroom, and garage — not another cabinet.

## Watch-outs
- Insights columns all `?` on last 5 posts — the join to `ig_insights_*.json` is broken or the fetch didn't run; brief is decision-making blind.
- Two live posts share the identical caption (`18130778470554791` and `18122603191697557`) — poster deduplication may have failed.
- `untapped_warm=0` across 3 inbox sweeps in 24h is suspiciously clean; verify the sweep is actually parsing DMs, not just returning empty.
- Only 1 outbound engagement action (a single DM) in 36h against caps of 10/30/5 — pipeline is under-utilized, not over.
