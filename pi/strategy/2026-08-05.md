# GHP daily strategy brief — 2026-08-05

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-08-05T14:07:11+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Fix the IG insights pipeline (data is dark)**
- WHAT: All 5 recent posts show `likes=? comments=? reach=?`. We cannot rank content by conversion, which is the whole point of this daily brief.
- WHO/WHERE: `automation/logs/ig_insights_*.json` (check newest file — is it empty, stale, or failing to join to `social/posted_archive.json`?)
- WHY: Zero metrics = zero ability to double down on winners. Revenue optimization is blind until this is restored.
- HOW: `ls -lat automation/logs/ig_insights_*.json | head -5` and inspect the latest — likely a token expiry or a joiner bug in the strategy script.

**2. Investigate `untapped_warm=0` + zero outbound engagement in log**
- WHAT: Engagement log for 36h shows ONLY 3 `inbox_sweep self` entries — no follows, likes, or comments went out. Untapped warm is 0.
- WHO/WHERE: `social/engagement_log.json` + whatever writes `untapped_warm` in the inbox sweeper.
- WHY: Either (a) nobody engaged us in 36h (unlikely — the reach is nonzero) or (b) the reciprocation tracker is broken. Either way we are leaving the daily cap (10/30/5) fully unused, which is directly foregone revenue-adjacent reach.
- HOW: Grep the sweep script for how it writes non-`self` rows and confirm the writer is running end-to-end, not just the first phase.

**3. Prune / freeze the 212-deep post queue**
- WHAT: Queue depth is 212. At 1-2 posts/day that is 3-6 months of stale inventory; back-to-school angles will ship in December.
- WHO/WHERE: `social/post_queue.json`
- WHY: The content-engine is out-producing publish cadence by ~10x. Old captions will feel dated and evergreen slots are already spoken for. Every new generation is compounding the problem.
- HOW: Cap the queue at ~30 items — archive the tail to `social/post_queue.archive.json` and pause generation until depth drops below 20.

## Content angle to push
**Back-to-school / dorm, this week only.** The one dated post in the last 5 (`17910164364446678` — sage green dorm comforter, "save this before move-in season sneaks up on you") is the only one with clear seasonal urgency, and August 5 is peak move-in prep window. The blog side already caught this (untracked posts today: `2026-08-03-best-shower-caddy-for-college`, `2026-08-04-best-over-the-door-organizer-for-nursery`) but IG queue head is 100% cabinet/mattress/closet evergreen. Push 2-3 dorm/shower-caddy/small-space-college captions to the FRONT of the queue this week before the window closes.

## Watch-outs
Insights pipeline appears dead (all `?`); engagement log shows only self-sweeps for 36h (no outbound actions); untapped_warm=0 is suspicious given post volume; post_queue at 212 is a generation-vs-publish mismatch; 20+ untracked `bg-2026-06-*.jpg` Pinterest files sitting since June suggest an incomplete Pinterest run that was never committed or purged.
