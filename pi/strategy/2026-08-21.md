# GHP daily strategy brief — 2026-08-21

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-08-21T14:10:54+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

1. **Repair the IG insights pipeline** — all 5 recent posts show `likes=? comments=? reach=?`, so every subsequent brief is flying blind and content decisions are unfalsifiable.
   - WHO/WHERE: `automation/logs/ig_insights_*.json` and whatever writes it; joined to `social/posted_archive.json`.
   - WHY: You cannot pick a winning angle when the entire feedback loop is null. This is upstream of everything else.
   - HOW: Manually run the insights fetcher for media IDs 18105322931129139, 18113695585752130, 18608922436003615, 17904995907480402, 17970042372006033 and confirm a fresh `ig_insights_*.json` lands with non-null numeric fields.

2. **Investigate why `untapped_warm=0` and outbound engagement is zero** — 24h of engagement log shows only 3 `inbox_sweep self` rows, no follow/like/comment actions and no reciprocable prospects surfaced. Under the 10/30/5 cap the daily budget is being left entirely on the table.
   - WHO/WHERE: `social/engagement_log.json`, the inbox-sweep script in `pi/dm-funnel/`, and whatever populates `untapped_warm`.
   - WHY: Either the sweep genuinely found nothing (unlikely two days running) or the warm-prospect detector is broken. Either way, revenue-relevant reciprocation is at zero.
   - HOW: Run one sweep by hand with verbose logging and confirm the warm-prospect classifier still returns non-empty on a known-engaging follower; if empty, diff against last week's sweep output.

3. **De-duplicate the post queue vs. posted archive** — the queue head ("Three weeks ago this cabinet…", "Picture this: it is 4am…") is identical to entries in the last-5-posts list, i.e. already published. With `queue_size=248` this suggests the queue isn't being popped, or archive-writes aren't filtering the queue.
   - WHO/WHERE: `social/post_queue.json` vs. `social/posted_archive.json`; poster script that reads the head.
   - WHY: Risk of re-posting identical captions to IG, which tanks reach and looks like a bot.
   - HOW: Diff top-20 of `post_queue.json` against last 20 in `posted_archive.json`; if overlap, pop the already-posted items from the queue before the next scheduled post fires.

## Content angle to push
**Back-to-school / dorm & first-apartment upgrades**, window closing fast. The most recent post (media 18105322931129139, "dorm room desk isn't the problem — your wall is empty") is the only seasonal piece in the last 5 and today is 2026-08-21 — move-in weekends are peaking now. Queue is heavy on year-round themes (cabinet, kitchen, sink, pillow). Ask the content engine for 3-5 more dorm/small-space/rental-friendly posts (command-strip shelving, over-door hooks, under-bed bins, mini-fridge organizers) and float them to the top of the queue for the next 7 days.

## Watch-outs
- Insights fully null on last 5 posts (measurement broken). — Queue head duplicates already-posted captions (poster or dedupe broken). — 0 outbound engagement actions and `untapped_warm=0` for 24h+ (sweep classifier suspect). — Engagement log only records `inbox_sweep self`, no per-action rows, so daily caps cannot be audited.
