# GHP daily strategy brief — 2026-06-02

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-06-02T14:10:44+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Fix the IG insights pipeline — all 5 recent posts show `likes=? comments=? reach=?`**
- WHO/WHERE: `automation/logs/ig_insights_*.json` and the join logic in `pi/strategy/` brief generator; media IDs 18130380709600264, 18315795139302670, 18400197637155262, 18098496266140274, 18465834370100122
- WHY: Zero post-performance data across 5 consecutive posts means every content/angle decision (including this brief) is unanchored. Revenue prioritization is impossible without knowing which captions convert.
- HOW: SSH to Pi, check newest `automation/logs/ig_insights_*.json` for actual content vs the join key the brief script uses — likely a media_id mismatch or a stale/empty insights file.

**2. Repair the DM script's "Message" button selector (iheartorganizing timeout 2026-06-01T18:10:56Z)**
- WHO/WHERE: Playwright DM module, locator `get_by_role("button", name=re.compile(r"^Message$", re.IGNORECASE)).first`
- WHY: IG's profile UI shifts the Message control between button/role/aria periodically. One DM error today often means *all* DM outreach is silently failing — and DMs are the warm-prospect closing channel.
- HOW: Manually load iheartorganizing's profile in a headed Playwright run; capture the current selector for Message and add a fallback chain (button → div[role=button] → text="Message").

**3. Audit the untapped_warm detector — count=0 is suspicious, not clean**
- WHO/WHERE: inbox sweep module that writes "untapped warm prospects" (the script that powers this brief's section)
- WHY: With 4 inbox sweeps logged in 36h and zero warm reciprocity candidates flagged, either the detector is broken OR genuine inbound engagement collapsed. Either case starves the engagement caps (10 follow / 30 like / 5 comment will go unused).
- HOW: Spot-check 2-3 recent IG notifications manually against the detector's output; verify the "engaged us & we haven't reciprocated" join still works after any recent IG DOM changes.

## Content angle to push

**Affiliate-friendly pillow angle for side sleepers, leaning into the "74% of us sleep on our sides" stat already in post 18465834370100122.** Two of the last 5 posts (and at least one queued post) target side-sleeper neck pain — that's signal the content engine has identified an audience, and pillows are higher-AOV affiliate territory than under-sink bins. Push the next caption toward a specific *product category recommendation* (cervical contour vs. memory foam vs. latex) with a clear "save this" CTA, so when insights come back online we can actually measure which sub-angle converts. **Note:** without numeric reach/likes data this is judgment, not evidence — re-evaluate once action #1 lands.

## Watch-outs

- All 5 recent posts have null insights (likes/comments/reach all `?`) — pipeline is broken, not posts underperforming.
- DM automation hit a 30s Playwright timeout on iheartorganizing — likely affecting other targets silently.
- `untapped_warm count=0` despite 4 inbox sweeps in 36h — either detector regression or inbound genuinely flatlined; both warrant a human eyeball.
- Queue head is heavy on repeated under-sink-cabinet framing ("things went to die" / "bottles falling on my feet") — risk of feeling samey to repeat viewers.
