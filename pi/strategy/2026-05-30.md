# GHP daily strategy brief — 2026-05-30

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-05-30T14:07:01+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Fix the insights ingest — all 5 recent posts show likes=? comments=? reach=?**
- WHO/WHERE: `automation/logs/ig_insights_*.json` ⟷ `social/posted_archive.json` join; media IDs 18160749547395612, 17969405327908011, 18114279580875787, 18130380709600264, 18315795139302670
- WHY: Every content/engagement decision below this brief depends on knowing which posts won. Five posts of null insights = a week of flying blind. Highest revenue leverage because we can't double down on what we can't measure.
- HOW: Pull the newest `ig_insights_*.json`, confirm whether it's empty/malformed or just not joining on media_id, and patch the join (or the IG Graph API call if the file is empty).

**2. Patch the `thehomeedit` comment-click selector and re-run that target**
- WHO/WHERE: 2026-05-29T22:20:28Z `comment_error thehomeedit`; locator `a[href*='/p/']` intercepted by `div.x1qjc9v5.x9f619.x78zum5...` overlay (60 retries, 30s timeout)
- WHY: `thehomeedit` is a top-tier niche-adjacent target. An overlay intercept means IG layout shifted — this will silently kill comments on that whole class of accounts until fixed.
- HOW: Switch to `.click({force: true})` or dismiss the overlay first (likely a "see more" / login modal) before `.nth(1)` click in the engagement-sweep Playwright script.

**3. Reciprocate the 1 untapped_warm and diagnose why the pool is empty**
- WHO/WHERE: latest inbox sweep at 2026-05-30T13:36:15Z surfaced only 1 untapped_warm
- WHY: Yesterday we sent 2 comments (cap 5), 0 follows (cap 10), 0 likes (cap 30) — we're running 5-20% of caps. Either the warm-prospect detector regressed or the sweep is filtering too aggressively. Caps exist to protect us, not to be a ceiling we never approach.
- HOW: Action the 1 warm prospect now (follow + thoughtful comment), then read the inbox-sweep filter to confirm `._community`/promo exclusion isn't over-matching legitimate accounts.

## Content angle to push

**Push a "summer heat + side-sleeper neck pain" angle** — it ties the strongest hook pattern we have (3 of last 5 posts are sleep-themed: $1,400 mattress, 4am neck pain, side-sleeper pillow) to a seasonal trigger (end-of-May, audience entering hot-sleep months). Concrete caption seed: *"Side sleepers — your pillow is fine in winter and a torture device in July."* Revenue-adjacent (cooling pillows, breathable cases) and pattern-interrupts the queue, which currently has another cabinet post + another generic neck-pain post stacked back-to-back. Caveat: without insights data we are guessing which sleep hook actually converted — pair this with action #1.

## Watch-outs

- All 5 recent posts have `likes=? comments=? reach=?` — insights pipeline is broken or unjoined.
- `thehomeedit` comment failed with overlay-intercept after 60 retries; selector regression likely affects more accounts.
- `untapped_warm=1` is suspiciously low given 2x/day sweep cadence — possible filter over-match.
- `el_cracksito2013` surfaced as a follow candidate (skipped only because no_follow_button); off-niche account suggests target list needs a niche filter pass.
- Queue head repeats both dominant themes (cabinet, neck pain) with no diversification across the next 3 posts.
