# GHP daily strategy brief — 2026-08-17

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-08-17T14:04:02+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Fix the insights join in the daily brief pipeline**
- WHAT: All 5 recent posts show `likes=? comments=? reach=?`. The brief can't make revenue-first calls without engagement signal.
- WHO/WHERE: `pi/strategy/` script that joins `social/posted_archive.json` to `automation/logs/ig_insights_*.json`; media IDs 17896677240374841, 18227026603322659, 17953384878080121, 18162323293466949, 18105322931129139.
- WHY: Without numbers, tomorrow's brief is guessing too. This is the #1 blocker on every downstream judgment call.
- HOW: Open `pi/strategy/` (likely `build_brief.py` or similar), inspect the join key against the newest `ig_insights_*.json` — probably a media-id-as-string vs int mismatch or the insights file isn't being picked up.

**2. Purge seasonally-dead content from the 240-item queue**
- WHAT: A "tiered tray → Fourth of July-ready" post went out (media 18162323293466949) and today is 2026-08-17. 4th of July content in mid-August is a credibility hit and wastes a slot.
- WHO/WHERE: `social/post_queue.json` (240 deep) and `social/posted_archive.json`.
- WHY: Queue depth is healthy, so pruning is safe. Every stale-holiday slot displaces evergreen "system-not-stuff" content that's likely converting.
- HOW: `grep -iE '(fourth of july|july 4|labor day|back to school|dorm)' social/post_queue.json` — flag/remove or push past date-of-relevance.

**3. Confirm engagement sweeps are actually executing actions, not just scanning**
- WHAT: Last 36h shows 3 `inbox_sweep` entries and zero `follow`/`like`/`comment` actions. Caps (10/30/5) are wide open. `untapped_warm=0` on top of that is suspicious.
- WHO/WHERE: `pi/dm-funnel/` (or wherever the sweep runs) → `social/engagement_log.json`.
- WHY: If the sweep is finding nothing to act on 2x/day for a week, either targeting is broken or the reciprocity queue isn't being populated. Either way = zero relationship growth.
- HOW: On Pi, tail the last sweep log and check: did it find candidates and filter them all out, or did it find zero? Grep the log for "candidates=" or equivalent.

## Content angle to push
**"Nothing in your kitchen has a home" — homes-not-containers.** The one recent post whose caption I can read directly hits this ("The reason your cabinets stay messy is that nothing has a home. Containers don't fix this."), and the queue is already leaning the same direction ("cabinet where things went to die", "bottles falling on my feet"). Push the content engine to line up 3-5 more posts in the *homes-for-things > buying-more-organizers* frame — under-sink, pantry, entry drop-zone, medicine cabinet, junk drawer. **Caveat: I can't cite likes/reach because the insights pipeline is broken (see action #1) — this is a caption-signal read, not a numbers read.**

## Watch-outs
- Insights all `?` across 5 posts → brief pipeline is flying blind
- Zero outbound engagement actions in 36h despite 10/30/5 caps wide open
- `untapped_warm=0` with 2x/day sweeps → likely a filter/targeting bug, not a real dry spell
- Live Fourth-of-July post on 2026-08-17 → queue has seasonally stale items still in rotation
- 240-item queue depth is healthy but hides stale content — worth a scheduled prune
