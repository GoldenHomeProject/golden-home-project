# GHP daily strategy brief — 2026-05-26

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-05-26T23:56:54+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

1. **Dedupe `social/post_queue.json` against `posted_archive.json` before next scheduled post.**
   - WHO/WHERE: queue head item "Three weeks ago this cabinet was where things went to die..." is already in posted_archive as media=18101976548008102. Also note posted_archive shows two near-identical "nothing has a home" cabinet posts (18105064988478438 and 18079040495560007) — duplicate has already shipped to IG once.
   - WHY: posting the same caption twice burns audience trust and tanks reach on the duplicate (algorithm penalizes recycled content). A duplicate has already gone out; queue is set up to do it again.
   - HOW: run a caption-hash diff between `social/post_queue.json` and the last 30 entries of `social/posted_archive.json`; drop or rewrite any queue item with a fuzzy match.

2. **Verify the engagement sweep actually ran in the last 24-36h.**
   - WHO/WHERE: `ssh raspberrypi5-ts 'systemctl --user list-timers ghp-engagement*'` and `tail social/engagement_log.json`.
   - WHY: window shows zero actions AND zero untapped_warm. Either the timer skipped or the inbox sweep silently no-op'd. Both sweeps in a 36h window producing nothing is anomalous — caps are 10/30/5/day, so even a quiet day should show *something*.
   - HOW: check timer last-trigger; if it fired but log is empty, the inbox-fetch or login flow likely broke — re-auth and run one manual sweep (still within daily caps).

3. **Fix the insights pipeline — all 5 recent posts show `likes=? comments=? reach=?`.**
   - WHO/WHERE: `automation/logs/ig_insights_*.json` join against `social/posted_archive.json` for media IDs 18105064988478438, 17989746005992201, 18117766345684687, 18101976548008102, 18079040495560007.
   - WHY: without per-post metrics we can't tell which angle (cabinets vs pillow vs mattress) is actually pulling reach, and every downstream judgment call (this brief included) is flying blind. Revenue depends on doubling down on what converts.
   - HOW: check the most recent `ig_insights_*.json` exists and is non-empty; if missing/stale, re-run the insights collector and confirm it writes media-keyed records the brief script can join on.

## Content angle to push

**Side-sleeper pillow geometry, data-led.** The queued item leading with "74% of us sleep on our sides" + the 30-degree spine angle hook is the strongest revenue angle currently in inventory — it (a) names a specific, measurable problem most people don't know they have, (b) maps directly to an affiliate-friendly product category (pillows), and (c) breaks the cabinet-organization monotony of the last 5 posts (3 of 5 = cabinets, 2 of which are near-duplicates). Pair it with the queued $1,400-mattress confession post for a two-beat sleep-problem narrative this week.

## Watch-outs

- Engagement log empty for 24-36h despite 2x/day sweep cadence — sweep may be silently failing.
- All 5 recent posts have missing insights (`?` for likes/comments/reach) — metrics pipeline likely broken.
- Posted_archive contains two near-identical "nothing has a home" cabinet captions — dedup has already failed once.
- Queue head item "Three weeks ago this cabinet..." appears verbatim in posted_archive as media=18101976548008102 — about to repost.
- `untapped_warm=0` is suspicious given two daily sweeps; likely a symptom of #2, not a real zero.
