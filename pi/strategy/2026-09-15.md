# GHP daily strategy brief — 2026-09-15

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-09-15T14:05:38+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

1. **Fix the insights → posts join so we can measure anything.**
   - WHO/WHERE: `automation/logs/ig_insights_*.json` → `social/posted_archive.json`; media IDs `17937615456367750`, `18390434311164341`, `17985194421097908`, `17988600426112673`, `18037538666831058`
   - WHY: All 5 most recent posts show `likes=? comments=? reach=?`. We are shipping a 316-deep queue blind. Every content decision downstream (angle, hook, CTA) is guesswork until this join works.
   - HOW: On the Pi, run the insights fetch for those 5 media IDs and confirm the daily strategy script actually reads the newest `ig_insights_*.json` — likely a filename/timestamp mismatch or missing IG_USER_ID env in the strategy job.

2. **Diagnose why engagement caps are 100% unused.**
   - WHO/WHERE: `social/engagement_log.json` — last 36h shows only `inbox_sweep self`, zero `follow`/`like`/`comment` rows. `untapped_warm count=0`.
   - WHY: Daily budget of 10/30/5 is being burned to $0. The sweeps are running (3 entries logged) but the sweep is doing no outbound. Either the "warm prospect" query returns empty (detection bug) or the sweep is short-circuiting before it acts.
   - HOW: Tail `automation/logs/engagement_sweep_*.log` for the 13:42 UTC run today and confirm whether the sweep found 0 candidates vs. found candidates and skipped. If empty seed list, seed 3-5 handles manually from last week's commenters.

3. **Break template drift in the queue head before it ships.**
   - WHO/WHERE: `social/post_queue.json` — head contains "Three weeks ago this cabinet was where things went to die" (cabinet #1) and "four years of bottles falling on my feet… cabinet under the kitchen sink" (cabinet #2). Two consecutive cabinet-death-trap posts.
   - WHY: Recent archive already ran "patio…low-grade mess for longer than I want to admit" + "closet…low-grade mess for longer than I want to admit" — near-identical opener. IG will throttle repeat structures; audience will scroll.
   - HOW: Rewrite queue item #3 (under-sink) hook away from "cabinet where things go to die" — reuse the pain-symptom opener style from item #2 (4am neck) instead. One edit, no re-queue needed.

## Content angle to push
**Pain-symptom cold-open → single-product home fix**, modeled on queue item #2: *"it is 4am, your neck won't turn left, and the alarm is in 90 minutes."* Every recent post opens with a *space* ("my patio", "your pantry", "your closet", "your kitchen") — none open with a *body sensation the reader is having right now*. Pain-first hooks index harder on save/share, and the natural product bridge (pillow, mattress topper, ergonomic chair) is higher-AOV than cabinet hardware. Push 2-3 of these into the next batch and A/B against the space-first opener once insights are actually readable.

## Watch-outs
- All 5 recent posts show `?` insights — measurement pipeline broken.
- Engagement log has zero outbound actions in 36h despite 3 sweep runs — sweep is a no-op.
- `untapped_warm=0` is suspicious given we're posting daily; likely a detection or filter regression.
- Queue head has 2 consecutive cabinet-themed posts and repeats the "low-grade mess" template already used twice in the archive.
