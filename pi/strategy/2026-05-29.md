# GHP daily strategy brief — 2026-05-29

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-05-29T14:04:48+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Fix the IG insights → posted_archive join (data blackout)**
- WHAT: All 5 most recent posts show `likes=? comments=? reach=?`. We are flying blind on which captions converted. Without this, every "what's working" decision is a guess.
- WHO/WHERE: `automation/logs/ig_insights_*.json` ↔ `social/posted_archive.json` join in the strategy script; media IDs 18121386385656164, 18160749547395612, 17969405327908011, 18114279580875787, 18130380709600264.
- WHY: Strategy briefs are useless without performance signal. This is upstream of every revenue decision.
- HOW: Check whether the newest `ig_insights_*.json` is being written and whether the join key matches `media_id` (not `id` or `ig_id`). One field rename likely fixes it.

**2. Investigate the engagement sweep under-utilization**
- WHAT: 36h log shows 3 `inbox_sweep` + 1 DM. Zero likes, zero follows, zero comments. Caps allow 30/10/5 per day — we used ~0% of the like cap.
- WHO/WHERE: Pi `engagement_log.json`, the sweep handler that decides outbound actions; only outbound was DM → `organizedbyellis` at 04:06 UTC.
- WHY: Outbound reciprocation is the cheapest revenue lever we have on a sub-only stack. If the sweep is silently degraded (rate limit? login challenge? empty target list?), every day it sits broken is wasted reach.
- HOW: SSH the Pi and read the last sweep stderr/stdout — `tail -200 ~/.ghp-strategy/runs/last.log` plus whatever the sweep logs to; confirm it's still authenticated and the target list isn't empty.

**3. Add a monetization hook to the next side-sleeper post**
- WHAT: Side-sleeper / pillow content is 3 of the last 5 posts (18121386385656164, 18160749547395612, 18130380709600264). Captions tell a great story (74% stat, $1,400 mattress regret) but I see no link-in-bio CTA or product handoff.
- WHO/WHERE: Next pillow post in `social/post_queue.json` — modify before it ships.
- WHY: We are spending creative inventory on the theme that recurs most. If it converts, we need to know; if it doesn't convert without a CTA, that's a content problem masquerading as an audience problem.
- HOW: Append one line — "Link in bio for the pillow that fixed mine" (or whatever the actual lead magnet/affiliate is) — to the next side-sleeper caption only, so we can A/B against the unhooked versions once insights are restored.

## Content angle to push
**"I spent $X trying to fix this — here's what actually worked"** first-person purchase-regret stories, anchored to the side-sleeper pillow theme. The $1,400-mattress caption (media 18160749547395612) is the strongest hook in the last 5 because it names a specific dollar amount and a specific failed fix before pivoting. Side-sleeper content already dominates the recent set (3/5) — lean into it with concrete dollar regrets, not generic tips.

## Watch-outs
- Insights data blackout (all 5 posts `?/?/?`); `untapped_warm=0` could be real or could mean the inbox sweep is silently failing; outbound engagement actions sit at ~0% of daily caps despite 3 sweeps firing.
