# GHP daily strategy brief — 2026-05-28

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-05-28T14:13:30+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Debug the follow_error storm on the Pi engagement bot**
- WHAT: 7 of ~10 follow attempts in the last 24h returned `no_state_change` (houseofhammer5, weird__designs, weluv._.t, mh.magazines, froggy_in_spaceland, its_naved_32_______, plus the 23:33 sweep). Only 2 follows actually went through (cabellhouse, that_wild_katt).
- WHO/WHERE: `social/engagement_log.json`, Pi engagement-sweep script on `raspberrypi5-ts`, IG follow-button selector logic.
- WHY: 70%+ failure rate means the warm-prospect → follow-back → DM funnel (the actual revenue path) is barely running. Two successful follows in a day is below floor.
- HOW: `ssh raspberrypi5-ts 'tail -200 ~/.ghp-engagement/runs/last.log'` and check whether the follow button selector changed or session is stale; manually re-login if needed.

**2. Fix the IG insights ingestion — every post shows `likes=?  comments=?  reach=?`**
- WHAT: All 5 most recent posts (18117766345684687, 18101976548008102, 18079040495560007, 18121386385656164, 18160749547395612) have empty metrics in the join.
- WHO/WHERE: `automation/logs/ig_insights_*.json`, the join step in the strategy script that pulls insights into `posted_archive.json`.
- WHY: Without per-post metrics we cannot tell which captions convert. Content decisions (including the angle below) are guesses until this is restored.
- HOW: Check the freshness of the newest `ig_insights_*.json` file; if stale, re-run the insights fetcher; if fresh, the join key in the strategy script is broken.

**3. Sanity-check the inbox sweep — untapped_warm count is 0**
- WHAT: Zero untapped warm prospects after both the 13:34Z and 23:33Z and 13:35Z sweeps.
- WHO/WHERE: inbox_sweep entries in `social/engagement_log.json`, the sweep parser.
- WHY: With sleep/pillow content shipping daily, a true zero is unlikely. Combined with item 1, this points to either a parser regression or auth issue, not a clean inbox. If real, it also explains why follows are scraping the bottom of the barrel.
- HOW: Manually open IG notifications/DMs for the last 24h and cross-check against what the sweep logged; if there are real engagers it missed, the parser broke.

## Content angle to push
Push the **"I spent $1,400 on the wrong fix"** angle as a standalone post (media=18160749547395612 already opened this thread, and 18121386385656164 supports it with the 74%-side-sleeper stat). The pattern that's repeating across 3 of the last 5 posts is: *expensive wrong purchase → cheap correct fix → specific spec*. That sequence is the affiliate-revenue setup. Next caption should name the actual pillow spec (loft height, fill type) for a side sleeper — that's the line where a reader DMs asking what to buy. We cannot confirm with numbers because insights are missing (see action 2), but the queue is heavy on cabinet/under-sink (4 of 9 head items look organizational) and light on the sleep-spec angle that has the clearest monetization path.

## Watch-outs
- 70% follow_error rate on Pi sweep; insights metrics blank across last 5 posts; untapped_warm=0 after 3 sweeps; queue is over-indexed on under-sink/cabinet vs. the sleep angle that has affiliate potential; `kiwi.living_1234` returned `no_follow_button` which is a different failure mode worth a second look.
