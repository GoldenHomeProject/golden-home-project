# GHP daily strategy brief — 2026-08-20

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-08-20T14:13:03+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Diagnose why engagement sweeps ran but took zero actions**
- WHO/WHERE: `social/engagement_log.json` — last 36h shows only 3 `inbox_sweep self` rows, no follow/like/comment actions. Untapped_warm=0 from same sweeps.
- WHY: We're at 0/10 follow, 0/30 like, 0/5 comment on the daily caps. The revenue funnel starts here — if two sweeps in a row surface nobody to reciprocate, either discovery is broken or the warm-prospect filter is too tight. A healthy account should have *some* warm inbound in 36h.
- HOW: Read the last two sweep run logs and one raw inbox pull; confirm whether the API returned zero engagers vs. the ranker filtered them all out.

**2. Fix the insights pipeline — all 5 recent posts show `likes=? comments=? reach=?`**
- WHO/WHERE: `automation/logs/ig_insights_*.json` — the join to `social/posted_archive.json` is producing no metrics for media IDs 18162323293466949, 18105322931129139, 18113695585752130, 18608922436003615, 17904995907480402.
- WHY: Content angle decisions right now are blind. We can't tell if the pillow/neck-pain hook, dorm float shelf, or under-sink transformation is the winner. Every day this stays broken = another day of queue-shuffling on gut feel.
- HOW: Check timestamp on newest `ig_insights_*.json` and confirm the fetcher is actually running post-publish (not just once/day pre-post).

**3. Ride the back-to-school window while it's live (Aug 20 is peak)**
- WHO/WHERE: `social/post_queue.json` head — reorder the dorm/back-to-school captions to the front, ahead of the 4am-neck-pain and under-sink posts.
- WHY: The one dorm caption ("your wall is empty… stick-on floating…") is seasonally timed for the ~2-week move-in window that starts now. The other queue heads (cabinet-declutter, neck pain, kitchen mess) are evergreen and can wait a week without decay. Back-to-school cannot.
- HOW: Manually promote 2-3 dorm/small-space/move-in captions to positions 1-3 in the queue file.

## Content angle to push
**Small-space / dorm move-in transformations, framed as "wall is empty" or "desk is the problem"** — the one dorm post already in queue (media 18105322931129139) is the only seasonally-aligned piece in the last 5 posts and next 3 queued. Every other recent post is evergreen home-declutter. With Aug 20 = peak dorm shopping and 245 items in queue, the content engine should generate 3-5 more dorm/first-apartment variants (command-strip organizers, over-the-door racks, under-bed bins) this week before the window closes. I can't cite likes/reach because insights are broken — see action #2.

## Watch-outs
- Engagement log has ONLY `inbox_sweep self` entries in 36h — no outbound actions AND `untapped_warm=0` from every sweep, which is suspicious.
- All 5 most recent posts return `?` for likes/comments/reach — insights fetch is stale or broken.
- Queue depth 245 is healthy but nothing seasonal is queued at the head besides the one dorm caption; the tiered-tray/Fourth-of-July caption at the top is 6+ weeks past its window.
