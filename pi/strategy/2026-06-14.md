# GHP daily strategy brief — 2026-06-14

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-06-14T14:04:03+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

1. **Restore post-insights collection (data is blind right now)**
   - WHO/WHERE: `automation/logs/ig_insights_*.json` joined to `social/posted_archive.json`; last 5 media all show `likes=?  comments=?  reach=?`
   - WHY: Every recent post has missing metrics. We cannot tell which angle (sleep, under-sink, plug-in safety) is actually converting. Strategy without insights is guessing — revenue decisions downstream all blocked on this.
   - HOW: SSH the Pi and run the insights fetcher manually; check whether the IG Graph token expired or the script silently fails: `ssh raspberrypi5-ts 'cd ~/golden-home-project && python3 automation/fetch_ig_insights.py --debug 2>&1 | tail -50'`

2. **Burn down the 46-deep post queue or stop generating**
   - WHO/WHERE: `social/post_queue.json` (queue_size=46), `social/dm_funnel_cron.log`, content-engine cron
   - WHY: Yesterday's snapshot said "queue exhausted, ending" (commit e59d0ad) yet head shows 46 items now — content engine produced 40+ in a day. At 1-2 posts/day that's a month of inventory before any feedback loop closes. We're shipping captions that may already be stale by the time they post.
   - HOW: Pause the content-engine GH Action for 48h and let the queue drain: `gh workflow disable content-engine.yml` (re-enable after we see what insights say is working).

3. **Reciprocate to commented accounts before the 24h window closes**
   - WHO/WHERE: `@thehomeedit` and `@neatmethod` (commented on 2026-06-13 14:05/14:07Z)
   - WHY: untapped_warm=0 means no inbound to chase, but we just spent 2 of 5 daily comments on these two majors. If they reply and we ghost, the warm signal evaporates. These are the highest-trust orgs in the niche.
   - HOW: Next inbox_sweep, prioritize checking those two threads for replies/follow-backs and queue a single like-back or reply (well within the 30/5 caps).

## Content angle to push
**The "I spent $X and it didn't fix it" reveal** — the mattress post (`media=18086771459434877`, "$1,400 mattress to fix my neck pain. Didn't help.") and the container post ("Buying more containers will never fix your under-sink cabinet. I tried clear bins...") both lead with *expensive-thing-that-failed*. That's the strongest hook pattern in the last 5. Next caption should open with a specific dollar amount + specific failure, not a generic "picture this" scene. Caveat: I can't confirm with engagement numbers — insights are all `?`.

## Watch-outs
- All 5 recent post insights show `?` for likes/comments/reach — metrics pipeline appears broken.
- Queue grew from "exhausted" to 46 in ~24h — content engine may be over-producing.
- Only 2 comments + 3 inbox sweeps in 36h, zero follows/likes logged — well under caps but unusually quiet; verify engagement script isn't silently no-op'ing.
- untapped_warm=0 is suspicious given we just commented on two large accounts yesterday; could be a stale sweep or a parser bug.
