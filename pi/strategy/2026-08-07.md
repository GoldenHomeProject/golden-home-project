# GHP daily strategy brief — 2026-08-07

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-08-07T14:04:08+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

1. **WHAT:** Fix the `claude_empty` comment failure that killed 6 consecutive comment attempts on 2026-08-06 14:10-14:19Z.
   **WHO/WHERE:** Targets: `iheartorganizing`, `organizedbyellis`, `cindyology`, `simplyspaced`, `lifeinjeneral`, `shiragill`. Code: `pi/dm-funnel/` (comment generation path calling Claude). Log: `social/engagement_log.json`.
   **WHY:** Zero comments landed in the entire 36h window — the 5/day comment cap is being 100% wasted on empty Claude responses. Six failures in 9 minutes = systemic, not transient. Highest-leverage fix because comments are the top-of-funnel that produces the untapped_warm we currently have zero of.
   **HOW:** Reproduce one call manually (`claude -p --model claude-opus-4-7 <prompt>`), check for empty stdout / prompt template regression / missing input variable, and add a retry-with-fallback so the daily 5 comment cap actually gets used.

2. **WHAT:** Audit why `untapped_warm=0` after 3 inbox sweeps in 24h.
   **WHO/WHERE:** Inbox sweep code path (2026-08-06T13:43, 2026-08-06T23:45, 2026-08-07T13:44 all self-only). Check the DM/comment-reply detection in `pi/dm-funnel/`.
   **WHY:** Three sweeps returned only `self` — either (a) nobody is engaging (correlates with issue #1: no comments going out = no reciprocal engagement) or (b) the detector is broken. Either way this is the reciprocation funnel dying silently. No warm prospects = no follow/DM conversion pipeline.
   **HOW:** Manually open IG inbox + last 5 posts, count real unreplied comments/DMs from non-`._community` accounts, and compare to what the sweep reports. If detector is broken, fix parser; if organic engagement really is zero, that's a content-signal problem (see below).

3. **WHAT:** Pull actual likes/comments/reach for the last 5 posts so tomorrow's brief has real numbers.
   **WHO/WHERE:** `automation/logs/ig_insights_*.json` join in the strategy script — all 5 posts came back with `likes=?  comments=?  reach=?`.
   **WHY:** Making content-angle calls without post-performance data is guessing. This blocks the whole judgment loop the brief exists to serve.
   **HOW:** Check whether the latest `ig_insights_*.json` file is being written (cron alive?) and whether the media_id join key matches; if insights fetcher hasn't run in >24h, kick it once manually.

## Content angle to push

**Angle: under-sink / hidden-cabinet transformation, framed around "things falling on my feet" pain.** The queue already has two variants pointed at this ("Three weeks ago this cabinet was where things went to die" + "four years of bottles falling on my feet every morning") — both use the same specific-physical-annoyance hook that worked in the mattress post (`I bought a $1,400 mattress to fix my neck pain. Didn't help.`) and the closet post (`low-grade mess for longer than I want to admit`). Push one of these two to the top of the queue next. It's the pattern that's already in the winning captions: named dollar amount OR named time span + specific body/sensory complaint, not aesthetic language. **Caveat:** I cannot confirm those posts actually won because likes/comments/reach came back `?` — this is pattern-matching on caption style, not measured performance. Fixing action #3 above lets us validate this angle for real.

## Watch-outs

- 6/6 comment attempts failed with `claude_empty` on 2026-08-06 — comment generator is broken.
- Post insights all `?` — insights fetcher or join key likely broken; blind on performance.
- `untapped_warm=0` across 3 sweeps — either detector broken or organic engagement collapsed; both bad.
- Backup files piling up in `social/` (`.bak-20260611`, `.bak-20260725` x2, `.bak2`, `.bak3`) — cleanup drift.
- Queue depth 218 is healthy, but no point posting more if none of the last 5 have measured performance.
