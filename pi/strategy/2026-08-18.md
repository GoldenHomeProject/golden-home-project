# GHP daily strategy brief — 2026-08-18

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-08-18T14:08:02+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

1. **WHAT:** Wire real IG insight numbers (likes/comments/reach) into this brief. **WHO/WHERE:** `automation/logs/ig_insights_*.json` → the joiner in `pi/strategy/` that builds the "Last 5 posts" block; all 5 posts in today's input show `likes=? comments=? reach=?`. **WHY:** Without per-post numbers, no content decision below tier "vibes" is possible — the whole point of the daily brief is to pick winners, and today's data is blind. **HOW:** Grep the joiner for how it keys posts to insight rows and fix the media_id lookup (or backfill the missing insight pull) so tomorrow's brief renders real integers.

2. **WHAT:** Seed the empty untapped-warm pool by commenting on 3–5 fresh saves/comments from the last two posts' engagers (kitchen-cabinet + backsplash themes). **WHO/WHERE:** Engagers on media `18113695585752130` (cabinet) and `17953384878080121` (backsplash); use the same inbox-sweep path that populates warm prospects. **WHY:** `untapped warm = 0` two sweeps in a row means either the funnel is dry or the extractor isn't catching new engagers — either way, yesterday's only outbound comments were on `cindyology` and `iheartorganizing` (2 of 5 daily cap used). Room to run. **HOW:** Manually pull commenters on those two media IDs via the existing IG graph helper and queue 3 comments through the standard comment path (well under the 5/day cap).

3. **WHAT:** Trim the post queue's cabinet/under-sink cluster. **WHO/WHERE:** `social/post_queue.json` — head shows two near-duplicate "cabinet where things went to die" / "under the kitchen sink was the place things went" captions back-to-back in a queue of 241. **WHY:** Posting the same "messy cabinet redemption arc" twice inside a week trains the algorithm to flatten reach on both. Queue depth 241 means there is no scarcity reason to keep the dupe. **HOW:** Open `social/post_queue.json`, delete the second under-sink caption (keep the stronger one), commit.

## Content angle to push
**Back-to-school dorm/desk organization, this week only.** Post `18105322931129139` ("dorm room desk isn't the problem — your wall is empty") is the only seasonally-timed post in the last 5 and hits the exact 2-week window when college move-in searches peak. The other 4 posts are evergreen kitchen. Content engine should generate 2–3 more dorm/small-space captions (stick-on shelves, over-door hooks, under-bed bins) queued to fire before Sep 1. Can't quantify the lift because insights are blank (see action 1), but the seasonal window closes regardless.

## Watch-outs
- All 5 recent posts have `likes=? comments=? reach=?` — insights join is broken or insights pull didn't run; fix before this brief is useful again.
- `untapped_warm=0` across two consecutive sweeps — likely extractor issue, not a real dry funnel, given active comment reciprocity yesterday.
- Post queue has two near-identical under-sink cabinet captions in the head — dedupe before either ships.
