# GHP daily strategy brief — 2026-08-30

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-08-30T14:09:06+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

1. **Fix the IG insights pipeline — we are posting blind**
   - WHO/WHERE: `automation/logs/ig_insights_*.json` join in `pi/strategy/` script; last 5 posts (media IDs `18619390060003367`, `18133069864723053`, `18028642553675299`, `18077127896341641`, `17903975967329994`) all show `likes=? comments=? reach=?`.
   - WHY: Every strategy decision below (which angle to double down on, which post to boost via comments, when to prune the 279-deep queue) is unanswerable without insight data. Five consecutive posts with null metrics = broken join or expired IG token, not a slow day.
   - HOW: SSH the Pi, run the insights fetcher manually against one of those 5 media IDs and check whether the failure is auth (rotate token) or a filename/date-window mismatch in the join.

2. **Spend today's engagement budget — 0 outbound actions in 36h**
   - WHO/WHERE: `social/engagement_log.json` shows only `inbox_sweep self` entries at 2026-08-29T13:39Z, 2026-08-29T23:30Z, 2026-08-30T13:39Z. Zero follows/likes/comments logged. Untapped_warm=0 means no one is queued either.
   - WHY: Daily caps (10 follow / 30 like / 5 comment) are the free-tier growth lever. Leaving them at zero while the queue posts into a vacuum wastes the account's warmup.
   - HOW: Manually seed 5-10 targeted likes on hashtag feeds that map to the top-performing angle (see below) so the next auto-sweep has warm-prospect signal to build on.

3. **Diagnose why untapped_warm=0 three sweeps in a row**
   - WHO/WHERE: `pi/dm-funnel/` sweep output; sweeps at 13:39Z and 23:30Z on 08-29 and 13:39Z on 08-30 all produced zero warm prospects.
   - WHY: Either (a) reach genuinely collapsed (correlates with the missing insights above — possible IG API/token issue), or (b) the reciprocity detector is silently filtering everyone. Both are revenue-blocking.
   - HOW: Read the last sweep's raw response before the "count=0" filter to confirm whether IG returned zero interactions or the filter dropped them.

## Content angle to push
**The 4am pain-point / product-as-relief narrative** — the pillow post (media `18028642553675299`, "it is 4am, your neck won't turn left") and the "low-grade mess" story post (`17903975967329994`) both open with visceral I-lived-this scenes rather than tips. That's the format missing from the queue head (all three next-ups are "picture this" or "three weeks ago this cabinet…"-style hook openers, none tied to a concrete Amazon ASIN payoff). Push the next reel/carousel toward: **specific nighttime pain → the one product that fixed it → save-for-later**. Caveat: I cannot cite likes/reach numbers to prove this because insights are null across all 5 posts — this recommendation is based on caption structure, not measured performance.

## Watch-outs
- Insights null on 5/5 recent posts (likely token or join failure); untapped_warm=0 across 3 consecutive sweeps; engagement log has zero outbound actions in 36h; queue depth 279 is high — posting into a measurement blackout is compounding the problem.
