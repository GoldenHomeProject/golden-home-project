# GHP daily strategy brief — 2026-08-16

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-08-16T14:15:16+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Fix the Instagram insights pipeline — blind flying right now**
- **WHERE:** `automation/logs/ig_insights_*.json` join in `pi/strategy/` script; all 5 recent posts show `likes=? comments=? reach=?`
- **WHY:** Every strategy decision (which angle to push, which post to boost with a comment reply, which caption format is working) depends on this. Zero of the last 5 posts have numbers attached. This blocks the entire daily-brief loop from being anything better than a guess.
- **HOW:** SSH the Pi, check the newest `ig_insights_*.json` for freshness and schema — the join is failing (either stale insights file, media_id mismatch, or Graph API pull broken). Fix the join or re-run the insights fetcher before tomorrow's brief.

**2. Audit the engagement log — only 1 action in 36h is broken, not "clean"**
- **WHERE:** `social/engagement_log.json`; sole entry `2026-08-16T13:33:54Z inbox_sweep self`
- **WHY:** With 2x/day sweeps and caps of 10 follow / 30 like / 5 comment, a normal day should show dozens of entries. Combined with `untapped_warm count=0`, either the sweep is short-circuiting (no candidates found) or actions aren't being logged. Either way we're leaving the entire daily engagement cap on the table.
- **HOW:** `ssh raspberrypi5-ts 'tail -200 ~/.ghp-strategy/../engagement_sweep.log'` (or wherever the sweep writes) and check whether the second sweep even ran and whether the candidate query returned zero.

**3. Pull the Fourth-of-July tiered-tray post and re-scan queue for seasonal drift**
- **WHERE:** `media=18162323293466949` (already posted 4th-of-July content on/near 2026-08-16); `social/post_queue.json` head (234 items — needs a seasonal filter pass)
- **WHY:** Off-season holiday posts read as auto-generated and hurt the account's "real human home advice" positioning — the exact voice the CEO memory says drives conversion. One is a bad look; a queue full of them is a slow bleed.
- **HOW:** Grep the queue for "fourth", "july", "halloween", "christmas", "valentine", "easter" and either delete or re-date them to a `seasonal_hold.json` sidecar.

## Content angle to push
**Side-sleeper neck/shoulder pain — pain-first hook, not stat-first.** Recent post led with "74% of us sleep on our sides" (stat). Queue has the same theme reframed as "it is 4am, your neck won't turn left, and the alarm is in 90 minutes." That second frame is far stronger — vivid, first-person, buyer-intent. Push the 4am-neck angle next and hold the stat version until we have insights back to A/B them. **Caveat:** all 5 recent posts show `likes=? reach=?` so this is a copy-craft call, not a data call — revisit once action #1 lands.

## Watch-outs
- `untapped_warm=0` + only 1 engagement entry in 36h = sweep is likely broken or silent, not "we're caught up"; all 5 recent post insights blank; Fourth-of-July content shipped in mid-August; queue depth of 234 with visibly repeated cabinet-under-sink hooks in the head suggests dedup drift.
