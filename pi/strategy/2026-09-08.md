# GHP daily strategy brief — 2026-09-08

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-09-08T14:05:57+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

1. **Get real numbers on the mattress/neck-pain angle before doubling down**
   - WHAT: Manually pull like/comment/reach for `media=18141637246589801` and `media=18188477200398066` (both mattress/neck-pain hooks) from IG insights.
   - WHO/WHERE: `automation/logs/ig_insights_*.json` — the fetcher is returning `?` for likes/comments/reach on all 5 recent posts.
   - WHY: Two of the last 5 posts are the same neck-pain angle, and the queue head has a third (`4am, neck won't turn left`). We're about to triple-down on a hook with zero measured signal.
   - HOW: SSH the Pi, run the insights fetch manually for those media IDs, paste results into next brief.

2. **Re-engage the two commented accounts before the 48h window closes**
   - WHAT: Follow-up like on 1-2 recent posts from `@iheartorganizing` and `@thehomeedit`.
   - WHO/WHERE: Comments posted 2026-09-07 14:08 and 14:10 UTC — ~24h old.
   - WHY: Both are large home-org accounts we just commented on. `untapped_warm=0` means we have no inbound to reciprocate, so the highest-leverage outbound is warming the accounts we already touched. Costs 2 of the 30 daily likes.
   - HOW: On next inbox sweep, add a like-2-recent-posts step for those two handles only.

3. **Diversify the queue away from cabinet/mattress before publishing more**
   - WHAT: Insert 2-3 non-cabinet, non-mattress captions into `social/post_queue.json` head.
   - WHO/WHERE: `social/post_queue.json` — current head is cabinet, mattress, under-sink cabinet (2 of 3 are cabinet dread).
   - WHY: Last 5 posts already include one cabinet + two mattress hooks. Queue head repeats both. Without insight numbers (see action 1) we can't justify the concentration.
   - HOW: Have the content-engine generate 3 captions on a different pain point (see angle below) and prepend to queue.

## Content angle to push
**"The $40 fix I tried after the $1,400 mattress didn't work"** — the mattress posts already lead with the $1,400 anchor (`media=18141637246589801`); the winning follow-up is the cheap subscription-tool fix (pillow height, sleep position tracker, free stretching app). Keeps the neck-pain hook (unmeasured but repeatedly chosen) while making the payoff a subscription/free-tool recommendation — which is where revenue lives per the CEO constraints. Reference the existing `$1,400 mattress → foam topper → chiro` ladder from the current caption and land on a $0-40 solution.

## Watch-outs
- All 5 recent posts show `likes=?  comments=?  reach=?` — the insights join is broken or the fetcher is failing silently; without it, every "what's working" call is a guess.
- `untapped_warm=0` two sweeps in a row is either genuine (nothing inbound) or a detector bug; worth a spot-check of the last inbox sweep log.
- Queue size 300 with heavy cabinet/mattress skew at the head means today's content-engine run may extend the concentration further before we have numbers.
