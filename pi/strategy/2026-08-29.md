# GHP daily strategy brief — 2026-08-29

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-08-29T14:12:12+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Diagnose why engagement caps are 90%+ unused**
- WHAT: 36h of engagement_log shows 0 follows / 0 likes / 0 comments and only 1 DM (`lifeinjeneral` at 2026-08-28T18:11Z). Caps allow 10/30/5 per day — we're leaving 20+ warm touches on the floor daily.
- WHO/WHERE: `social/engagement_log.json`, `pi/dm-funnel/`, `social/dm_funnel_cron.log`, `social/keyword_cron.log`
- WHY: Inbox sweeps ran fine (13:36Z, 23:37Z, 13:39Z) but produced `untapped_warm=0` AND no outbound actions. Either the funnel is silently no-op'ing or the discovery step is returning empty. Revenue depends on this loop.
- HOW: `ssh raspberrypi5-ts 'tail -200 ~/golden-home-project/social/dm_funnel_cron.log ~/golden-home-project/social/keyword_cron.log'` and check whether candidates are being found but skipped vs. never surfaced.

**2. Fix the insights pipeline — every post shows likes=? comments=? reach=?**
- WHAT: All 5 recent posts (18102216248071670, 18619390060003367, 18133069864723053, 18028642553675299, 18077127896341641) return `?` for every metric. We are picking content angles blind.
- WHO/WHERE: `automation/logs/ig_insights_*.json`, whatever GH Action writes it
- WHY: Without per-post likes/reach we can't tell whether the "story hook" captions (4am neck pain, "three weeks ago this cabinet") are the ones actually converting, vs. the flatter "your patio is one furniture set away…" style. Content angle recommendations are guesses until this is fixed.
- HOW: Check the most recent `ig_insights_*.json` file exists and is populated; if empty, re-run the insights fetch workflow manually.

**3. Send today's DM slot to the one warm hand-raiser we already have**
- WHAT: `lifeinjeneral` got a DM on 8/28 — no reply logged yet, but that's the only live thread. If they replied, we owe a follow-up; if not, pick the next candidate manually rather than wait for the funnel that isn't producing (see action 1).
- WHO/WHERE: `lifeinjeneral` IG DMs; `social/engagement_log.json`
- WHY: `untapped_warm=0` means the automated queue is dry. One manual touch keeps the DM cap from being wasted while action 1 gets diagnosed.
- HOW: Open the thread in IG, respond or move on; log the action to `engagement_log.json`.

## Content angle to push
**"Specific-moment story hooks" — the 4am/neck-pain and "three weeks ago this cabinet" openers.** Three of the top-of-queue captions already lean this way (memory-shopping frame + product as resolution). We can't prove it with numbers yet (see action 2), but pattern-wise these outperform the flat "your patio is one furniture set away" construction because they name a time, a body part, or a moment. Content-engine should bias next batch toward: *specific time of day + physical discomfort or frustration + save-this-for-later CTA*. Avoid another "your X deserves a glow-up before [holiday]" — that framing appears once in the last 5 and reads like generic seasonal filler.

## Watch-outs
- **`untapped_warm=0` + zero outbound follows/likes/comments** in 36h — funnel likely silently broken, not "caught up."
- **All post insights are `?`** — insights fetch may have stopped writing; every content decision downstream is flying blind.
- **Queue at 276** is not a problem, but it means content is not the bottleneck — engagement + measurement are.
- **13 untracked `.bak` and `.log` files** in `social/` (per git status) — housekeeping drift; not urgent but worth a sweep this week.
