# GHP daily strategy brief — 2026-06-13

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-06-13T14:07:52+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Diagnose the 6-in-a-row `no_posts_on_profile` comment failures.**
- **WHO/WHERE:** `social/engagement_log.json` entries 2026-06-12T14:03–14:10Z; targets `organizedbyellis`, `iheartorganizing`, `neatmethod`, `cindyology`, `shiragill`, `thehomeedit`. Driver likely `pi/dm-funnel/` or the comment-sweep script.
- **WHY:** Six consecutive errors on the top-tier organizing accounts in a 7-minute window is not "these creators have empty profiles" — it's a scraper regression or IG-side rate signal. We are getting zero comment landings on the most valuable adjacent audiences.
- **HOW:** Manually load one of those profiles in a logged-in session, then re-run the comment-target picker with `--debug` against one handle to see whether the parser is hitting a new IG layout or being soft-blocked.

**2. Fix the post-insights join — we are flying blind on the last 5 posts.**
- **WHO/WHERE:** `automation/logs/ig_insights_*.json` ↔ `social/posted_archive.json` join in the strategy brief script (`pi/strategy/`). All five most-recent media (18179385856400397, 18094617590253170, 18123193945593946, 18117625282835495, 18080292026210433) show `likes=? comments=? reach=?`.
- **WHY:** Revenue-first means we have to know which captions are working. Right now the content engine is generating from intuition, not signal. Yesterday's "what's working" loop is broken.
- **HOW:** Tail the latest `ig_insights_*.json`, confirm those 5 media IDs appear; if absent, re-run the insights fetcher; if present, fix the lookup key in the brief script.

**3. Spend the unused engagement budget on warm-prospect seeding.**
- **WHO/WHERE:** Inbox sweeps ran 3x in 36h (`13:45`, `23:34`, `13:42`) but logged 0 follow / 0 like / 0 comment actions and `untapped_warm=0`. Daily caps 10 follow / 30 like / 5 comment are completely unused.
- **WHY:** Either the sweep is genuinely finding nothing (we should manually seed 1–2 follow targets from saved/reels watchers) or it's silently failing to surface warm prospects. Either way, capacity is sitting idle while we have ~43 posts queued — top-of-funnel is the bottleneck.
- **HOW:** Manually pick 3–5 commenters from the last 5 posts' comment threads, drop them into the warm-prospect file, and let the next sweep act on them within caps.

## Content angle to push

**"Specific-duration-of-suffering" hooks on non-cabinet rooms — push the bedroom/safety vertical next.** Three of the last five captions are kitchen/under-sink cabinet ("avoided opening this cabinet for two whole years", "Three weeks ago this cabinet was where things went to die", "Buying more containers will never fix your under-sink cabinet") and the queue head has another under-sink cabinet post — 4 of 8 recent+upcoming. The "4am, your neck won't turn left" pillow hook and the "plug-in upgrade that makes your home safer after dark" are the diversifiers, and the safety/plug-in angle is the only one with a clean Amazon-affiliate path (outlet nightlights, motion plugs). Generate the next 3 captions in the "I lived with X for [exact duration]" format but in bathroom, nightstand, and entryway/safety territory — not kitchen cabinets.

## Watch-outs

- 6 consecutive `no_posts_on_profile` errors on top organizing accounts in 7 minutes — scraper bug or soft-block, not coincidence.
- Last 5 posts have no insights numbers attached — measurement is broken.
- `untapped_warm=0` with 3 sweeps in 36h while engagement caps sit at 0/10, 0/30, 0/5 — sweep may be silently returning empty.
- Queue head is cabinet-heavy (under-sink theme appearing 3 of next 3) — risk of feed monotony.
