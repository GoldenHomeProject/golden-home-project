# GHP daily strategy brief — 2026-06-03

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-06-03T14:08:50+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Fix the IG insights pipeline — we're flying blind.**
- WHO/WHERE: `automation/logs/ig_insights_*.json` join in `pi/strategy/` brief generator; last 5 media IDs (18315795139302670, 18400197637155262, 18098496266140274, 18465834370100122, 18596156872042762) all returned `likes=? comments=? reach=?`.
- WHY: Strategy is supposed to be evidence-based. Zero of the last 5 posts have metrics resolved — either Graph API auth lapsed, the join key is broken, or the insights fetcher isn't running. Every recommendation downstream is a guess until this is fixed.
- HOW: SSH the Pi, tail the most recent `ig_insights_*.json`, confirm it has rows; if empty, re-auth the IG Graph token and re-run the fetcher manually for those 5 media IDs.

**2. Investigate `untapped_warm=0` — the warm-funnel signal is either dead or not being captured.**
- WHO/WHERE: `social/engagement_log.json` shows only `inbox_sweep` and one `dm_error` in the last 36h — no follows, likes, or comments recorded outbound, and no warm prospects detected inbound.
- WHY: Two inbox sweeps + 1 DM attempt in 36h with **zero** untapped engagers is the suspicious shape of a broken capture, not a clean week. The Pi is sweeping but apparently logging nothing actionable.
- HOW: Manually open IG inbox + recent post notifications, eyeball whether real engagement exists; if yes, the sweep parser is dropping rows — diff the sweep code against last-known-good.

**3. Repair the DM click path that failed on `shiragill` (2026-06-02T18:12:37Z).**
- WHO/WHERE: Playwright locator in the DM module — `get_by_role("button", name=re.compile(r"^Message$", re.IGNORECASE)).first` is being intercepted by `<div class="wbloks_1 wbloks_93 wbloks_91" data-bloks-name="bk.components.Collection">` (an IG overlay/modal).
- WHY: This is the only outbound DM attempt logged and it failed after the full 30s retry budget. If every DM is blocked by this overlay, the outreach arm is silently broken — and shiragill is now a stale lead.
- HOW: Add a pre-click dismiss step for the `bk.components.Collection` overlay (press `Escape` or click `[aria-label="Close"]` first), then retry; manually DM shiragill from your phone in the meantime so the lead doesn't cool.

## Content angle to push

**Break the cabinet/pillow loop — push a "bathroom drawer" or "bedside-table chaos" angle.** The last 5 posts and the queue head are stacked: 3 under-sink-cabinet, 2 pillow/neck-pain (and queue position 1 is *another* cabinet post, position 2 *another* neck post). With no insights resolved (`likes=? comments=? reach=?` on all 5) we cannot prove this is working — and we *can* see we're risking audience fatigue from repetition. Pivot the next content-engine run to a fresh room (bathroom vanity drawer or nightstand) using the same "stop buying more containers" frame that already shipped in the under-sink post — same hook, new surface. Once insights come back online, A/B the engagement of the new room vs the cabinet posts.

## Watch-outs

- All 5 recent posts have `likes=? comments=? reach=?` — IG insights pipeline appears broken.
- `untapped_warm=0` with only 2 sweeps in 36h — likely capture bug, not a clean inbox.
- DM automation failed on shiragill due to IG overlay intercepting clicks — entire DM arm may be down.
- Queue is 27 deep but heavily duplicative (cabinet x2, pillow x1 in the head alone) — risk of repetition fatigue.
- Engagement log shows zero follows/likes/comments outbound in 36h despite the 10/30/5 daily cap — sweep may not be acting, only reading.
