# GHP daily strategy brief — 2026-09-20

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-09-20T14:07:12+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Fix the insights join — every post shows `likes=? comments=? reach=?`**
- WHO/WHERE: `automation/logs/ig_insights_*.json` → the join used by the strategy script (last 5 posts from `social/posted_archive.json`). Check whether the newest `ig_insights_*.json` exists and whether the media_id key matches (`18445369654126368` etc.).
- WHY: All 5 most recent posts return `?` on every metric. Without insights, every downstream decision (which angle works, which caption to reuse, whether the queue skew is justified) is guesswork.
- HOW: `ssh raspberrypi5-ts 'ls -lt ~/golden-home-project/automation/logs/ig_insights_*.json | head -5 && jq "keys | length" $(ls -t ~/golden-home-project/automation/logs/ig_insights_*.json | head -1)'` — if empty or stale, re-run the insights fetch; if fresh, the join key is wrong.

**2. Burn today's engagement budget on real outbound — caps are being wasted**
- WHO/WHERE: `automation/instagram_engagement.py` (or the sweep entrypoint). Last 36h shows only `inbox_sweep self` — zero follows, zero likes, zero comments outbound.
- WHY: Untapped warm = 0 (nobody engaged us to reciprocate to), so the growth vector this cycle has to be cold-warm outreach against tag/competitor accounts. Sitting on 10/30/5 daily caps two days running is 20/60/10 of pure runway lost, and it directly explains why untapped_warm keeps landing at 0.
- HOW: Trigger a targeted sweep against 2-3 adjacent creator tags (dorm/small-space/rental storage — skip `._community` / promo / giveaway per rule). One like+comment pass, not a follow spree.

**3. Stop generating; the queue is 317 deep — pivot the free cycles to monetization**
- WHO/WHERE: `social/post_queue.json` (317 items) vs. anything revenue-adjacent — link-in-bio, affiliate slot in captions, Pinterest crosspost prioritization.
- WHY: 317 posts at 1x/day is ~10 months of runway. Every generator run right now is producing content we won't publish until mid-2027. Revenue-first rule says shift the cycles.
- HOW: Pause the queue-generator cron for 48h; use the freed judgment budget to add an affiliate/CTA slot to the caption template for the next 30 queued items.

## Content angle to push

**Rental-friendly (no-drill, no-paint) storage** — direct extension of what's already in the queue but named for the buyer. Three of the last five captions are storage/organization (bathroom shelves, entryway, cabinet under sink) and the queue head repeats the "cabinet where things went to die" hook, so we know that lane resonates. Give the content-engine an explicit "renter-safe" framing on the next 5-10 pins — renters buy fixes, homeowners just plan them, and it's the highest-intent slice of that same angle. Cannot cite likes/reach numbers because insights returned `?` for all 5 posts (see action #1).

## Watch-outs

- Insights pipeline is silently broken — 5 consecutive posts with no metric data.
- 36h of engagement log is *only* self inbox sweeps — either the outbound sweep isn't running or it's running with an empty target list.
- untapped_warm=0 two runs in a row is consistent with #2 (nothing outbound → nothing to reciprocate to), not necessarily an audience problem.
- Queue at 317 while generator likely still running = compounding waste.
