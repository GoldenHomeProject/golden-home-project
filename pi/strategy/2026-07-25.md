# GHP daily strategy brief — 2026-07-25

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-07-25T14:11:11+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

1. **Investigate the two follow_error events blocking today's outreach quota.**
   - WHO/WHERE: handles `pakhli_manua` and `mentalhealth.fams` from the 2026-07-25T13:45Z sweep in `social/engagement_log.json`; sweep code in `pi/` engagement modules.
   - WHY: Both errors returned `no_state_change`, meaning the sweep burned attempts without progressing the 10/day follow budget. Only 2 untapped warm prospects surfaced, so every follow slot is high-value.
   - HOW: Grep `social/engagement_log.json` for `follow_error` over the last 7 days — if `no_state_change` is chronic, patch the follow handler to skip-and-log rather than counting the attempt.

2. **Reciprocate the 2 untapped warm prospects manually today.**
   - WHO/WHERE: untapped_warm list from the latest inbox sweep (see most recent `automation/logs/` or `social/` inbox artifact).
   - WHY: Warm-reciprocation is the highest conversion signal we have and the count is tiny (n=2) — a human can clear it in 5 min without touching automated caps.
   - HOW: Open each profile, leave one on-topic comment (well under the 5/day cap) referencing their actual post, then follow if relevant.

3. **Kill the duplicate under-sink caption clogging the feed.**
   - WHO/WHERE: media IDs `18063373211740305`, `18110992469058210`, `18099472732957681`, `18130778470554791`, `18122603191697557` — all 5 most recent posts share the identical "Buying more containers will never fix your under-sink cabinet" opener.
   - WHY: IG suppresses near-duplicate captions and the queue head shows a *third* under-sink variant lined up. This is likely why insights returned `likes=? comments=? reach=?` — either the fetch is broken or reach cratered.
   - HOW: Prune duplicate-opener items from `social/post_queue.json` (queue_size=172, room to be picky) and verify the insights fetcher in `automation/` isn't silently failing on all 5.

## Content angle to push

**Neck/shoulder relief at 4am** — the queue item starting *"Picture this: it is 4am, your neck won't turn left…"* is the only non-cabinet angle in the top 3 and breaks a 5-post cabinet monoculture. Push it next: pain-state opener + specific product outcome is our strongest historical hook shape, and after 5 back-to-back under-sink posts the algorithm needs topical variance to re-broaden reach.

## Watch-outs

- All 5 recent posts show `likes=? comments=? reach=?` — insights join is broken OR reach is genuinely zero; confirm before trusting tomorrow's brief.
- 5 consecutive near-identical captions posted — content-engine dedupe is not working.
- 2 follow_errors in a single sweep with `no_state_change` — handler may be silently burning quota.
- untapped_warm=2 is unusually low; verify the inbox sweep actually ran to completion.
