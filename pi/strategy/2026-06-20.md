# GHP daily strategy brief — 2026-06-20

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-06-20T14:04:30+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

1. **Fix the IG comment-posting selector (zero comments landed in the last 24h).**
   - WHO/WHERE: `pi/dm-funnel/` engagement code path that handles `comment_post`; targets affected — `lifeinjeneral`, `organizedbyellis`, `breathing.room.home`, `horderly`, `shiragill`, `simplyspaced`, `cindyology` (all `comment_error` at 2026-06-19T14:03-14:15Z).
   - WHY: 7 consecutive `comment_post` timeouts on the textarea click — a `wbloks_1`/`x1qjc9v5` overlay or `role="dialog"` keeps intercepting pointer events. Comments are the highest-signal action (cap of 5/day) and we landed 0/5 yesterday. That's our best discovery lever sitting broken.
   - HOW: Before `locator.click()` on the comment textarea, dismiss the intercepting overlay (`page.keyboard.press("Escape")` then re-locate, or click the textarea via `locator.focus()` + `page.keyboard.type()` instead of click-then-type) and retry once.

2. **Manually comment on 3-5 prospects today while the bot is broken — don't lose the day's comment cap.**
   - WHO/WHERE: pick from yesterday's failed targets — `organizedbyellis`, `breathing.room.home`, `horderly`, `simplyspaced` (organizing/home niches that map directly to our under-sink + pillow content).
   - WHY: Comment cap is 5/day and we burned 0 of it. These are warm peer accounts the bot already chose as relevant. Hand-posting protects today's reach while the selector fix ships.
   - HOW: From phone, leave one genuine 1-sentence comment on each account's most recent post — reference a specific detail, no link, no pitch.

3. **Capture insights (likes/comments/reach) for the last 5 posts — the strategy loop is flying blind.**
   - WHO/WHERE: `automation/logs/ig_insights_*.json` joiner against `social/posted_archive.json`; affected media IDs 18169107988438159, 18073147433680217, 18455303995112614, 18150970045493792, 17976436875049141.
   - WHY: All 5 posts show `likes=?  comments=?  reach=?` in the brief input. Without these numbers I cannot rank angles by performance, and tomorrow's brief will be just as blind. Queue is 64 deep — we're feeding the funnel without measuring it.
   - HOW: Run the IG insights fetch for those 5 media IDs (or check why the latest `ig_insights_*.json` isn't joining) and re-emit the file the strategy script reads.

## Content angle to push

**"Stop buying organizers — fix the system" angle, applied to a *new* room (bathroom vanity or entryway closet).** The under-sink post ("Buying more containers will never fix your under-sink cabinet") and the cabinet rebuild post are both in the recent-5 *and* the head of the 64-post queue — the angle is clearly working enough that the engine is doubling down on kitchen-sink. Extend it to bathroom/entryway before kitchen fatigue sets in. Lead caption: "I had X years of [problem] every morning. I tried bins. I tried labels. Here's what actually fixed it — and it wasn't another organizer."

## Watch-outs

- 7/7 comment_post attempts failed yesterday with the same overlay-intercept pattern — selector rot, ship the fix today.
- Untapped warm prospects count = 0 and post insights are all `?` — both look like upstream data-fetch breakage (inbox enrichment + insights join), not actual zero state. Verify before trusting tomorrow's brief.
- Queue is 64 deep and 3/3 head captions are kitchen/bedroom — diversify before the audience notices the loop.
