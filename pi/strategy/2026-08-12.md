# GHP daily strategy brief — 2026-08-12

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-08-12T14:13:56+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

1. **Fix the insights join so posts show real numbers.**
   WHO/WHERE: `automation/logs/ig_insights_*.json` → `social/posted_archive.json` join in the strategy brief script (`pi/strategy/*`); affected media IDs 18088070462180529, 18095600717358600, 18032886038830495, 17981514854877564, 17938619823090252.
   WHY: All 5 most recent posts show `likes=? comments=? reach=?`. Without this, every downstream judgment call (which angle to push, which post to boost with a story reshare, whether the queue is landing) is blind — including this brief.
   HOW: `ssh raspberrypi5-ts 'ls -1t ~/golden-home-project/automation/logs/ig_insights_*.json | head -3'` and confirm the join key/schema matches what the strategy script expects.

2. **Seed the warm-prospect pipeline manually — it has gone dry.**
   WHO/WHERE: `social/engagement_log.json` (untapped_warm=0 across 3 sweeps in 36h); the one DM went to `shiragill`. Skip `._community` accounts.
   WHY: Sweeps at 13:34, 23:37, and 13:37 all returned zero untapped warm — the inbox is not producing prospects, so reciprocation-only mode has nothing to reciprocate. Only 1 outbound DM in 36h means we are burning almost none of our 10/30/5 caps.
   HOW: Spend one manual session searching 2-3 pillar hashtags (side-sleeper pillow, small-kitchen-cabinet organization, entryway) and hand-seed 5-8 likes + 2 comments so the next sweep has something to reciprocate against.

3. **Reorder the queue head — two "cabinet-under-the-sink" posts back-to-back.**
   WHO/WHERE: `social/post_queue.json` positions 1 and 3 ("Three weeks ago this cabinet…" and "four years of bottles falling…"). Queue size 230, so there's slack.
   WHY: Same object, same room, same before/after beat — posting them within 48h reads as repetition to the algorithm and to the audience.
   HOW: Swap position 3 with something from a different pillar (sleep, entryway, patio) further down the queue.

## Content angle to push

**Concrete-stat, pain-driven hooks** — modeled on media 17938619823090252 ("74% of us sleep on our sides… 30 degree misalignment"). It is the only one of the last 5 that opens on a hard number rather than a personal anecdote. Since insights are `?` we cannot confirm engagement, but stat-led hooks are the differentiated shape in the last 5-post window; the other four all open with a personal story about a cabinet/patio/entryway. Ask the content engine to draft 2-3 more in this shape (specific %, specific body part or cost, one-line save prompt).

## Watch-outs

- All 5 recent posts show `likes=? comments=? reach=?` — insights pipeline is broken or unjoined, and the daily brief is running blind until this is fixed.
- 3 sweeps in a row returned untapped_warm=0 — either discovery net is too narrow or the inbox_sweep matcher regressed; worth eyeballing the raw sweep output.
- Only 1 outbound action (`dm_sent shiragill`) in 36h despite caps of 10/30/5 — we are massively under-utilizing the daily budget.
