# GHP daily strategy brief — 2026-09-12

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-09-12T14:09:14+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Fix the IG insights pipeline — we are flying blind on revenue signal**
- WHO/WHERE: `automation/logs/ig_insights_*.json` join to `social/posted_archive.json`; all 5 most recent posts (18016589321866793, 18089090465654289, 18141637246589801, 17937615456367750, 18390434311164341) show `likes=? comments=? reach=?`
- WHY: Every post in the last window is missing likes/comments/reach. Without post-level performance we cannot decide which angles are converting to profile visits, DMs, or subs. This blocks every downstream revenue decision.
- HOW: Run the insights fetcher manually for those 5 media IDs and check the log for auth/permission errors — if the Graph token dropped a scope, re-auth; if the join key changed, patch the joiner.

**2. Audit why outbound engagement was 0 in the last 36h**
- WHO/WHERE: `social/engagement_log.json` shows only `inbox_sweep self` at 13:37Z and 23:31Z on 09-11 — no follow/like/comment events
- WHY: Sweeps ran but produced zero outbound actions AND untapped_warm=0. Either the discovery step is returning empty (broken filter / rate-limited / logged-out session) or actions are firing but not being logged. Either way we are burning the daily 10/30/5 budget at 0 utilization — the top of the funnel is dark.
- HOW: On the Pi, tail the last sweep run log and confirm whether the prospect discovery query returned candidates before the action stage; if empty, unblock the discovery source before the next 14:00 UTC sweep.

**3. Purge seasonal + duplicate items from the 309-deep queue head**
- WHO/WHERE: `social/post_queue.json` — head contains a second "cabinet where things went to die" (dupes the just-posted 18089090465654289) and a second neck-pain 4am hook (dupes 18141637246589801). Posted archive still has a July 4th centerpiece caption (18016589321866793) going out in September.
- WHY: Repeating a hook 2 posts apart trains the algorithm we're spammy, and off-season content erodes save/share rate — both directly suppress the reach that drives sub conversion. Queue depth is not the bottleneck; queue quality is.
- HOW: Delete the duplicate cabinet + neck-pain entries at the head, move any remaining Q2/summer-holiday captions to the back, and re-sort so the next 3 posts are seasonally aligned (Sept–Nov: fall pantry, mudroom, holiday prep runway).

## Content angle to push
**Fall reset / "get the house holiday-ready in 6 weeks" runway.** The pantry post (18390434311164341, "Your pantry can look like this by Sunday night") is the only one of the last 5 that's on-season for September, and the queue is bleeding two off-season repeats. Push a fall-reset series with concrete deadlines (Sunday, this week, before Thanksgiving) — high urgency, high DM-intent, and seasonally native for the algorithm right now.

## Watch-outs
- All 5 recent posts have null insights (likes/comments/reach = `?`) — insights fetch or join is broken.
- Zero outbound follow/like/comment logged in 36h despite 2 sweeps completing.
- `untapped_warm=0` — plausible only if discovery is silently returning empty; verify before trusting.
- July 4th centerpiece caption was posted in September — queue scheduler is not enforcing seasonality.
- Cabinet-organization and 4am-neck-pain hooks each appear twice within the posted+queued head — dedupe needed.
