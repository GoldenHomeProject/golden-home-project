# GHP daily strategy brief — 2026-06-05

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-06-05T14:09:50+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Fix the DM "Message" button selector — DM funnel is broken**
- WHAT: The DM funnel timed out at 2026-06-04T18:02Z on `breathing.room.home` because it couldn't locate the `Message` button. Until this is fixed, zero DMs are going out — that's the warmest revenue path silenced.
- WHO/WHERE: `pi/dm-funnel/` (the cron is logging to `social/dm_funnel_cron.log`); failing locator is `get_by_role("button", name=re.compile(r"^Message$", re.IGNORECASE))`.
- WHY: Single `dm_error` in the last 24-36h with `no_message_btn` + `Timeout 8000ms`. IG almost certainly shipped a DOM/label change (often `Message` becomes an icon-only button or moves into an overflow menu on the profile header).
- HOW: Add a fallback chain — try `get_by_role("link", name=/^Message$/i)`, then `get_by_test_id` if present, then an aria-label/href-based selector (`a[href*='/direct/t/']`); raise timeout to 15s and screenshot on failure for next debug.

**2. Pull post insights — every one of the last 5 posts shows `likes=?  comments=?  reach=?`**
- WHAT: Insights are not joining to `posted_archive.json`, so we are flying blind on which angle (sleep vs. under-sink) is actually working. Without numbers, content selection and the daily brief are guessing.
- WHO/WHERE: `automation/logs/ig_insights_*.json` → `social/posted_archive.json` join; media IDs missing data: `18400197637155262`, `18098496266140274`, `18465834370100122`, `18596156872042762`, `17970202323079046`.
- WHY: Yesterday's brief doc explicitly says insights are joined into the input, but every row came back `?`. Either the insights pull is failing silently or the media_id key isn't matching.
- HOW: Run the insights fetcher manually against one of those 5 media IDs, confirm the join key/field names, and add a one-line stderr if the join misses >50% of recent posts.

**3. Trim the post queue head — duplicate "under-sink cabinet" framing back-to-back**
- WHAT: Queue head has two near-identical under-sink openers ("Three weeks ago this cabinet was where things went to die" and "I had four years of bottles falling on my feet"), and the last 5 posted already include a third under-sink post (`18098496266140274`) and a save-worthy under-sink tips post (`18596156872042762`). Posting the same angle 5x in a row tanks reach on a cold account.
- WHO/WHERE: `social/post_queue.json` — reorder so a sleep/pillow post (we have one queued: the 4am neck pain caption) sits between the two under-sink ones.
- WHY: Queue size is healthy at 31, so we have room to interleave without slowing cadence. IG's ranker punishes topical repetition on small accounts.
- HOW: Move the "4am neck won't turn left" caption to slot 2; push the second under-sink caption to slot 3+.

## Content angle to push
**Side-sleeper pillow angle, with the "74% of us sleep on our sides / standard pillows = 30° spine angle" hook.** That post (`18465834370100122`) is the only one in the recent 5 that leads with a hard stat instead of a confessional opener, and it's the angle with the least repetition in the queue. Content engine should generate 2 more side-sleeper variants (one problem-first, one product-comparison) before queuing any more under-sink content — the queue already has at least 3 under-sink pieces and we just posted 2.

## Watch-outs
- `untapped_warm count=0` is suspicious given the inbox sweep ran at 23:38Z — either the sweep is healthy *or* the warm-prospect detector is broken; sanity-check that detection logic ran (it's the kind of silent zero that hides a regression).
- `pi/dm-funnel/__pycache__/` is showing as untracked — confirm `.gitignore` covers it so it doesn't get committed.
- Only 2 engagement events logged in 24-36h (1 dm_error + 1 inbox_sweep) — well under the 10/30/5 caps, but also suggests like/follow/comment sweeps may not be writing to the log; verify they're firing.
