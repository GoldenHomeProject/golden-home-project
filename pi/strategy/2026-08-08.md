# GHP daily strategy brief — 2026-08-08

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-08-08T14:01:15+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Fix the IG insights pipeline — all 5 recent posts show `likes=? comments=? reach=?`**
- WHO/WHERE: `automation/logs/ig_insights_*.json` (join fed to `pi/strategy/`), media IDs 18121474267881973, 17910164364446678, 18070239620714601, 17925775788385747, 18088070462180529.
- WHY: Every one of the last 5 posts came through as `?` on all three metrics. Without this signal, tomorrow's brief is blind on "what's working" and the content-angle recommendation is guesswork. This is the meta-problem — fix it once and every downstream decision improves.
- HOW: `ls -lt automation/logs/ig_insights_*.json | head -3` and check whether the latest file is fresh + has non-null values; if the fetcher is stale/rate-limited, re-run it against those 5 media IDs.

**2. Verify `untapped_warm=0` is real, not a broken classifier**
- WHO/WHERE: `social/engagement_log.json` (last 3 inbox sweeps at 13:44Z / 23:45Z / 13:45Z), inbox-sweep logic in `pi/dm-funnel/`.
- WHY: Three sweeps in 24h returned exactly 0 warm prospects. Given we're actively posting into a 221-deep queue and only reciprocated to 2 people (cindyology, iheartorganizing), a true zero is suspicious. If the classifier broke, we're leaving the comment budget on the table for no reason.
- HOW: Spot-check the raw inbox against the last sweep's output — if there are IG comments/DMs on recent posts that didn't surface as "untapped_warm," the filter needs a look.

**3. Use the unused comment budget — only 2/5 comments posted in 24-36h**
- WHO/WHERE: engagement sweep (Pi 2x/day), cap = 5 comment/day per feedback rules; cindyology + iheartorganizing already hit today.
- WHY: We're at 40% of the daily comment cap with the highest-intent action underused. Comments convert warm audiences; likes don't. Even the 10-follow / 30-like caps show zero activity in the log — either those actions aren't being logged (see #1's pipeline concern) or the sweep isn't executing them.
- HOW: Next sweep, target 2-3 more comments on organization/dorm-season accounts in the same neighborhood as cindyology/iheartorganizing; skip `._community` and promo accounts.

## Content angle to push

**Dorm move-in season, now.** The queue is 221 deep and organization-heavy ("cabinet where things went to die," "closet low-grade mess," "4 years of bottles falling") — good evergreen, but only 1 of the last 5 posts (media 17910164364446678, sage green comforter) is dorm/back-to-school. It's August 8 — the caption itself literally says "before move-in season sneaks up on you." Push 2-3 dorm-move-in posts to the top of the queue this week (under-desk storage, shower caddy, closet doubler) while intent is peaking. Can't cite lift numbers because insights are `?` (see action #1), but the calendar signal alone justifies it.

## Watch-outs

- All 5 posts return `?` on likes/comments/reach — insights fetcher likely stale or broken.
- `untapped_warm=0` across 3 sweeps looks too clean given active posting cadence.
- Engagement log shows zero follows/likes in 24h — either not happening or not being logged.
- 221-item queue = no content shortage; risk is stale/off-season captions surfacing (see dorm angle above).
