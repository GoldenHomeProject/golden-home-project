# GHP daily strategy brief — 2026-08-04

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-08-04T14:11:47+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Fix the insights pipeline — likes/comments/reach are all `?` on the last 5 posts**
- WHO/WHERE: `automation/logs/ig_insights_*.json` join in `pi/strategy/` script; posts `18095933063571256`, `18080660579679095`, `18212838100340628`, `18117400016490500`, `18121474267881973`
- WHY: Every one of the last 5 posts shows `likes=? comments=? reach=?`. Zero signal = zero ability to pick a working angle. This is the highest-leverage fix because it unblocks every other content decision.
- HOW: SSH to the Pi, run the insights fetcher manually against one of the media IDs above and confirm whether the failure is auth/scope, missing log file, or a broken join key.

**2. Investigate the `no_follow_button` skips — likely IG DOM change**
- WHO/WHERE: `social/engagement_log.json` entries at `2026-08-03T23:45:23Z` (`simple_thoughts_lab`) and `2026-08-03T23:45:39Z` (`hausofbreenak`)
- WHY: Two skips in a single sweep with the same reason, on real accounts (not `._community`). If the selector broke, we're silently missing our 10/day follow budget every sweep.
- HOW: Open one of those two profiles in the sweep's headless browser and confirm whether the follow button selector is stale; patch the selector in the follow module if so.

**3. Manually seed 5-10 warm prospects — `untapped_warm=0` is not credible**
- WHO/WHERE: inbox sweep output feeding `untapped warm prospects` count in `pi/strategy/`
- WHY: With 209 queued posts and daily posting cadence, a true zero warm-prospect count over 24h means either engagement collapsed or the detector is broken. Either way, human judgment is needed to unblock the reciprocity flow.
- HOW: Pull the last 20 commenters/likers on the 5 recent posts by hand in IG, cross-check against the reciprocated list, and re-seed anything missed into the follow/like queue (staying under 10 follow / 30 like today).

## Content angle to push
Push the **"$1,400 mattress didn't fix it — a $X pillow/topper did"** angle from media `18117400016490500`. It's the only post in the last 5 with a specific dollar figure and a personal 10-year narrative, which is the highest-differentiation format in the mix (the other four are generic "save this" organizer/rental captions). Queue has three near-duplicate cabinet/organizer captions in the head — the mattress/neck-pain story breaks the pattern and is easier to attribute revenue to (higher-AOV category). Note: I can't confirm this with engagement numbers because all 5 posts show `likes=? comments=? reach=?` — recommendation is based on caption differentiation, not measured performance. Revisit once action #1 lands.

## Watch-outs
- All 5 recent posts missing insights data (pipeline broken); `no_follow_button` appearing twice in one sweep (possible IG DOM change); `untapped_warm=0` is suspiciously clean; queue at 209 while commit `e7a79c5` says "queue exhausted (9th consecutive)" — two different queues or a stale label somewhere.
