# GHP daily strategy brief — 2026-09-06

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-09-06T14:14:11+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Fix Instagram insights ingestion — the strategy loop is blind**
- WHAT: Last 5 posts all show `likes=? comments=? reach=?`. Without post-level metrics, every downstream decision (which angle to push, which post to boost with manual engagement, which to kill) is a guess.
- WHO/WHERE: `automation/logs/ig_insights_*.json` (source), joined by the strategy script to `social/posted_archive.json`. Media IDs `18001186088795007`, `18627445102005153`, `18188477200398066`, `17910006384465924`, `18016589321866793` are all unresolved.
- WHY: 4 of 5 recent posts have zero measured signal — you cannot say "push more X" if X's reach is unknown. This blocks the entire content-angle decision below.
- HOW: `ssh raspberrypi5-ts 'ls -lat ~/golden-home-project/automation/logs/ig_insights_*.json | head -5'` and check whether the fetcher ran or IG token expired; fix the fetcher, don't backfill by hand.

**2. Deduplicate + reorder the 292-item queue before it ships more near-duplicates**
- WHAT: Queue head has TWO under-sink cabinet posts back-to-back, and the last posted archive item was also the under-sink container post. That's 3 in a row on the same trope — IG will de-rank.
- WHO/WHERE: `social/post_queue.json` — items 1 ("Three weeks ago this cabinet…") and 3 ("four years of bottles falling…") are the same product angle as posted media `18627445102005153`.
- WHY: With 292 queued, duplicates are inevitable and the poster runs on schedule with no dedupe. Same-angle triples train the algo that this account is repetitive.
- HOW: Move one of the two cabinet posts to position 30+ in `social/post_queue.json`, and grep the queue for the top-5 caption openers to spot other clusters.

**3. Kill or reschedule the "4th of July centerpiece" post — it's September**
- WHAT: Media `18016589321866793` posted a July-4 CTA on/around 2026-09-05. Off-season seasonal posts read as bot output and hurt account trust with the exact audience (home/DIY buyers) that would otherwise convert.
- WHO/WHERE: `social/posted_archive.json` + wherever the seasonal-caption generator lives. Also check `social/post_queue.json` for other holiday-mismatched captions before the next scheduled post fires.
- WHY: This is a live signal that the caption pipeline has no season gate. One misfire is cosmetic; a queue full of them is a revenue leak.
- HOW: Grep `social/post_queue.json` for `4th of July|Christmas|Halloween|Valentine|Easter|Thanksgiving` and either delete or date-gate them.

## Content angle to push

**Cabinet/drawer "why more containers won't fix it" angle** — with the caveat that this is a *queue-signal* call, not a *performance-signal* call, because insights are broken (see action 1). The under-sink container post (`18627445102005153`) plus two more of the same theme at the queue head suggests the content engine has locked onto this trope. Rather than fight it, pick the ONE strongest cabinet post, ship it with an affiliate-ready product mention (drawer dividers, tension rods, lazy susan — all Amazon-tag friendly), and hold the other two for 2+ weeks apart. The "you don't need more storage, you need the right one thing" hook is high-intent buyer language and matches the "hidden home problem → specific fix" pattern that the smell/mattress/patio posts also follow — this is the account's actual niche.

## Watch-outs

- All 5 recent posts have null insights — fetcher likely broken since at least 2026-09-04.
- `untapped_warm=0` with only 1 follow in 36h means the outreach funnel is empty; caps aren't the bottleneck, discovery is.
- Zero likes/comments in the engagement log for 36h — only inbox sweeps and one follow. Verify the like/comment actions aren't silently failing.
- 292-item queue with visible duplicates at the head means the content generator is outrunning quality control.
