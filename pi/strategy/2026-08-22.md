# GHP daily strategy brief — 2026-08-22

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-08-22T14:05:56+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

1. **Backfill missing post insights so the daily brief can actually see performance.**
   - WHO/WHERE: `automation/logs/ig_insights_*.json`, media IDs 18113695585752130 / 18608922436003615 / 17904995907480402 / 17970042372006033 / 18402009514095260.
   - WHY: All 5 most-recent posts show `likes=? comments=? reach=?` — the brief is flying blind and cannot pick a winning angle without this.
   - HOW: Run the IG insights fetcher manually for those 5 media IDs and confirm the join key in the brief script actually matches; if the fetcher errored, patch and re-run once.

2. **Draw down the 251-deep post queue toward a realistic working set.**
   - WHO/WHERE: `social/post_queue.json` (queue_size=251), archive at `social/posted_archive.json`.
   - WHY: 251 queued captions is months of inventory — any angle learned from insights today will not ship for weeks. That kills the feedback loop and delays revenue.
   - HOW: Prune queue to ~30-50 (keep newest / highest-signal captions, archive the rest to `social/post_queue.json.bak-2026-08-22`) so future content-engine output actually reaches the feed.

3. **Manually seed untapped_warm since automation is returning zero.**
   - WHO/WHERE: DM funnel / inbox sweep pipeline (`pi/dm-funnel/`), last 3 sweeps at 2026-08-21T13:33Z, 2026-08-21T23:34Z, 2026-08-22T13:39Z.
   - WHY: `untapped_warm count=0` for three consecutive sweeps is suspicious given active posting — either the sweep is broken or genuinely no one is engaging. Both are revenue problems.
   - HOW: Spot-check the latest 2-3 posts' likers/commenters manually in IG; if warm prospects exist that the sweep missed, file a bug; if the feed is genuinely dead, that confirms action #1's urgency.

## Content angle to push
**"Under-sink / cabinet-of-doom reset"** — two of the last five captions (media 18113695585752130 and the queue head) already hit this beat, suggesting the content engine is converging on it independently. Without insight numbers we cannot confirm it is winning, but the doubling is a signal worth leaning into with an affiliate-friendly angle: shelf risers, tension rods, stackable bins — high-intent search terms, easy Amazon links, and it aligns with the kitchen/bath niche that's already producing captions.

## Watch-outs
- All 5 recent posts have null likes/comments/reach → insights pipeline likely broken.
- `untapped_warm=0` across 3 sweeps → sweep parser may be silently failing.
- Queue backlog of 251 → new content angles will not reach the feed for weeks.
- Multiple `.bak` files piling up in `social/` (copy_library, dm_keyword_registry, pinterest_queue x3, post_queue, posted_archive) → cleanup drift, worth a `git clean`-adjacent tidy pass.
