# GHP daily strategy brief — 2026-05-27

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-05-27T14:03:51+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Diagnose the 44% follow_error rate before next sweep**
- WHO/WHERE: `social/engagement_log.json`, IG automation selectors on `raspberrypi5-ts`. Errored handles today: padtoscano, fionadeeayewhy, houseofhammer5, weird__designs, weluv._.t, mh.magazines, froggy_in_spaceland.
- WHY: 7 of 16 attempts (~44%) returned `no_state_change` across both sweeps. That's not a profile-edge case — that's selector drift or shadow-throttling. Burning untapped_warm inventory without converting.
- HOW: `ssh raspberrypi5-ts 'grep no_state_change ~/golden-home-project/social/engagement_log.json | tail -50'` and re-test one handle manually; if button DOM changed, patch the selector.

**2. Activate the like + comment budget (currently 0/30 and 0/5 used)**
- WHO/WHERE: top of the 10 untapped_warm queue — including today's successful follows (dra.daniela.slindvain, msfran_kay, lindley__kay, cabellhouse, that_wild_katt) where reciprocation is now expected.
- WHY: Engagement sweep is running follow-only. A thoughtful comment converts to follow-back at materially higher rate than a cold follow, and we have full quota untouched.
- HOW: Add a `like_recent_posts(n=2) + comment_top_post(n=1)` step ahead of the follow action in the next sweep on the warmest 5 prospects.

**3. Fix the post-insights join**
- WHO/WHERE: `automation/logs/ig_insights_*.json` ↔ `social/posted_archive.json` join on media_id (18105064988478438, 17989746005992201, 18117766345684687, 18101976548008102, 18079040495560007).
- WHY: All 5 most-recent posts show `likes=? comments=? reach=?`. Content strategy is currently blind — cannot tell if the cabinet angle is fatiguing or compounding.
- HOW: Run the insights fetcher manually for those 5 media IDs and confirm the join key matches what the strategy script reads.

## Content angle to push

**"The expensive mistake" angle with a concrete dollar figure** — the `$1,400 mattress that didn't fix my neck` caption already sitting #2 in the queue. Push it next. Reasoning from the data: 3 of the last 5 posts are cabinet-organization variants, and posts `18105064988478438` and `18079040495560007` share an identical opening line ("The reason your cabinets stay messy is that nothing has a home") — the cabinet angle is repeating itself in archive. Pillow/sleep vertical is fresh, the queued caption has a hard stat (`74% sleep on side`) and a purchase-regret anchor ($1,400), both of which drive saves and align with the revenue-first directive.

## Watch-outs

- 44% follow_error rate (`no_state_change`) — likely IG selector drift or rate-limit.
- Duplicate caption detected in posted_archive (media 18105064988478438 and 18079040495560007 open identically).
- All recent post insights returning `?` — insights pipeline appears broken.
- Like (0/30) and comment (0/5) budgets unused two sweeps in a row — pure waste.
- `selfelove._community` correctly skipped — `_community` filter is working; no action.
