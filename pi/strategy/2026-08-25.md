# GHP daily strategy brief — 2026-08-25

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-08-25T14:03:12+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

1. **Instrument insights so briefs stop flying blind.** WHAT: The last 5 posts all show `likes=? comments=? reach=?` — the join between `social/posted_archive.json` and `automation/logs/ig_insights_*.json` is broken (or insights aren't landing). WHO/WHERE: `automation/logs/ig_insights_*.json`, the script that generates this brief in `pi/strategy/`, and `social/posted_archive.json`. WHY: Without per-post metrics, no one (human or Claude) can pick a winning angle — every downstream recommendation below this line is a guess. HOW: `ls -lt automation/logs/ig_insights_*.json | head -5` and confirm the newest file has media IDs matching the last 5 posts; if missing, re-run the insights fetcher; if present, fix the join key in the strategy script.

2. **Manually seed 3-5 warm prospects into the funnel.** WHAT: `untapped_warm count=0` with 3 recent inbox sweeps means either (a) genuinely nobody is engaging, or (b) the untapped-detector logic is stale. WHO/WHERE: `pi/dm-funnel/` + `social/dm_keyword_registry.json` — check what "engaged us" means in code. WHY: Zero warm leads for 24h across 3 sweeps is a revenue-critical signal; DM funnel is the highest-intent path we own. HOW: Grep the sweep logic for the "engaged but not reciprocated" filter, hand-verify against 2-3 recent post comment threads on IG, and if the filter is correct, spend today's 5-comment cap on high-signal replies to comments on the mattress/neck-pain post to prime the funnel.

3. **Trim the 265-post queue before adding more.** WHAT: Queue depth of 265 items means new content-engine output sits weeks behind current trends. WHO/WHERE: `social/post_queue.json` (backups already exist: `post_queue.json.bak`, `.bak2`, `.bak3`). WHY: Revenue-first + subscription-only means every post slot is precious; a 265-deep queue guarantees seasonal drift (the July 4th tiered-tray post already published shows this — it's late August). HOW: Prune anything holiday/seasonal past its window and anything older than 30 days from queue insertion, keeping evergreen pain-point posts (under-sink, closet, hot-sleeper) at the top.

## Content angle to push

**Neck/shoulder pain from sleep setup** — the mattress/neck-pain post (`18402009514095260`) and the queued 4am-neck-won't-turn variant are both hitting the same nerve, and it's the only pain-point in the recent set with a clear physical-symptom hook (vs. generic "organize your cabinet"). Push one more angle in this cluster: pillow height + side-sleeper alignment, tying to the same "you blamed the wrong thing" framing that's showing up twice already. Real numbers are unavailable (see action #1), but two independent captions converging on this pain is itself the signal.

## Watch-outs

- All 5 recent posts show `likes=? comments=? reach=?` — insights pipeline is broken or unjoined.
- `untapped_warm=0` across 3 consecutive sweeps — suspicious; verify the detector, don't trust it silently.
- 14 untracked backup files (`.bak`, `.bak-*`, `__pycache__/`, cron logs) cluttering `social/` — housekeeping drift, add to `.gitignore` or clean up.
- Queue at 265 with a July 4th post already having gone out in late August — seasonal FIFO discipline is slipping.
