# GHP daily strategy brief — 2026-09-05

Generated by `pi/strategy/daily-strategy.sh` on RaspberryPi5 at 2026-09-05T14:08:04+00:00.

**Inputs:** engagement log (last 36h), untapped_warm count, last 5 posts + insights, post queue head, prior brief.

## Top 3 actions for next 24h

**1. Turn on insights capture — the strategy loop is flying blind.**
- WHO/WHERE: `automation/logs/ig_insights_*.json` (empty/missing joins for all 5 recent posts); `social/posted_archive.json`
- WHY: All 5 recent posts show `likes=? comments=? reach=?`. Without engagement numbers we cannot rank which of the 291 queued captions to prioritize, and every future brief degrades to vibes. This is the single biggest leverage move because it unlocks every other decision.
- HOW: SSH the Pi, check whether `ig_insights_*.json` is being written (`ls -lt ~/golden-home-project/automation/logs/ig_insights_*.json | head`); if the fetcher is stale or the token expired, refresh the IG Graph token and re-run the insights job for the last 5 media IDs.

**2. Comment on mcmillansonia's recent post to convert yesterday's follow into a real touch.**
- WHO/WHERE: `@mcmillansonia` (follow logged 2026-09-05T13:36:45Z, reason `follow_back_untapped_warm`); untapped_warm queue now empty (count=1 → 0 after this follow)
- WHY: They engaged us, we followed back, but a silent follow is the weakest form of reciprocation. One thoughtful comment (well under the 5/day cap) is what actually opens DMs and drives the funnel toward revenue. Warm queue is dry, so this is the highest-quality outreach available today.
- HOW: Open their profile, comment 1 specific line on their most recent home/organization post (skip if `._community` / promo). Log it in `social/engagement_log.json` with reason `warm_reciprocate`.

**3. Refill the warm-prospect pipeline — count=1 is a supply problem.**
- WHO/WHERE: Inbox sweep script that populates `untapped_warm` (runs at ~13:36 / ~23:38 UTC per log)
- WHY: With only 1 untapped_warm in 36h, the follow/comment caps (10/5) are massively underutilized — we're leaving 9 follows and 4 comments on the table daily. The bottleneck is discovery, not action budget.
- HOW: Add one lightweight discovery pass to the next inbox sweep — pull commenters from the last 5 of our own posts (media IDs above) who aren't already followed, filter out `._community`/promo, and enqueue as `untapped_warm`. No cadence change to the sweep itself.

## Content angle to push
**"Under-sink cabinet reset"** — the queue already has 2 variants of it (three-weeks-ago cabinet, four-years-of-bottles) and the recent post `media=18627445102005153` ("Buying more containers will never fix your under-sink cabinet") is one of the 5 most recent, suggesting it's a lane the engine keeps returning to. Push the next under-sink variant to the front of the 291-deep queue and A/B the hook (containers-don't-fix-it vs. things-went-to-die) so that once insights capture is restored (action 1), we get a clean read on which hook wins. Rationale is structural, not numeric — we literally cannot cite likes/reach until action 1 lands.

## Watch-outs
- All 5 recent posts have null insights (likes/comments/reach) — insights pipeline is broken or the join key is off; fix before next brief.
- `untapped_warm=1` after a sweep is suspiciously low — either discovery is under-scoped or the sweep silently failed to enqueue.
- Post queue at 291 with no visible ranking signal means FIFO is burning good captions on bad days; not urgent, but worth a scoring pass once insights are back.
