# GHP self-review — 2026-06-15

Generated by `pi/learnings/self-review.sh` on RaspberryPi5.

## NO PROPOSALS — week was clean (of *new* recurring failures)

This is the third consecutive week the only recurring pattern is the one already journaled. PR #4 (`learnings/2026-06-01`) and PR #5 (`learnings/2026-06-08`) both correctly returned NO PROPOSALS for the same reason; nothing has changed since.

**Pattern observed (last 7d, 2026-06-08 → 2026-06-15, 21 events):** all three watchdog targets log `"http":"000000","status":"fail"`. Triple-simultaneous fails at `2026-06-09T17:45–17:56–21:15–21:38` and the `06-13` cluster (`08:20`, `17:54`, `20:33`) confirm Pi-local link drops, not three independent provider outages. The `000000` sentinel still originates at `pi/watchdog/ghp-watchdog.sh:58` — `curl`'s `%{http_code}=000` (connect-level fail) concatenates with the `|| echo "000"` fallback.

**Already journaled — must not re-propose:**
- PR #2 (`learnings/2026-05-18`): `curl --retry 2 --retry-delay 2 --retry-connrefused` at `pi/watchdog/ghp-watchdog.sh:58`. **Still unmerged** (file at HEAD is unchanged at that line).
- PR #3 (`learnings/2026-05-25`): pre-cycle gate probing `https://1.1.1.1/`, log `status:"skip"` when Pi has no WAN. **Still unmerged** (no gate present around `ghp-watchdog.sh:94`).

**GitHub Actions failures:** none in window (`actions_failures` for PR #4 was `0\n0`, PR #5 was `1` — single one-off, not recurring).

**Repo error logs:** none.

**Recommended action (not a new proposal):** merge PR #3 — additive gate, byte-identical behavior when WAN is up, would have eliminated all 21 events this week and ~128 events over the trailing 30 days. The bottleneck is review/merge, not idea generation; a fourth variant would be noise.
