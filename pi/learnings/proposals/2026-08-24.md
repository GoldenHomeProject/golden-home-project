# GHP self-review — 2026-08-24

Generated by `pi/learnings/self-review.sh` on RaspberryPi5.

## NO PROPOSALS — week was clean.

**Analysis window:** 2026-08-17 → 2026-08-24.

**GitHub Actions — 4 Pages failures on 2026-08-17 (15:00–18:14 UTC):** already fixed in the last 7 days. `git log .github/workflows/pages.yml` shows five commits landed since (`b493718` → `d46d092`) that (a) rewrote the artifact-staging path so `_site` no longer exceeds the 1 GB Pages limit, (b) added a 3-attempt retry ladder for transient Pages 503s at `.github/workflows/pages.yml:144-170`, and (c) added `paths-ignore` at `.github/workflows/pages.yml:28-35` so social/*.json queue churn no longer triggers deploys. The workflow header at `.github/workflows/pages.yml:3-16` explicitly credits those exact 08-17 failures as the reason. No new patch is safe here without regressing that work.

**Watchdog — sustained multi-endpoint outage 2026-08-15 19:41 → 21:07:** all three targets (`goldenhomeproject.com`, `meta_graph_api`, `ntfy_server`) returned `http:"000000"` in lockstep every 90 s for ~85 minutes. The lockstep across three unrelated third parties is the signature of a local Pi uplink drop, not three real service failures. This is a **single incident** during the analysis window and therefore a one-off per the ground rules. The `FAIL_THRESHOLD=2` logic at `pi/watchdog/ghp-watchdog.sh:19` did trip, but the `notify()` call at `pi/watchdog/ghp-watchdog.sh:44-53` targets `ntfy.sh`, which is unreachable during exactly the outage it needs to report — an inherent property of a single-Pi push architecture, not a patchable bug within the constraints (any fix requires either a second exfil path = new paid service, or client-side queueing = new infrastructure, both explicitly disallowed).

**Watchdog — 26 remaining lone-cycle `000000` blips across 08-15 → 08-24:** identical pattern to the 2026-07-27, 2026-08-03, and 2026-08-10 journal entries. Each is a single 90 s cycle, does not cross `FAIL_THRESHOLD=2`, and reflects upstream/Pi transience. **Journaled do-not-repropose** — the `000000` cosmetic double-fallback at `pi/watchdog/ghp-watchdog.sh:58` has been explicitly skipped three weeks running per the "no cosmetic refactors" rule.

**Repo error logs:** empty.

**Prior-week self-review 2026-08-17:** did not produce a proposal (`claude` CLI hit its session limit), so nothing to reconcile.

Nothing recurring and patch-worthy this week under the constraints.
