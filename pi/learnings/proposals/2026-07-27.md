# GHP self-review — 2026-07-27

Generated by `pi/learnings/self-review.sh` on RaspberryPi5.

Looking at the last 7 days (2026-07-20 through 2026-07-27):

**Watchdog failures in window:** 7 total events, all `http:"000000"` (curl connection failures):
- goldenhomeproject.com: 3 isolated fails (07-25 ×2, 07-26)
- meta_graph_api: 2 isolated fails (07-26, 07-27)
- ntfy_server: 2 isolated fails (07-26, 07-27)

**Analysis:**
- Every failure is a single-cycle transient. None are consecutive within a target, so `FAIL_THRESHOLD=2` in `pi/watchdog/ghp-watchdog.sh:19` never tripped — no false alerts fired.
- Root cause of each blip is upstream / Pi network, not GHP code.
- GitHub Actions failures: empty.
- Repo error logs: empty.
- No revenue path was interrupted.

The `http:"000000"` (six zeros instead of three) at `pi/watchdog/ghp-watchdog.sh:58` is a real code smell — `-w '%{http_code}'` already emits "000" on connect failure, and the `|| echo "000"` fallback then appends another "000". But this is cosmetic (regex match on line 59 works either way, and no downstream parser expects 3-digit format), and the ground rules explicitly say "skip cosmetic refactors."

## NO PROPOSALS — week was clean.
