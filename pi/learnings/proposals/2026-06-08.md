# GHP self-review — 2026-06-08

Generated by `pi/learnings/self-review.sh` on RaspberryPi5.

I've cross-referenced this week's failures against the journal. The watchdog 000000 pattern has been proposed twice already (PR #2 and PR #3, both still unmerged on main — `pi/learnings/proposals/` contains only `.gitkeep`).

## NO PROPOSALS — week was clean (of *new* recurring failures)

The only recurring pattern this week is the same one journaled twice and explicitly skipped in the 2026-06-01 review.

**Pattern observed (32 events, 2026-06-01 → 2026-06-08):** all three watchdog targets log `"http":"000000","status":"fail"`. Triple-simultaneous fails at `2026-06-04T01:03:58`, `01:04:59`, `01:12:59` (all three targets to the second) prove it's Pi-local link drop, not three independent provider outages. The `000000` sentinel originates at `pi/watchdog/ghp-watchdog.sh:58` when curl's `%{http_code}=000` (connect-level fail) concatenates with the `|| echo "000"` fallback.

**Already journaled — must not re-propose:**
- PR #2 (`learnings/2026-05-18`): `curl --retry 2 --retry-delay 2 --retry-connrefused` at `ghp-watchdog.sh:58`. Unmerged.
- PR #3 (`learnings/2026-05-25`): pre-cycle gate probing `https://1.1.1.1/`, log `status:"skip"` when Pi has no WAN. Unmerged (`ghp-watchdog.sh` shows no gate at line 94).

**GitHub Actions failures don't qualify as new recurring:**
- 2 × `Pipeline Health Watchdog` (2026-06-04 15:20 + 20:08) were the missing-label bug fixed by commit `92348f0` later the same day (2026-06-04 21:52). Post-fix runs aren't failing.
- 1 × `GHP Instagram Poster` (2026-06-03) — single one-off, doesn't qualify.

**Recommended action (not a new proposal):** merge PR #3 — it's the cheaper, additive gate; behavior is byte-identical when WAN is up. Re-proposing a third variant of the same fix would be noise — the bottleneck is review/merge, not idea generation.
