# GHP self-review — 2026-06-01

Generated by `pi/learnings/self-review.sh` on RaspberryPi5.

## NO PROPOSALS — week was clean (of *new* recurring failures)

The only recurring failure in the input is the same one already covered by two unmerged proposals in the journal:

**Recurring pattern observed (75 events, 7 days):** all three watchdog targets log `"http":"000000","status":"fail"`. The string `000000` can only be produced by `curl ... || echo "000"` at `pi/watchdog/ghp-watchdog.sh:58` when curl's `%{http_code}=000` (connect-level failure) concatenates with the fallback `echo "000"`. Triple-simultaneous fails (e.g., `2026-05-17T07:12:27`, `07:13:29`, `07:14:29`; `2026-05-25T17:56:01`; `2026-05-26T12:28:03`, `12:29:05`, `12:51:07`; `2026-06-01T…`) are Pi-local link drops, not three independent provider outages.

**Already in the journal — must not re-propose:**
- PR #2 (`learnings/2026-05-18`): add `--retry 2 --retry-delay 2 --retry-connrefused` to `check_one()` curl. *Still open / unmerged — `pi/watchdog/ghp-watchdog.sh:58` is unchanged.*
- PR #3 (`learnings/2026-05-25`): pre-cycle gate that probes `https://1.1.1.1/` and logs `status:"skip"` when the Pi has no WAN. *Still open / unmerged — no gate present in `ghp-watchdog.sh`.*

Either of those patches would have eliminated this week's 75 events. Re-proposing a third variant of the same fix would be noise — the bottleneck is review/merge, not idea generation.

**Recommended action (not a new proposal):** merge PR #2 or PR #3 (PR #3 is the cheaper, more conservative one — pure additive gate, byte-identical behavior when network is up). No code change here.

No GitHub Actions failures and no repo error logs this week, so nothing else qualifies as recurring.
