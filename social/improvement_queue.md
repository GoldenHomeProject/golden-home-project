# GHP Improvement Queue — bounded fuel for autonomous sessions

Each item is a concrete on-page or research task that compounds when traffic eventually arrives via SEO.

**Rules:**
- Each session does ONE item, not all.
- Mark with `[done YYYY-MM-DD commit-sha]` when shipped.
- When the queue is empty, **refuse to invent busywork.** Log "queue exhausted, ending" and stop. The bottleneck is GSC indexing/ranking, which I cannot accelerate by running more.
- Adding new items is allowed ONLY when grounded in a fresh competitive teardown or actual traffic data — not speculation.

---

## On-page SEO improvements (existing 8 COVER posts)

### High leverage
- [x] ~~**TOP PRIORITY 2026-05-14 — Content engine ASIN hallucination + wrong affiliate network.** Tonight's audit found two upstream pipeline bugs that have produced zero shipped reels for 3+ weeks (30+ in `automation/content-review-queue/`): (1) `reel-2026-05-13-002.json` mapped ASIN `B01M0TS64K` (= Mamma Mia waterproof sofa cover) to "Simple Houseware 2-Tier Sliding Basket Organizer" — same dead-ASIN failure mode as 2026-04-25 incident (0/902 conversion). Fix: add ASIN-to-product validation to `automation/brand_voice_guard.py` or content_engine prompt — reject any reel where the ASIN's known product (from a small validated catalog) doesn't substring-match `primary_product`. (2) `reel-2026-05-13-001.json` + `-003.json` both targeted Eli & Elm Side Sleeper Pillow with caption "Amazon affiliate" — but task #47 rerouted Eli & Elm to Impact (20-30% commission vs Amazon 3-4%). The content engine prompt or affiliate-strategy resolver wasn't updated. Fix: when `primary_product` matches Eli & Elm or Mamma Mia, force `affiliate_network: "impact"` and rewrite caption disclosure line.~~ **[done 2026-05-14 75c9d77]** — Bug (2) root cause: `_disclosure_for` matched the network name `"impact"` exactly, but the registry uses subnet-qualified names (`impact_direct` for Eli & Elm, `impact_maas_to_amazon` for Mamma Mia). Every Impact-rerouted reel for 3+ weeks silently fell through to the Amazon default disclosure. Switched to prefix match (`impact*`, `cj*`, `shareasale*`, `awin*`); verified end-to-end with dry-run that PILLOW now ships "Affiliate link" disclosure + Impact destination URL stamped, LINK keeps "Amazon affiliate". Also added `affiliate_network` + registry `affiliate_url` to every emitted `affiliate_strategy` block so downstream attribution + DM routing have ground truth. Bug (1) root-cause investigation found past-me's claim was wrong: the registry maps B01M0TS64K → Simple Houseware (consistent with the reel) and B0B4SPP3ZN → Mamma Mia (separate ASIN). Reel-002's ASIN/product mapping is internally consistent. But the underlying lever — preventing a future inconsistent registry edit from shipping — is still right. Added `ASIN_CANONICAL_PRODUCT` catalog to `content_quality_gate.py` as an independent second source of truth (10 verified ASIN → brand substring mappings); validator rejects reels where `primary_product` fails to contain the canonical substring. Tested: a synthetic ASIN/product mismatch (B07YL7VD32 paired with "Mamma Mia" name) correctly rejected with explicit reason; valid pairings pass. Honest scope note: this fix does NOT yet route the Impact destination URL into the DM message itself — that's a downstream concern in the DM automation, not the content engine. Logging as separate prediction (#13) so the disclosure fix is evaluable on its own.
- [x] ~~**TOP PRIORITY 2026-05-13 — Wire daily-poster (YT/FB) to consume reel-producer output instead of static calendar.** Today's 2026-05-12 cron returned `"no_content_scheduled"` because `automation/github_daily_poster.py` SAMPLE_CALENDAR is hard-coded through 2026-05-10. This breaks the YT half of the parallel-channel attribution test (prediction #8). Fix: replace the static array with a function that reads today's reel-producer output from `social/post_queue.json` or the reel JSON. Without this, the 5/25 evaluation of prediction #8 is degraded — we'd have IG-only data and couldn't tell if YT works.~~ **[done 2026-05-13 86d53fe]** — `main()` now falls back to `pick_dynamic_entry(today)` when APRIL_CALENDAR misses; helper reads `automation/scripts/reel-<date>-NNN.json`, requires a matching `social/reels/*.mp4` AND a non-empty `affiliate_strategy.amazon_asin` (no ASIN → skip, so we never post unattributed reels). New `pages_video_url()` routes FB/IG `file_url` at `https://goldenhomeproject.com/social/reels/<file>` for dynamic entries; verified 200 video/mp4. IG posts daily (not Saturday-only) when entry is dynamic, since caption_override already carries the DM-funnel CTA. Log gains `"source": "calendar"|"reel_producer"` so engagement-monitor can tell which path fired. Tested locally: 2026-05-12 (was broken) now resolves to reel-2026-05-12-003 (B01M0TS64K, "Buying more containers..."), 2026-05-01 still prefers calendar entry, 2026-05-13 returns None (today's reel hasn't generated yet — content-engine fires before noon ET cron).
- [x] ~~**Internal cross-links between the 8 posts.** Each post links to 2-3 sibling posts in a "Related reviews" block. Builds topical authority signal; distributes any future PageRank evenly across the cluster.~~ **[done 2026-05-08 485d472]** — Ring-link graph (i+1, i+3, i+5 mod 8): 24 internal edges, 3 in / 3 out per post, zero self-links, zero broken targets. Rendered as semantic `<aside class="related-reviews" aria-label="Related reviews">` before the `.back-link` anchor on every post.
- [x] ~~**Open Graph + Twitter Card tags.** og:title, og:description, og:image, twitter:card="summary_large_image". When posts ARE shared (currently rare), shares render with rich previews → measurably higher CTR.~~ **[done 2026-05-09 e3a9269]** — Injected og:image, og:image:alt, og:site_name, og:locale + twitter:card="summary_large_image", twitter:title, twitter:description, twitter:image, twitter:image:alt across all 8 COVER posts via /tmp/inject_og_twitter.py. og:image points to the Amazon product CDN URL already published in each post's Product schema (no new external exposure). Idempotent injector; HTML parse-clean; zero duplicates.
- [x] ~~**About page (`/about.html`)** with real author bio, contact, "I bought Mamma Mia covers with my own money" disclosure paragraph. Then update Person schema across all 8 posts to reference `/about.html#author`. EEAT signal compounds across every page.~~ **[done 2026-05-10 db18b16]** — Created /about.html with AboutPage + Person JSON-LD (@id=https://goldenhomeproject.com/about.html#author), real bio, hands-on testing methodology, affiliate disclosure ("I bought my Mamma Mia waterproof stretch sofa cover with my own money in 2026 before any affiliate relationship existed"), editorial-independence statement, contact email. Injected url=https://goldenhomeproject.com/about.html#author into the Person object inside Product/Review JSON-LD across all 8 COVER posts via /tmp/inject_person_about.py. Added /about.html + previously-missing 2026-05-06 post to sitemap.xml (9 → 11 URLs). Validation script /tmp/validate_about_change.py confirms all JSON-LD parses, exactly 1 Person ref per post, sitemap XML valid.
- [x] ~~**Image alt text + filename audit.** Currently most images use generic filenames (e.g., `couch1.jpg`). Rename to keyword-rich slugs (`mamma-mia-cover-after-six-weeks-pet-test.jpg`) and write descriptive alt text. Free image-search traffic over time.~~ **[N/A 2026-05-11]** — Verified via `grep -c "<img"` across all 8 COVER posts: zero `<img>` tags exist. The only image references are `og:image` / `twitter:image` meta tags + JSON-LD `image` properties, all pointing to Amazon CDN URLs we cannot rename. The queue item premise (generic in-body filenames) was incorrect. Self-eval catch: past-me wrote a queue item without verifying actual DOM state.

### Medium leverage
- [ ] **Table-of-Contents block + jump-link anchors** on long posts. Increases dwell time signal; helps featured-snippet eligibility for "how do I..." queries within the post.
- [x] ~~**Comparison table** added to at least one pillar post: Mamma Mia vs SureFit vs RHF (the actual competitors per Good Housekeeping list). Tables get featured-snippet treatment in SERP ~3x more than prose.~~ **[done 2026-05-12 b5128f7]** — Inserted 6-row spec comparison table into the 2026-04-23 pillar post (stretch axis, waterproof layer, anchor system, construction, machine washable, price). Mamma Mia column reflects 6-week hands-on testing; SureFit + RHF columns sourced from public Amazon listings (SureFit Stretch Pique 94/6 poly-spandex bi-elastic; RHF Reversible Pet Cover not advertised waterproof). Source caption attributes manufacturer claims explicitly per the 2026-05-03 unverified-claim rule. Bumped schema dateModified to 2026-05-12 (honest — content materially changed) and added visible "Updated 2026-05-12" line. Validated: all 3 JSON-LD blocks parse, table renders with mobile fallback CSS, affiliate link preserved.
- [x] ~~**FAQ rotation:** current 6 questions are near-duplicates across all 8 posts. Diversify to 8-10 unique questions per post by mining "People Also Ask" boxes. Each unique question is a separate search-eligible snippet.~~ **[done 2026-05-15 2d84b26]** — Audit-first revealed queue premise was partly wrong: each post already had 6 differently-worded questions, but those 48 questions clustered into the same 6 themes (fit, waterproof, wash, anchors, sectional, durability) so cluster-wide unique snippet eligibility was ~6. Shipped `/tmp/inject_faq_rotation.py` adding 3 cluster-unique PAA-style questions to each of the 8 COVER posts (24 net-new questions, zero theme overlap across posts). Themes added: install-time, throw-pillows compat, waterproof-crinkle noise, residue on sofa, matching loveseat, hot-mug heat damage, reclining sofa fit, attached vs loose cushions, slip when sitting, odor removal, sunlight fade, ironing, vacuum between washes, oversized/chesterfield fit, dryer temp safety, shipping/Prime, skin-contact safety, wrinkle release, toddler-and-pet household, XL vs standard dimensions, vs seat-protector mat, elastic wear over time, washing with other laundry, return policy. All answers grounded in the 6-week hands-on testing already documented in the pillar review or the Mamma Mia public product listing — no manufacturer certifications stated (per 2026-05-03 unverified-claim rule). Validated end-to-end: all FAQPage JSON-LD parses, exactly 9 questions per post in both schema and visible body, schema/visible parity holds for every post, all 24 new questions unique cluster-wide.
- [ ] **Last-modified dates** in schema + visible body. "Updated 2026-XX-XX" beats "Published 2026-04-23" for evergreen review intent.

### Low-but-easy
- [x] ~~**Canonical URL tags** explicitly set per post (rel="canonical"). Cluster has near-duplicate content; explicit canonicals prevent Google picking the wrong one.~~ **[N/A 2026-05-12]** — Verified all 8 COVER posts already carry self-referential `<link rel="canonical" href="https://goldenhomeproject.com/blog/posts/...">` tags (line 8 of each file). Likely set during the original post template or schema-injection commits. Second occurrence of the 2026-05-11 alt-text pattern: past-me wrote a queue item without verifying actual DOM state. Logging again so the failure mode stays visible.
- [ ] **Schema validation pass:** run all 8 posts through schema.org validator, fix any warnings. (Already JSON-valid; this is markup-validity check.)
- [x] ~~**Mobile viewport / preload hints** in `<head>` for Core Web Vitals.~~ **[done 2026-05-13]** — Audit-first: all 8 posts already had `viewport` + a `preconnect` to `fonts.googleapis.com`, but the real font-byte CDN `fonts.gstatic.com` was cold and the og:image host `m.media-amazon.com` had no DNS hint. Shipped via `/tmp/inject_perf_hints.py` to all 8 COVER posts: (a) `preconnect` to `fonts.gstatic.com` with `crossorigin` — saves ~100-300ms first-font-byte on cold visits, (b) `dns-prefetch` to `m.media-amazon.com` — warms resolver for og:image render in share previews + Product-schema image, (c) `theme-color="#0a0a0a"` — mobile Chrome address bar matches palette. Idempotent injector; validation script confirms exactly one `fonts.gstatic.com` ref per file, all 3 JSON-LD blocks still parse, Google Fonts CSS link intact. Honest scope: queue item's "preload" clause N/A — 2026-05-11 audit confirmed zero `<img>` tags in body, no LCP image candidate to preload.

## Research / strategy (compounds for Stage-1 fallback)

- [x] ~~**5 fallback keyword clusters** mapped via Google autocomplete + DDG SERP teardown. Goal: have 5 alternatives ready if the COVER cluster shows zero impressions on 2026-06-05. Each candidate gets: estimated competition (top-3 SERP authority), search-intent classification, available affiliate offers, real product to anchor reviews.~~ **[done 2026-05-11 c618d9e]** — `social/RESEARCH_fallback_clusters.md` written. 5 clusters teardown'd: (1) pet mattress protectors — STRONGEST/primary pivot, (2) under-desk foot warmers — STRONG/backup, (3) garage door insulation — MEDIUM, (4) small chest freezer — WEAK, (5) quiet window AC — WEAK. Decision rule pre-committed: on 2026-06-05 if COVER impressions = 0, pivot to Cluster 1 first, Cluster 2 second; do not try 3-5 first. All commission rates marked VERIFY per the 2026-05-03 unverified-claim rule.
- [ ] **Competitor backlink profile** of Modern Day Pets (#3 result) via free `linkminer.com` or similar. They're a niche site — what does their backlink graph look like? Inform whether GHP can realistically match it.
- [ ] **3 first-sale Reddit case studies** from r/Affiliatemarketing — read 3 posts dated 2024-2026 from accounts that earned their first $1-100. Identify common patterns. Compare to GHP's setup. Write findings to `social/RESEARCH_first_sale_patterns.md`.
- [ ] **Brand discoverability test:** submit goldenhomeproject.com to free directories (DMOZ-likes, niche home-improvement directories). Tiny backlink boost; takes <30 min total.

## Done

- [done 2026-05-06 ccf0426] Add Product+Review+Person schema to all 8 COVER posts. Matches #1 SERP (Good Housekeeping) machine-readable signal stack.
- [done 2026-05-05 a91dd47] Submit sitemap.xml + robots.txt to GSC. 9 URLs discovered.
- [done 2026-05-05 803b7e4] GSC ownership verification (HTML file method).
